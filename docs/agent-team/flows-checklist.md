# Treno flow inventory & coverage log

_The QA agent works through this list **round-robin**: each run it picks the next flow
(the one with the oldest `last_checked`), tests it end-to-end on the TEST environment,
and appends a result to the Coverage log at the bottom. Over a full cycle every flow is
provably checked — not one random journey._

## How the QA agent uses this file
1. Read this file. In the **Coverage log**, find the flow with the oldest `last_checked`
   (or any never-checked flow). That's this run's target.
2. Test that ONE flow end-to-end (use `apps/web/e2e/` seed + fixtures; test env only).
3. Append a dated row to the Coverage log: `flow_id | date | PASS/FAIL/BLOCKED | note`.
4. If FAIL and it's a small fix → PR to `staging` (label `agent:qa`). Otherwise → GitHub
   issue (label `qa`) with repro. Either way, still log the result.
5. Commit the updated Coverage log as part of the PR, or in a tiny `[agent:qa] coverage`
   PR if no code changed.

## Flow inventory

Legend — **Tier**: free / pro / nutritionist(coach) / any. **Multi**: needs 2 users acting
in sequence (harder — schedule after single-user flows are green).

### Auth & account
| ID | Flow | Tier | Multi | Notes |
|----|------|------|-------|-------|
| F01 | Sign up (create account) | any | no | Real UI signup hits email confirm; seed mints pre-confirmed users. Verify redirect to onboarding / confirm screen. |
| F02 | Log in | any | no | Seeded user → dashboard. |
| F03 | Log out | any | no | Returns to logged-out home; protected routes redirect. |
| F04 | Password reset request | any | no | Request screen + email trigger (can't read inbox — assert the request UX). |
| F05 | Auth redirect / route protection | any | no | Logged-out hitting /dashboard, /onboarding → /login. |

### Client journey
| ID | Flow | Tier | Multi | Notes |
|----|------|------|-------|-------|
| F10 | Client onboarding questionnaire (9 steps → dashboard) | free | no | Automation-hostile: number inputs commit on blur, options are cards. |
| F11 | Generate AI plan from intake | free | no | Needs OpenAI — assert request fires / plan renders (cost-aware). |
| F12 | Log a meal | free | no | |
| F13 | Log a workout | free | no | |
| F14 | Pro-gating holds (cardio, supplements) | free | no | Free user sees upgrade prompt, not the feature. |
| F15 | Cardio tracking (as Pro) | pro | no | |
| F16 | Supplements tracking (as Pro) | pro | no | |

### Coach / PT journey
| ID | Flow | Tier | Multi | Notes |
|----|------|------|-------|-------|
| F20 | Coach setup questionnaire (6 steps) | coach | no | |
| F21 | Publish public marketplace profile | coach | no | Renders publicly. |
| F22 | Request verification (pending, no badge yet) | coach | no | Trust-layer guarantee: no "Verified" badge while only pending. |

### Marketplace
| ID | Flow | Tier | Multi | Notes |
|----|------|------|-------|-------|
| F30 | Discover coaches renders | any | no | |
| F31 | View a public coach profile | any | no | |
| F32 | Request / hire a coach | client | no | Kicks off the PT↔client link. |

### PT ↔ client relationship (multi-user)
| ID | Flow | Tier | Multi | Notes |
|----|------|------|-------|-------|
| F40 | Coach invites client / client accepts | both | yes | |
| F41 | Coach assigns custom anamnesis / plan | both | yes | |
| F42 | Client completes assigned intake; coach sees it | both | yes | |
| F43 | Coach reviews client & leaves guidance | both | yes | |

### Feedback & reviews (multi-user)
| ID | Flow | Tier | Multi | Notes |
|----|------|------|-------|-------|
| F50 | Client leaves a review of coach | both | yes | |
| F51 | Coach responds / check-in cycle | both | yes | |

### Billing
| ID | Flow | Tier | Multi | Notes |
|----|------|------|-------|-------|
| F60 | Upgrade tier (Stripe test mode) | any | no | Use Stripe test cards; never real charges. |
| F61 | Tier boundaries after upgrade/downgrade | any | no | |

## Coverage log
_Append one row per run. `last_checked` = most recent date a flow appears here._

| flow_id | date | result | note |
|---------|------|--------|------|
| F01 | 2026-07-03 | BLOCKED | No spec for signup flow; /signup page renders HTTP 200 (smoke only). |
| F02 | 2026-07-03 | PASS | Seeded coach and client both log in via real form → dashboard/onboarding. |
| F05 | 2026-07-03 | PASS | Logged-out user hitting /dashboard redirected to /login. |
| F01 | 2026-07-03 | PASS | Re-run after infra fix: all 10 smoke tests green (10/10). /signup renders OK; full form-submission spec still missing. |
| F03 | 2026-07-03 | BLOCKED | No deterministic spec existed; logout spec added to smoke.spec.ts in this PR — pending CI confirmation. |
| F04 | 2026-07-04 | BLOCKED | No deterministic spec for password reset UX; /reset-password page renders 200 OK (smoke public-pages). |
| F01 | 2026-07-04 | PASS | signup.spec.ts: form renders, password validation, full signup → /onboarding (all 3 tests green). |
| F02 | 2026-07-04 | PASS | smoke.spec.ts: login form fields, seeded coach + client sign-in (11/11 smoke green after launchOptions fix). |
| F03 | 2026-07-04 | PASS | smoke.spec.ts: sign-out → /login, subsequent /dashboard still blocked (11/11 smoke green). |
| F05 | 2026-07-04 | PASS | smoke.spec.ts: /dashboard → /login for unauthenticated user (confirmed again). |
| F10 | 2026-07-05 | PASS | client-onboarding.spec.ts: questionnaire loads + full 9-step completion → /dashboard (4/4 green). |
| F14 | 2026-07-05 | PASS | client-onboarding.spec.ts: free-tier gating — cardio + supplements show upgrade prompts. |
| F30 | 2026-07-05 | PASS | client-onboarding.spec.ts: discover coaches renders for free client. |
| F20 | 2026-07-05 | PASS | pt-onboarding.spec.ts: coach setup questionnaire loads (1/3 pass); 2 stubs still test.fixme (F20 full + F21 BLOCKED). |
| F11 | 2026-07-05 | BLOCKED | No prior spec; generate-plans.spec.ts added this run — AI routes mocked via page.route(); awaiting CI green to promote to PASS. |
| F12 | 2026-07-06 | PASS | log-meal.spec.ts: /diet/new renders for free client (upgrade prompt shown), food search mocked, full add-meal → save → /diet flow (2/2 green). |
| F04 | 2026-07-06 | FAIL | No password-reset request UI: /reset-password only serves post-invite "set password" flow; unauthenticated visitor without a token sees "invalid link" banner, not an email-request form. reset-password.spec.ts added with fixme for missing form. GitHub issue filed (label: qa). |
| F40 | 2026-07-07 | PASS | invite-accept.spec.ts (new): coach sees pending invite on /clients → client accepts via real /invite/accept UI → relationship persisted (personal_trainer_id + role) → coach sees client as active (1/1 green, re-ran twice). Real invite *creation* (POST /api/personal-trainer/invites) hard-requires RESEND_API_KEY, unset in e2e/CI, so the spec seeds the pending invite row directly and drives everything downstream for real. GitHub issue filed for the RESEND_API_KEY gap (label: qa). |
| F13 | 2026-07-07 | PASS | log-workout.spec.ts (new): seeded a one-day training plan (single exercise from the global `exercises` catalog) directly via a new `seedTrainingDay()` helper, skipping the "New Training Plan" builder UI to focus on session logging. Free client opens /training/session/<dayId>, fills weight+reps for every set, marks each complete, finishes the workout → redirects to /training (workout_logs row inserted). 1/1 green, re-ran twice. |
| F15 | 2026-07-08 | PASS | cardio-tracking.spec.ts (new): F40 and F60 both already green/blocked-on-secrets (see #69, #74), so this run resumed round-robin at the next never-checked flow. Seeded a free client, force-upgraded to 'pro' via a new `upgradeUserRole()` helper (signup trigger only bootstraps free/personal_trainer from metadata, so no Stripe path exists to reach Pro deterministically), logged in, confirmed /cardio renders the tracking form (not the F14 upgrade-prompt lock screen), logged a session end-to-end, and confirmed it appears in the session list. 1/1 green, re-ran twice. |
| F16 | 2026-07-08 | PASS | supplements-tracking.spec.ts (new): F40 and F60 remain green/blocked-on-secrets (#69, #74; F60's billing-upgrade.spec.ts is on open PR #75 pending merge), so this run continued round-robin at the next never-checked flow. Seeded a free client, force-upgraded to 'pro' via the existing `upgradeUserRole()` helper, logged in, confirmed /supplements renders the tracking UI (not the F14 upgrade-prompt lock screen), added a supplement, and marked it taken — toggle flips to `aria-pressed="true"`. 1/1 green, re-ran twice more (3/3 total). |
| F21 | 2026-07-08 | PASS | pt-onboarding.spec.ts: un-fixme'd the existing "coach publishes a marketplace profile" test (F40/F60 still resolved per #69/#75/#74, so round-robin continued to the next never-checked flow — F20's leftover fixme stubs). Fixed two stale locators in `publishCoachProfile()`/`requestVerification()` (`e2e/lib/flows.ts`): the profile slug is generated as `<slugified-name>-<id6>` (e.g. `e2e-coach-e825d5`), not a fixed `coach-` prefix, so the `input[disabled]` value-filter regex never matched; and the certification-link field's accessible name is "Certification link" (the "REPS / PT diploma…" text is only the placeholder), so `getByRole` never found it. With both fixed: coach publishes a public marketplace profile (headline, bio, price, Discover toggle) → renders on `/find-coach/<slug>` for a logged-out visitor. 2/2 green, re-ran twice. |
| F22 | 2026-07-08 | PASS | Same pt-onboarding.spec.ts test as F21 (one spec covers both): after publishing, the coach submits a verification request (pending) and the public profile is asserted to show NO "Verified" badge while status is only pending — the trust-layer guarantee holds. |
| F31 | 2026-07-09 | PASS | F40 and F60 remain resolved (F40 PASS; F60 has open PR #75 + tracking issue #74 for the remaining Stripe-secrets gap), so this run continued round-robin at the next never-checked flow, F31. The existing pt-onboarding.spec.ts "coach publishes a marketplace profile…" test already drives a logged-out visitor to `/find-coach/<slug>`, so this run extended its assertions rather than duplicating a new spec: now also asserts the coach's headline text, the "Request coaching →" CTA link, and the "← Back to discover" link all render on the public profile. 2/2 pt-onboarding.spec.ts tests green (re-ran single-worker after a parallel-run cold-start flake on the unrelated login test self-resolved). |
| F32 | 2026-07-09 | PASS | coach-lead-request.spec.ts (new): F40/F60 still resolved (#69/#75/#74) and F31 already covered this run's earlier pass, so round-robin continued to the next never-checked flow, F32. Seeded a published coach directly (`publishCoachProfile()` in `e2e/lib/seed.ts`, now returns `{slug}`), free client drives the real 4-step `/find-coach/<slug>/request` wizard (Goal → Logistics → Message → Review) → `POST /api/coach-leads` → redirect to `/dashboard`; coach sees the pending lead on `/leads`, clicks "Accept lead"; relationship persisted (`personal_trainer_id` + `role='personal_trainer_client'`, verified via a DB read). 3/3 green (initial run + 2 repeats, one under parallel workers) — required `npm run build && npm run start` (CI mode): dev-mode on-demand route compilation was intermittently swallowing the final "Send request" click on first navigation to a not-yet-compiled route. |
| F41 | 2026-07-09 | PASS | assign-plan.spec.ts (new): F40/F60 still resolved (#69/#75/#74), so this run followed the charter's explicit next step and covered the first never-checked multi-user PT↔client flow, F41. Used `createLinkedPair()` (no F40 invite needed — that's its own spec) to seed an already-linked coach+client, then drove the real "New Training Plan" builder at `/clients/<id>/training/new` (name plan, add day, pick "Bench Press" from the shared exercise catalog, save) → redirects to the client file, plan shows Active. Client (separate browser context, logged in concurrently) opens their own `/training` and sees the coach-assigned plan. **Found and fixed a real gap in the shared seed helper**: `linkClientToTrainer()`/`createLinkedPair()` only set `user_profiles.personal_trainer_id`, but the RLS policy trainers actually hit ("Nutritionists can view client profiles", migration 001) checks a separate `nutritionist_clients` row with `status='active'` — the same upsert the real invite-accept route makes. Without it the coach's browser session got `406`/0-rows on every client-profile read. Fixed `linkClientToTrainer()` in `apps/web/e2e/lib/seed.ts` to upsert `nutritionist_clients` and set role/personal_trainer_id/nutritionist_id exactly as the real accept route does — this only touches test infra, no product code. 3/3 green (initial run + 2 repeats). |
| F04 | 2026-07-09 | PASS | Forgot-password flow BUILT (issue #62): /reset-password now serves an email-request form when no token is present (resetPasswordForEmail → implicit-flow hash link back to the same page → explicit setSession → set-password form). reset-password.spec.ts un-fixme'd + full-loop test added (admin generateRecoveryLink → set new password → sign in with it). 5/5 green, re-ran twice. |
| F42 | 2026-07-10 | PASS | custom-intake.spec.ts (new): F40 remains PASS and F60 remains blocked-on-secrets (open PR #75 + tracking issue #74), so this run continued round-robin to the next never-checked flow, F42 — the "client completes assigned intake" half of the custom-anamnesis feature (F41's assign-plan.spec.ts already covers the plan-assignment half). Seeded an already-linked coach+client pair via `createLinkedPair()` (client not yet onboarded — `onboarding_completed` defaults false), coach adds a required custom intake question via the real `/coach-questions` UI, client drives the real `/onboarding` wizard through the "Coach Questions" step it triggers (`MANAGED_CLIENT_STEPS` in `onboarding/page.tsx`) and answers it, coach opens `/clients/<id>` and sees the answer under "Custom intake answers". 3/3 green. Found along the way that the review step's finish button reads "Submit intake to coach" (not "Go to Dashboard") for managed clients — a real, working UI branch, just a different selector than the plain-client flow; no product bug, spec updated to match. |
| F43 | 2026-07-10 | PASS | coach-guidance.spec.ts (new): F40/F42 remain PASS and F60 remains blocked-on-secrets (open PR #75 + tracking issue #74), so this run continued round-robin to the next never-checked flow, F43 — "coach reviews client & leaves guidance." The app has no dedicated notes/guidance table or UI (checked migrations and the client-detail page); the real mechanism for a coach to leave guidance is the direct-message thread (`conversations`/`messages`, migration 004). Seeded an already-linked coach+client pair via `createLinkedPair()`, coach reviews the client file at `/clients/<id>`, opens `/clients/<id>/messages` and sends a guidance message (the coach-only `conversations` INSERT path, gated by RLS, is exercised for real), client (separate browser context) opens their own `/my-nutritionist/messages` and sees it. 3/3 green. |
| F50 | 2026-07-10 | PASS | coach-review.spec.ts (new): F40/F42/F43 remain PASS and F60 remains blocked-on-secrets (open PR #75 + tracking issue #74), so this run continued round-robin to the next never-checked flow, F50 — "client leaves a review of coach." Real `POST /api/coach-reviews`, no mocking: `createLinkedPair()` seeds a coach + linked client, eligible immediately under `has_coaching_relationship()` (migration 057, checks `personal_trainer_id`/`nutritionist_id` — no F40 invite needed, same pattern as F41-F43). Client opens `/find-coach/<slug>`, rates 5 stars, posts a title + body review via the real `CoachReviews` UI, reloads and confirms the review renders plus the denormalised `rating_avg`/`rating_count` aggregate (migration 057 trigger) updates to "5.0" / "1 review". Also covers the RLS negative case flagged as a TODO in `invite-to-review.spec.ts`'s mission stub: a second client with no relationship to the coach sees the published review but not the review form or "Post review" button. `createLinkedPair()`'s return type extended with `slug` (additive, `publishCoachProfile()` already computed it) so specs don't have to reconstruct the slug format by hand. 3/3 green (re-ran twice more), plus re-ran F41/F42/F43 together to confirm the `createLinkedPair()` signature change caused no regressions. |
| F51 | 2026-07-11 | PASS | coach-checkin.spec.ts (new): F40/F42/F43/F50 remain PASS and F60 remains blocked-on-secrets (open PR #75 + tracking issue #74), so this run continued round-robin to the last never-checked flow, F51 — "coach responds / check-in cycle." The real mechanism is the check-in/feedback-request feature (`feedback_requests`, migration 004; RLS: `nutritionist_id = auth.uid()` for the coach, `client_id = auth.uid()` for the client — matches what `createLinkedPair()` already seeds). `createLinkedPair()` seeds a linked coach+client; coach sends a check-in from the real `/clients/<id>/feedback` UI (default text/rating/yes-no question set); client (separate browser context) answers it on `/check-ins`; coach reopens `/clients/<id>/feedback` and sees the completed response with all three answers rendered — closing the full cycle. 3/3 green (re-ran twice more). This was the last flow in the F40-F61 priority-override list without a spec; round-robin now returns to whichever flow has the oldest `last_checked` per the base charter. |
| F61 | 2026-07-11 | PASS | tier-boundaries.spec.ts (new): F40/F42/F43/F50/F51 remain PASS and F60 remains blocked-on-secrets (open PR #75 + tracking issue #74) — that was the last flow in the F40-F61 priority list, so this run resumed the base charter's oldest-`last_checked` round-robin, landing on F61, the only remaining never-checked flow (F60 has an existing open PR already covering what's testable without Stripe secrets, so it wasn't re-attempted). F14 already proved a free user is locked out of Pro surfaces and F15/F16 proved a Pro user has access, but nothing proved the gate re-evaluates live when a role actually *changes* mid-session — the real shape of both an upgrade (checkout completes) and a downgrade (subscription cancelled/expired). Seeded a free client, logged in once, then drove `/cardio` and `/supplements` through three role states via the existing `upgradeUserRole()` helper (its type widened from `'pro' \| 'unlimited'` to also accept `'free'`, a small additive change): free → locked on both, upgrade to pro → both unlock on the next navigation (no re-login), downgrade back to free → both re-lock on the next navigation. The downgrade direction was the real gap this run closed. 3/3 green (re-ran twice more); also re-ran cardio-tracking.spec.ts (F15) and supplements-tracking.spec.ts (F16) to confirm the `upgradeUserRole()` type widening caused no regression. |
| F01 | 2026-07-11 | PASS | Full priority-override list (F40-F61) is now green/resolved per prior runs, so this run resumed the base charter's oldest-`last_checked` round-robin — F01/F02/F03/F05 were tied for oldest (2026-07-04); picked F01. Re-ran the existing `signup.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium` since the pinned `@playwright/test` browser revision wasn't pre-cached): form renders, mismatched-password validation rejects client-side, full signup → `/onboarding`. 3/3 green, no code changes needed — pure re-verification. F60 checked for drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only) is still open awaiting human sign-off per its `needs-human` label (correct per guardrails — Stripe-touching PRs need a human), CI green, no new action; tracking issue #74 (Stripe test-mode secrets gap) remains open and canonical, not duplicated. |
| F02 | 2026-07-12 | PASS | Priority list (F40-F61) remains resolved, so round-robin continued at the next tied-oldest flow (F02/F03/F05 tied at 2026-07-04, F01 already re-verified last run) — picked F02. Re-ran `smoke.spec.ts` via the deterministic suite: both "a seeded coach can log in" and "a seeded client can log in" pass (real form, real Supabase auth), plus route-protection and sign-out tests in the same spec. 11/11 green, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run. F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift. |
| F03 | 2026-07-12 | PASS | Priority list (F40-F61) remains resolved, so round-robin continued at the next tied-oldest flow — F02/F03/F05 were tied at 2026-07-04, F02 already re-verified earlier today, so this run picked F03. Re-ran the existing "a logged-in user can sign out and is denied re-entry" test in `smoke.spec.ts` via the deterministic suite: seeded user logs in, clicks Sign Out → redirected to `/login`, then confirms `/dashboard` still redirects to `/login` post-logout (session actually cleared, not just a UI state change). 11/11 smoke.spec.ts green on the full-file run, plus a standalone re-run of the sign-out test alone (2/2 total for F03) — no code changes needed, pure re-verification. Synthetic test users cleaned up after the run. F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift. |
| F10 | 2026-07-13 | PASS | Priority list (F40-F61) remains resolved and F05 (next-oldest at 2026-07-04) already has an in-flight re-verification on open PR #124, so to avoid duplicating that work this run picked the next tied-oldest group (F10/F11/F14/F20/F30, all last checked 2026-07-05) — F10 first by ascending flow ID, consistent with prior tie-breaks. Re-ran `client-onboarding.spec.ts` via the deterministic suite: questionnaire loads for a new client, full 9-step completion → `/dashboard`, free-tier gating on Pro surfaces, and discover-marketplace rendering (this one spec also covers F14/F30, already logged separately). First attempt under the default 2 parallel workers hit a flake in both login-dependent tests — `loginAs()`'s post-submit URL assertion caught the page mid-bounce back to `/login` even though the dashboard had fully rendered (visible in the failure snapshot: full authenticated layout, "E2E Client / FREE" sidebar) — consistent with the dev-mode cold-start races noted in prior F31/F32 runs. Re-ran with `--workers=1`: 4/4 green, repeated once more for confidence: 4/4 green again (8/8 total). No code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 88 removed — accumulated across prior sessions on the shared test project). F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift. |
| F11 | 2026-07-13 | PASS | Continued the same tied-oldest group (F10/F11/F14/F20/F30, last checked 2026-07-05) after F10 was re-verified earlier today (merged #126) — next by ascending flow ID is F11. Its only prior row (2026-07-05) logged BLOCKED with a note to "promote to PASS once CI is green"; that promotion never happened even though the spec (`generate-plans.spec.ts`, PR #52) merged the same day, so this run closed that gap. Fresh environment this session (`npm ci` at the repo root, `packages/shared` build, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `E2E_BASE_URL=http://localhost:3000` so Playwright's `webServer` builds/starts the app itself). Ran `generate-plans.spec.ts` via the deterministic suite: onboarding completes to populate `daily_calories`/`daily_protein`, `/generate-plans` auto-triggers all three AI routes (mocked via `page.route()`, no real OpenAI calls), and the "Your plans are ready!" success state renders with saved-plan links. 3/3 green (re-ran twice more). No code changes needed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 55 removed). F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift. |
| F14 | 2026-07-13 | PASS | Continued the same tied-oldest group (F10/F11/F14/F20/F30, last checked 2026-07-05) after F10 and F11 were both re-verified earlier today — next by ascending flow ID is F14, "Pro-gating holds (cardio, supplements)." Ran the existing `free-tier gating holds on Pro-only surfaces` test in `client-onboarding.spec.ts` via the deterministic suite (single worker): a free client hits `/cardio` and sees the "Upgrade to Pro" prompt, not the tracking feature. 2/2 green (re-ran once more for confidence). No code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 14 removed). F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift; F05's in-flight re-verification (PR #124) also left untouched to avoid duplicating that work. |
| F20 | 2026-07-14 | PASS | Continued the same tied-oldest group (F10/F11/F14/F20/F30, last checked 2026-07-05) — F10/F11/F14 were all re-verified on 2026-07-13, so next by ascending flow ID is F20, "Coach setup questionnaire (6 steps)." Ran `pt-onboarding.spec.ts` via the deterministic suite: "the coach setup questionnaire loads for a new coach" (step 1/6 + first prompt render) and "coach publishes a marketplace profile…" (which also exercises F21/F22/F31) both green; the full 6-step auto-fill completion test remains an intentional `test.fixme` (brittleness noted in its own comment, better covered by the agentic coach-onboarding mission) — not attempted, no regression from it. 4/4 green across two runs (2 parallel workers, then re-ran single-worker). No code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 46 removed). F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift; F05's in-flight re-verification (PR #124) left untouched (still open, unrelated to this run's target). |
| F30 | 2026-07-14 | PASS | Finished the tied-oldest group (F10/F11/F14/F20/F30, last checked 2026-07-05) — F10/F11/F14/F20 were all re-verified this week, so this run closed out F30, "Discover coaches renders." F05's re-verification remained on open PR #124 (not yet merged), so it was left untouched to avoid duplicating that in-flight work. Re-ran `client-onboarding.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): "discover marketplace renders for a prospective client" — free client hits `/discover`, sees "Discover Coaches" heading — green as part of the full 4/4 file run. A standalone single-test re-run then hit the now-familiar dev-mode cold-start login flake (`loginAs()` catching the page mid-bounce back to `/login`); retried once more and it passed clean, matching the pattern already logged for F10/F31/F32. No code changes needed — pure re-verification (3/3 total: 1 in the full-file run + 1 flake + 1 clean retry). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed). F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift. |
| F12 | 2026-07-14 | PASS | With the F10/F11/F14/F20/F30 tied-oldest group now finished, and F05's re-verification still in-flight on open PR #124 (left untouched to avoid duplicating that work), this run picked the next-oldest untouched flow, F12 "Log a meal" (last checked 2026-07-06). Re-ran `log-meal.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): `/diet/new` renders and is gated for free tier, plus the full add-meal-with-food-item → save → `/diet` flow. 2/2 green under default parallel workers. A standalone single-worker re-run then hit the now-familiar dev-mode cold-start login flake on the smoke test (`loginAs()` catching the page mid-bounce back to `/login`); retried once more and it passed clean (2/2), matching the pattern already logged for F10/F30/F31/F32 — no product regression. 4/4 total across both runs. No code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed). F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift. |
| F13 | 2026-07-15 | PASS | Priority list (F40-F61) remains resolved (F60 still blocked-on-secrets, PR #75 + issue #74 open/canonical, no new drift). F12 was re-verified yesterday, so the next-oldest untouched flow is F13 "Log a workout" (last checked 2026-07-07). Re-ran `log-workout.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): free client opens a seeded training session, logs weight+reps for every set, finishes the workout → redirects to `/training`. Initial run green (1/1). A default-parallel-workers re-run then hit the now-familiar dev-mode cold-start login flake (`loginAs()`'s URL assertion catching the page mid-bounce back to `/login` while the dashboard had actually rendered); retried twice with `--workers=1` and both passed clean. 3/3 green total — no product regression, no code changes needed. `npm run lint`/`npm run build` both pass (pre-existing warnings only, none in touched files). |
| F40 | 2026-07-15 | PASS | This run's session branch had accidentally been cut from `main` instead of `staging` (5 stray promotion commits, never pushed) — reset to `origin/staging` first per the shared guardrails before picking a target. F60 checked for drift first (priority-override flow): still zero coverage-log entries on merged `staging` — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open awaiting human sign-off (`needs-human`), and tracking issue #74 (missing Stripe test-mode secrets in e2e/CI) remains open/canonical with no new information since the last check — re-attempting would just duplicate that in-flight work, so it was left untouched again. F05 (oldest last_checked, 2026-07-04) also still has its re-verification in-flight on open PR #124 (unmerged), so — consistent with the pattern established by the F10/F30/F12 runs — it was left alone too, and this run picked the next-oldest untouched flow, F40 "Coach invites client / client accepts" (last checked 2026-07-07, part of the now-resolved priority list). Re-ran `invite-accept.spec.ts` via the deterministic suite (fresh env — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): coach sees the pending invite on `/clients`, client accepts via the real `/invite/accept` UI, relationship persists (`personal_trainer_id` + role), coach sees the client as active. 2/2 green (initial run + one re-run), no product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F15 | 2026-07-15 | PASS | Same session branch as the earlier F40 run today (already reset to `origin/staging`). F60 not re-attempted: PR #75 + tracking issue #74 remain open/canonical, no new drift. F05's re-verification remains in-flight on open PR #124 (unmerged), left untouched to avoid duplicating that work. F40 was just re-verified earlier today, so this run picked the next tied-oldest group (F15/F16/F21/F22, all last checked 2026-07-08) — F15 first by ascending flow ID, consistent with prior tie-breaks. Re-ran `cardio-tracking.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): free client force-upgraded to Pro via `upgradeUserRole()`, `/cardio` renders the tracking form (not the F14 upgrade-prompt lock screen), logs a 45-minute session end-to-end, appears in the session list. First attempt under the default worker hit the now-familiar dev-mode cold-start login flake (`loginAs()`'s URL assertion catching the page mid-bounce back to `/login`); retried with `--workers=1` and got 2/2 clean (3/3 total this run). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed). |
| F16 | 2026-07-16 | PASS | F40 remains PASS (re-verified 2026-07-15) and F60 remains blocked-on-secrets (open PR #75 + tracking issue #74, no new drift), so this run continued round-robin at the next tied-oldest flow — F15 was re-verified yesterday, so next by ascending flow ID in the F15/F16/F21/F22 group (last checked 2026-07-08) is F16. Noted in passing: open PR #156 ("F42 — client completes coach-assigned custom intake") appears to duplicate F42, which this log already shows PASS on 2026-07-10 via `custom-intake.spec.ts` — not this run's flow to fix, flagging for the janitor/a human rather than touching someone else's open PR. Re-ran `supplements-tracking.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): free client force-upgraded to Pro via `upgradeUserRole()`, `/supplements` renders the tracking UI (not the F14 upgrade-prompt lock screen), adds a supplement, marks it taken (`aria-pressed="true"`). 2/2 green (initial run + one re-run), no product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 48 removed). |
| F21 | 2026-07-16 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 remain open/canonical, no new information, not re-attempted. F05's re-verification remains in-flight on open PR #124 (still open, unmerged), left untouched to avoid duplicating that work. F16 was re-verified earlier today, so this run closed out the next tied-oldest flow in the F15/F16/F21/F22 group (last checked 2026-07-08) — F21, "Publish public marketplace profile" (F22 shares the same spec, re-verified in the same run below). Re-ran `pt-onboarding.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): "coach publishes a marketplace profile that renders publicly with no verified badge" green — headline/bio/price + Discover toggle saved, profile renders on `/find-coach/<slug>` for a logged-out visitor. 2/2 green across two full-file runs (the intentional `test.fixme` 6-step completion test still skipped, no regression from it). A single-worker re-run's first attempt hit the now-familiar dev-mode cold-start login flake on the unrelated "questionnaire loads" test (`loginAs()`'s URL assertion catching the page mid-bounce back to `/login`); retried clean (2/2). No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 42 removed). |
| F22 | 2026-07-16 | PASS | Same `pt-onboarding.spec.ts` run as F21 above (one test covers both): after publishing, the coach's verification request stays pending and the public profile is asserted to show NO "Verified" badge — the trust-layer guarantee still holds. No product regression, no code changes needed. |
| F04 | 2026-07-17 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 remain open/canonical, no new information, not re-attempted. F05's re-verification remains in-flight on open PR #124 (still open, unmerged), left untouched. F21/F22 were re-verified yesterday, so this run picked the next tied-oldest group (F04/F31/F32/F41, last checked 2026-07-09) — F04 first by ascending flow ID. Re-ran `reset-password.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): initial run 3/5 green — 2 failures (`net::ERR_CONNECTION_RESET` talking to Supabase directly, and the "check your inbox" state never rendering) traced to the spec importing `test`/`expect` from raw `@playwright/test` instead of `../fixtures`, so it never got the `page` fixture's automatic `routeSupabaseThroughNode()` wrap that every other Supabase-touching spec relies on for the cloud MITM-proxy workaround (canonical issue #57) — a real gap in this one spec, not a product bug (git history shows this file was written before that fixture pattern existed and was never updated). Fixed the import (`apps/web/e2e/specs/reset-password.spec.ts`): 4/5 green after the fix, re-ran once more to confirm (4/5 again, same one sub-test both times). The remaining failure ("full loop: recovery link → set new password → sign in with it") is a *different*, deeper limitation: `routeSupabaseThroughNode()` uses Node `fetch()`, which auto-follows redirects, so the Supabase recovery-link's redirect-with-URL-fragment (implicit auth flow: `.../auth/v1/verify` → `/reset-password#access_token=...`) collapses to the final response body without ever giving the browser the fragment, leaving the page stuck on "Checking your reset link...". A real user's email client does a genuine top-level navigation and hits none of this — confirmed by this same sub-test passing 5/5 under identical (fixture-less) code on 2026-07-09, so it's proxy/session-dependent flakiness, not a code regression. Documented the redirect-fragment gap as a comment on canonical issue #57 (same MITM-proxy root-cause family) rather than filing a duplicate issue or reworking the shared `routeSupabaseThroughNode()` helper (used by 7 other specs) in a QA run. Net: request-form UX, check-your-inbox state, and page/link rendering all verified real and deterministic; only the one email-link sub-test remains infra-blocked. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files); `npx tsc --noEmit` clean. Synthetic test users cleaned up automatically via spec teardown. |
| F31 | 2026-07-17 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 remain open/canonical (same `updated_at` as last check, no new information), not re-attempted. F05's re-verification remains in-flight on open PR #124 (still open, unmerged), left untouched. F04 was just re-verified earlier today (merged #165), so this run continued the same tied-oldest group (F04/F31/F32/F41, last checked 2026-07-09) — next by ascending flow ID is F31, "View a public coach profile." Re-ran `pt-onboarding.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): "coach publishes a marketplace profile…" (the same test that also covers F21/F22) drives a logged-out visitor to `/find-coach/<slug>` and asserts the coach's headline text, the "Request coaching →" CTA link, and the "← Back to discover" link all render — this is F31's actual coverage (extended into this test back on 2026-07-09 rather than duplicated into a new spec). 2/2 non-skipped tests green (the intentional `test.fixme` 6-step-completion stub still skipped, no regression from it), re-ran the full file once more for confidence: 2/2 green again (4/4 total). No code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 78 removed — accumulated from prior sessions on the shared test project). |
| F32 | 2026-07-17 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 still open/canonical, no new information, not re-attempted. F05's re-verification remains in-flight on open PR #124 (still open, unmerged), left untouched. F04/F31 were both re-verified earlier today, so this run closed out the last flow in the F04/F31/F32/F41 tied-oldest group (last checked 2026-07-09) — F32, "Request / hire a coach." Also found and closed open PR #156 (`[agent:qa] F42 — client completes coach-assigned custom intake, coach sees it`) as a duplicate: F42 is already merged and logged PASS on 2026-07-10 via `custom-intake.spec.ts` (PR #99), which covers the identical journey — coach creates a custom intake question, client answers it during onboarding, coach sees the response — and does so more thoroughly (drives real `/coach-questions` UI for question creation, vs. #156 seeding the question directly). Left a comment pointing to the superseding spec and closed it, per the no-duplicates guardrail. Re-ran `coach-lead-request.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build; first attempt failed on browser resolution only — `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium` needed since the pinned `@playwright/test` browser revision wasn't pre-cached, same as prior sessions — not a product issue): free client drives the real 4-step `/find-coach/<slug>/request` wizard (Goal → Logistics → Message → Review) → `POST /api/coach-leads` → redirect to `/dashboard`; coach sees the pending lead on `/leads`, accepts it; relationship persists. 2/2 green (initial run + one re-run under CI build+start mode). No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run automatically via spec teardown. |
| F41 | 2026-07-18 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 still open/canonical, no new information since the last check, not re-attempted. F05's re-verification remains in-flight on open PR #124 (still open, unmerged), left untouched to avoid duplicating that work. F04/F31/F32 were all re-verified yesterday, so this run closed out the last flow in the F04/F31/F32/F41 tied-oldest group (last checked 2026-07-09) — F41, "Coach assigns custom anamnesis / plan." Re-ran `assign-plan.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): `createLinkedPair()` seeds an already-linked coach+client, coach drives the real "New Training Plan" builder at `/clients/<id>/training/new` (name plan, add day, pick "Bench Press" from the shared exercise catalog, save) → plan shows Active on the client file; client (separate browser context) opens their own `/training` and sees the coach-assigned plan. 2/2 green (initial run + one re-run). No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 70 removed — accumulated from prior sessions on the shared test project). |
| F42 | 2026-07-18 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 still open/canonical (same as last check), not re-attempted. F05's re-verification remains in-flight on open PR #124 (still open, unmerged), left untouched. F41 was just re-verified earlier today (merged #175), so this run continued round-robin to the next tied-oldest group — F42/F43/F50, all last checked 2026-07-10 — picking F42 first by ascending flow ID, "Client completes assigned intake; coach sees it." Session branch had drifted 9 commits behind `origin/staging` (last synced before the F41/#175 merge), reset to `origin/staging` first per the shared guardrails before picking a target. Re-ran `custom-intake.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`): coach adds a required custom intake question via the real `/coach-questions` UI, client drives the real `/onboarding` wizard through the "Coach Questions" step and answers it, coach opens `/clients/<id>` and sees the answer under "Custom intake answers". First two attempts (dev-mode `npm run dev` webServer, default then `--workers=1`) both hit the test's 60s timeout mid-flow — screenshots showed the client onboarding wizard stalled partway through (step 5/10 on one attempt), consistent with the dev-mode on-demand route-compilation cold-start races already logged for F32/F41. Switched to CI build+start mode (`CI=1`, matching `playwright.config.ts`'s `webServer` branch) as F32 previously found: 2/2 green (33.9s, then 31.7s). No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 41 removed — accumulated from prior sessions on the shared test project). |
| F43 | 2026-07-18 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 still open/canonical (same `updated_at` as last check), not re-attempted. F05's re-verification remains in-flight on open PR #124 (still open, unmerged), left untouched to avoid duplicating that work. F42 was just re-verified earlier today (merged #179), so this run continued round-robin to the next tied-oldest flow in the F42/F43/F50 group (last checked 2026-07-10) — F43 by ascending flow ID, "Coach reviews client & leaves guidance." Session branch was re-synced to `origin/staging` first per the shared guardrails (starting point predated the F41/F42 merges). Re-ran `coach-guidance.spec.ts` via the deterministic suite (`npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, CI build+start mode per the established cold-start-flake precedent): `createLinkedPair()` seeds an already-linked coach+client, coach reviews the client file at `/clients/<id>` and sends a guidance message via the real coach-only `conversations` INSERT path on `/clients/<id>/messages`, client (separate browser context) opens `/my-nutritionist/messages` and sees it. 2/2 green (initial run + one re-run). No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 37 removed — accumulated from prior sessions on the shared test project). |
| F50 | 2026-07-19 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 still open/canonical (same `updated_at` as last check), not re-attempted. F05's re-verification remains in-flight on open PR #124 (still open, unmerged), left untouched to avoid duplicating that work. F42/F43 were both re-verified yesterday, so this run closed out the last flow in the F42/F43/F50 tied-oldest group (last checked 2026-07-10) — F50, "Client leaves a review of coach." Session branch had been cut from `main` instead of `staging` (stale, predating dozens of merges), reset to `origin/staging` first per the shared guardrails before picking a target. Re-ran `coach-review.spec.ts` via the deterministic suite (`npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, CI build+start mode per the established cold-start-flake precedent): `createLinkedPair()` seeds a coach + linked client, client opens `/find-coach/<slug>`, rates 5 stars, posts a title + body review via the real `POST /api/coach-reviews` path, reloads and confirms the review renders plus the denormalised `rating_avg`/`rating_count` aggregate updates to "5.0" / "1 review". 2/2 green (initial run + one re-run). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 39 removed — accumulated from prior sessions on the shared test project). |
| F05 | 2026-07-19 | PASS | F05 was the true oldest `last_checked` (2026-07-04) once F40/F60 (priority override) and the F04/F31/F32/F41/F42/F43/F50 group were resolved, so this run's target reverted to the base round-robin's oldest flow. Its re-verification has been stuck since 2026-07-12 on PR #124 — a coverage-log-only, 1-line append — which failed to auto-merge **12 runs in a row** purely because every other coverage PR appends to the same tail of this file, so its base went stale each time (`mergeable_state: dirty` every retry per the gatekeeper's comment history). Since the content itself was never in question (e2e green each time it was checked) and this is a trivial append with no real conflict to resolve, closed #124 as superseded by this PR rather than perpetuating the loop — see the closing comment there. Re-ran `smoke.spec.ts` fresh from current `origin/staging` (`npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `E2E_BASE_URL`/`E2E_EMAIL_DOMAIN` set to match CI): "logged-out users are kept out of the app" (F05's test — unauthenticated `/dashboard` → `/login` redirect) passed both in the full 11-test file run and standalone (2/2 green). The same run's seeded-coach/seeded-client login tests failed with `net::ERR_CONNECTION_RESET`-class timeouts — this is the known canonical cloud MITM-proxy constraint (#57), unrelated to F05 and explicitly not to be re-filed per the charter; F05 itself doesn't depend on login. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, no code changed). F60 checked for drift: PR #75 + issue #74 still open/canonical, `.github/workflows/e2e.yml` still has no `STRIPE_*` secrets — no change, not re-attempted. |
| F01 | 2026-07-19 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 + tracking issue #74 still open/canonical (same `updated_at` as last check, `.github/workflows/e2e.yml` still has no `STRIPE_*` secrets), not re-attempted. F05's re-verification PR #124 is now closed (superseded by this same run's F50/F05 entries above), so no in-flight work to avoid duplicating. F50/F05 were both re-verified earlier today, closing out that tied-oldest group, so this run picked the next tied-oldest group (F01/F51/F61, all last checked 2026-07-11) — F01 first by ascending flow ID, "Sign up (create account)." Fresh environment this session (`npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, CI build+start mode per the established cold-start-flake precedent). Re-ran `signup.spec.ts` via the deterministic suite: form renders, mismatched-password validation rejects client-side, full signup → `/onboarding`. 3/3 green, re-ran once more for confidence (3/3 again, 6/6 total). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 77 removed — accumulated from prior sessions on the shared test project). |
| F51 | 2026-07-20 | PASS | Priority list (F40-F61) still resolved; F60 checked for drift — PR #75 + tracking issue #74 both still open/canonical, same content as last check (`.github/workflows/e2e.yml` still has no `STRIPE_*` secrets), so not re-attempted (F51/F61 sessions on 2026-07-11 already established this precedent). Session branch had drifted ~90 commits behind `origin/staging` with unrelated stray promotion commits, reset to `origin/staging` first per the shared guardrails. F01 was re-verified yesterday, closing that slot in the F01/F51/F61 tied-oldest group (all last checked 2026-07-11), so this run picked the next by ascending flow ID — F51, "Coach responds / check-in cycle." Fresh environment this session (`npm ci` at repo root + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `E2E_BASE_URL=http://localhost:3000`). Re-ran `coach-checkin.spec.ts` via the deterministic suite: coach sends a check-in from `/clients/<id>/feedback`, client (separate browser context) answers it on `/check-ins`, coach reopens the client file and sees the completed response with all three answers rendered. 2/2 green (initial run + one re-run). No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). |
| F61 | 2026-07-20 | PASS | Priority list (F40-F61) still resolved; F60 checked for drift — PR #75 + tracking issue #74 both still open/canonical, same content as last check (`.github/workflows/e2e.yml` still has no `STRIPE_*` secrets), so not re-attempted. F51 was re-verified earlier today, closing the last slot in the F01/F51/F61 tied-oldest group (all last checked 2026-07-11), so this run closed it out — F61, "Tier boundaries after upgrade/downgrade." Session branch reset to `origin/staging` first per the shared guardrails (this run started from a stale checkout). Fresh environment this session (`npm ci` at repo root + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `E2E_BASE_URL=http://localhost:3000`, CI build+start mode per the established cold-start-flake precedent). Re-ran `tier-boundaries.spec.ts` via the deterministic suite: seeded free client drives `/cardio` and `/supplements` through three role states via `upgradeUserRole()` — free → locked on both, upgrade to pro → both unlock on the next navigation (no re-login), downgrade back to free → both re-lock on the next navigation. 2/2 green (initial run + one re-run). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 70 removed — accumulated from prior sessions on the shared test project). |
| F02 | 2026-07-20 | PASS | Priority list (F40-F61) still fully resolved; F60 checked for drift — PR #75 + tracking issue #74 both still open/canonical, same content as last check (`.github/workflows/e2e.yml` still has no `STRIPE_*` secrets), not re-attempted. With F01/F51/F61 all re-verified today, the true oldest `last_checked` is now the F02/F03 tied group (both last checked 2026-07-12) — picked F02 first by ascending flow ID, "Log in." Session branch reset to `origin/staging` first per the shared guardrails (designated branch was ~95 commits stale, cut from an old pre-#62 promotion point). Fresh environment this session (`npm ci` at repo root + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `E2E_BASE_URL=http://localhost:3000`, CI build+start mode per the established cold-start-flake precedent). Re-ran `smoke.spec.ts` via the deterministic suite: seeded coach and seeded client both log in via the real form → dashboard, plus the full 11-test file (public pages, login form fields, route protection, sign-out) all green. 11/11 on the full run, then re-ran the two login tests + sign-out standalone for confidence: 3/3 green (14/14 total this session). No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed — accumulated from prior sessions on the shared test project). |
| F03 | 2026-07-20 | PASS | Same `smoke.spec.ts` run as F02 above (one file covers both): "a logged-in user can sign out and is denied re-entry" — seeded user logs in, signs out → redirected to `/login`, then `/dashboard` still redirects post-logout (session actually cleared). Green in both the full-file run and the standalone re-run (2/2). No product regression, no code changes needed. |
| F60 | 2026-07-21 | BLOCKED | Priority-override flow, checked for drift first (per charter): still zero coverage-log entries for F60 on `staging` two weeks on. PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked — Stripe never contacted) remains open, unmerged, correctly labeled `needs-human`/`agent:qa` per the guardrail that Stripe-touching work needs a human's sign-off before merge; not stuck on a mergeability bug (unlike the old F05/#124 case), so not superseded or re-opened — that would just bypass the human gate it's correctly waiting on. Tracking issue #74 (no `STRIPE_*` secrets in `.github/workflows/e2e.yml`, so real checkout/webhook path is untestable) remains open/canonical, unchanged since filing. No new action; not re-attempted. |
| F10 | 2026-07-21 | PASS | With F60 unchanged (see row above), this run's actual target reverted to the base round-robin: true oldest `last_checked` is now the F10/F11/F14 tied group (2026-07-13), picked F10 first by ascending flow ID per the established tie-break convention. Session branch was stale (~140 commits behind `origin/staging`), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `E2E_BASE_URL=http://localhost:3000`, dev-mode webServer). Ran `client-onboarding.spec.ts` via the deterministic suite (`npm run e2e:test -w apps/web`, `--workers=1`): questionnaire loads for a new client, full 9-step completion → `/dashboard`, free-tier gating holds on Pro-only surfaces, discover-marketplace renders for a prospective client — all four across both the `chromium` and `mobile-chrome` projects. 8/8 green first try, re-ran once more for confidence: 8/8 green again (16/16 total), no dev-mode cold-start flake this time. No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed). |
| F11 | 2026-07-21 | PASS | F60 checked for drift first (per charter): PR #75 and issue #74 both still open/unmerged, identical `updated_at` to the last check, `.github/workflows/e2e.yml` still has no `STRIPE_*` secrets — no new information, not re-attempted, still correctly `needs-human`. F10 was re-verified earlier today, closing that slot in the F10/F11/F14 tied-oldest group (2026-07-13), so this run continued to the next by ascending flow ID — F11, "Generate AI plan from intake." Session branch (`claude/zealous-lamport-wv5nxy`) had drifted ~100 commits behind `origin/staging`, reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `E2E_BASE_URL=http://localhost:3000`). Ran `generate-plans.spec.ts` via the deterministic suite: client onboarding completes to populate `daily_calories`/`daily_protein`, `/generate-plans` auto-triggers all three AI routes (mocked via `page.route()`, no real OpenAI calls made — cost-aware per the flow's own notes), and the "Your plans are ready!" success state renders with saved-plan links. 1/1 green, re-ran once more for confidence (2/2 total). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 45 removed — accumulated from prior sessions on the shared test project). |
| F14 | 2026-07-21 | PASS | F60 checked for drift first (per charter): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, no new information since the last check — not re-attempted, still correctly `needs-human`. Checked open PRs/issues more broadly: no new duplicates, no in-flight PR targeting F14. F10/F11 were both re-verified earlier today, closing out the F10/F11/F14 tied-oldest group (all last checked 2026-07-13), so this run closed the last slot — F14, "Pro-gating holds (cardio, supplements)." Session branch (`claude/zealous-lamport-c83fnn`) had drifted ~100 commits behind `origin/staging`, reset first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `E2E_BASE_URL=http://localhost:3000`, dev-mode webServer). Ran `client-onboarding.spec.ts` via the deterministic suite (`--workers=1`): the full 4-test file passed (questionnaire load, 9-step completion, gating, discover-marketplace), including the "free-tier gating holds on Pro-only surfaces" test — a free client hits `/cardio` and `/supplements`, sees the "Cardio tracking is on Pro"/"Supplement tracking is a Pro tool" upgrade prompts, not the tracking features. 4/4 green first try; re-ran the gating test standalone for confidence (1/1 green, 2/2 total for F14). No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 28 removed — accumulated from prior sessions on the shared test project). |
| F12 | 2026-07-22 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, same content/`updated_at` as the last check, not re-attempted, still correctly `needs-human`. F10/F11/F14 were all re-verified 2026-07-21, closing that tied-oldest group, so the true oldest `last_checked` is now the F12/F20/F30 tied group (all last checked 2026-07-14) — picked F12 first by ascending flow ID, "Log a meal." Session branch reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Re-ran `log-meal.spec.ts` via the deterministic suite: `/diet/new` renders and is gated for free tier, full add-meal-with-food-item → save → `/diet` flow. 2/2 green under default parallel workers, re-ran single-worker for confidence: 2/2 green again (4/4 total), no flakes this run. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 34 removed — accumulated from prior sessions on the shared test project). |
| F20 | 2026-07-22 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, same content/`updated_at` as the last check, not re-attempted, still correctly `needs-human`. F12 was re-verified earlier today, so this run continued the F12/F20/F30 tied-oldest group (all last checked 2026-07-14) — next by ascending flow ID, F20, "Coach setup questionnaire (6 steps)." Session branch was cut from `main` instead of `staging` (stale, predating recent merges), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Re-ran `pt-onboarding.spec.ts` via the deterministic suite: "the coach setup questionnaire loads for a new coach" (step 1/6 + first prompt render) green; the full 6-step auto-fill completion test remains an intentional `test.fixme` (noted brittleness, better covered by the agentic coach-onboarding mission) — not attempted, no regression from it. 2/2 non-skipped tests green under default parallel workers, re-ran single-worker for confidence: 2/2 green again (4/4 total), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 46 removed — accumulated from prior sessions on the shared test project). |
| F30 | 2026-07-22 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, same content/`updated_at` as the last check, not re-attempted, still correctly `needs-human`. Also checked all other open PRs/issues broadly — no new duplicates, nothing else in flight targeting F30. F12/F20 were both re-verified earlier today, closing out the last slot in the F12/F20/F30 tied-oldest group (all last checked 2026-07-14) — F30, "Discover coaches renders." Session branch reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_BASE_URL=http://localhost:3000`/`E2E_SUPABASE_*` already present in the environment, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Re-ran `client-onboarding.spec.ts` via the deterministic suite: full 4-test file green, including "discover marketplace renders for a prospective client" — a free client hits `/discover` and sees the "Discover Coaches" heading. 4/4 green under default parallel workers, re-ran single-worker for confidence: 4/4 green again (8/8 total), no flakes this run. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F13 | 2026-07-23 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, no new information since last check, not re-attempted, still correctly `needs-human`. Searched open PRs/issues broadly for F13/F15/F40 (the current tied-oldest group, all last checked 2026-07-15) — no in-flight duplicates. Picked F13 first by ascending flow ID, "Log a workout." Designated session branch (`claude/zealous-lamport-5l4z8i`) had drifted onto stale pre-merge "Promote staging to prod" commits from an earlier cycle, reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_BASE_URL=http://localhost:3000`/`E2E_SUPABASE_*` already present in the environment, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Re-ran `log-workout.spec.ts` via the deterministic suite: seeded client opens a training session, logs weight+reps for the single set, finishes the workout → redirects to `/training`. 1/1 green under default worker, re-ran single-worker for confidence: 1/1 green again (2/2 total), no flakes this run. No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed — accumulated from prior sessions on the shared test project). |
| F15 | 2026-07-23 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, no new information since last check, not re-attempted, still correctly `needs-human`. Checked open PRs/issues broadly — no in-flight duplicates targeting F15. F13 was re-verified earlier today (merged #224), so this run continued the F13/F15/F40 tied-oldest group (all last checked 2026-07-15) — next by ascending flow ID, F15, "Cardio tracking (as Pro)." Designated session branch had drifted stale (predating recent merges), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_BASE_URL=http://localhost:3000`/`E2E_SUPABASE_*` already present in the environment, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Re-ran `cardio-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/cardio` renders the tracking form (not the F14 upgrade-prompt lock screen), logs a session end-to-end, appears in the session list. 1/1 green under default worker, re-ran once more for confidence: 1/1 green again (2/2 total), no flakes this run. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F40 | 2026-07-23 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `updated_at` unchanged since last check, not re-attempted, still correctly `needs-human`. Checked open PRs/issues broadly — no in-flight duplicates targeting F40. F13/F15 were both re-verified earlier today, closing out the last slot in the F13/F15/F40 tied-oldest group (all last checked 2026-07-15) — F40, "Coach invites client / client accepts." Designated session branch had drifted far behind `origin/staging` (predating recent merges), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm install` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `invite-accept.spec.ts` via the deterministic suite: coach sees the pending invite on `/clients`, client accepts via the real `/invite/accept` UI, relationship persists (`personal_trainer_id` + role), coach sees the client as active. 1/1 green, re-ran once more for confidence (2/2 total). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F16 | 2026-07-24 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, no new information since last check, not re-attempted, still correctly `needs-human`. Also searched open PRs/issues broadly for "F16"/"supplements" — no in-flight duplicates. F40 was re-verified yesterday, so the true oldest `last_checked` moved to the F16/F21/F22 tied group (all last checked 2026-07-16) — picked F16 first by ascending flow ID, "Supplements tracking (as Pro)." (Initially misread the state of F42's coverage from a stale local checkout and started building a duplicate spec for it before noticing `custom-intake.spec.ts` already covers F42 end-to-end and has been green since 2026-07-10 — discarded that work without committing it once the duplication was caught, per the no-duplicates guardrail.) Session branch reset to `origin/staging` first per the shared guardrails (designated branch had drifted onto a stale pre-merge point). Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_BASE_URL=http://localhost:3000`/`E2E_SUPABASE_*` already present in the environment, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `supplements-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/supplements` renders the tracking UI (not the F14 upgrade-prompt lock screen), adds a supplement, marks it taken (`aria-pressed="true"`). 1/1 green, re-ran once more for confidence (2/2 total), no flakes this run. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` both pass (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F21 | 2026-07-24 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `updated_at` unchanged since last check, not re-attempted, still correctly `needs-human`. Searched open PRs/issues broadly for "F21"/"F22"/"pt-onboarding" — no in-flight duplicates. F16 was re-verified earlier today, so this run continued the F16/F21/F22 tied-oldest group (all last checked 2026-07-16) — next by ascending flow ID, F21, "Publish public marketplace profile" (F22 shares the same spec, re-verified in the same run below). Designated session branch had drifted onto stale pre-merge commits, reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `pt-onboarding.spec.ts` via the deterministic suite: "coach publishes a marketplace profile that renders publicly with no verified badge" green — headline/bio/price + Discover toggle saved, profile renders on `/find-coach/<slug>` for a logged-out visitor (the intentional `test.fixme` 6-step-completion stub still skipped, no regression from it). 2/2 non-skipped tests green under default parallel workers, re-ran single-worker for confidence: 2/2 green again (4/4 total), no flakes this run. No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up automatically via spec teardown. |
| F22 | 2026-07-24 | PASS | Same `pt-onboarding.spec.ts` run as F21 above (one test covers both): after publishing, the coach's verification request stays pending and the public profile is asserted to show NO "Verified" badge — the trust-layer guarantee still holds. No product regression, no code changes needed. |
| F04 | 2026-07-24 | PASS | Priority list (F40-F61) remains resolved; F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `updated_at` unchanged since last check, not re-attempted, still correctly `needs-human`. Searched open PRs/issues broadly for "F04"/"reset-password" — no in-flight duplicates (only #75 is open under `agent:qa`). F21/F22 were re-verified earlier today, closing that slot, so the true oldest `last_checked` moved to the F04/F31/F32 tied group (all last checked 2026-07-17) — picked F04 first by ascending flow ID, "Password reset request." Session branch was reset to `origin/staging` first per the shared guardrails (designated branch had drifted stale, cut from an old pre-#62 promotion point). Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Re-ran `reset-password.spec.ts` via the deterministic suite: 4/5 green under default parallel workers (request-form UX, check-your-inbox state, page/link rendering all real and deterministic) — the "full loop: recovery link → set new password → sign in with it" sub-test failed the same way it has since 2026-07-17 (page stuck on "Checking your reset link..." — `getByLabel('New password')` never appears). Re-ran single-worker for confidence: 4/5 again, same one sub-test both times, consistent with the documented root cause already on canonical issue #57 (`routeSupabaseThroughNode()`'s Node `fetch()` auto-follows the Supabase recovery-link redirect, collapsing the URL-fragment `access_token` before the browser ever sees it — a real user's email client does a genuine top-level navigation and hits none of this). No new information vs. the existing #57 comment, so not re-commented — would just add noise. No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 69 removed — accumulated from prior sessions on the shared test project). |
| F31 | 2026-07-25 | BLOCKED | Priority list (F40-F61) remains resolved; F60 checked for drift — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, no new information since the last check, not re-attempted (needs human secret provisioning). Base round-robin's true oldest tied group was F31/F32 (last checked 2026-07-17); picked F31 first by ascending flow ID. Attempted to re-verify via the existing `pt-onboarding.spec.ts` (which covers F31's "coach publishes a marketplace profile" assertions), but this session hit a recurrence of the canonical cloud MITM-proxy login constraint (issue #57): `loginAs()` completes the Supabase token exchange (200) but the browser never leaves `/login` within the 30s retry window. Reproduced on both `npm run dev` and CI-mode (`build && start`) locally, and on the long-established, normally-green `pt-onboarding.spec.ts` — not a regression in this spec, this session's proxy/browser connectivity. Commented on #57 rather than filing a new issue per the shared guardrails' no-duplicates rule. No code change made this run (environment issue, not a product bug); synthetic test users created during investigation were cleaned up (`npm run e2e:seed -- cleanup`). |
| F32 | 2026-07-25 | PASS | Continuing the same F31/F32 tied-oldest group (both last checked 2026-07-17) in a fresh session/container — F31 was already re-attempted and logged BLOCKED earlier today by a separate run (cloud MITM-proxy login recurrence, issue #57), so this run picked up the next flow in that group by ascending flow ID, F32, "Request / hire a coach." F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, no new information since the last check, not re-attempted, still correctly `needs-human`. Searched open PRs/issues broadly — no in-flight duplicates targeting F32. Session branch reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Login worked cleanly this session — the #57 recurrence noted on F31 today did not reproduce here, consistent with it being transient cloud-proxy/browser connectivity rather than a code regression (commented on #57 accordingly, not re-filed). Re-ran `coach-lead-request.spec.ts` via the deterministic suite: free client drives the real 4-step `/find-coach/<slug>/request` wizard (Goal → Logistics → Message → Review) → `POST /api/coach-leads` → redirect to `/dashboard`; coach sees the pending lead on `/leads`, accepts it; relationship persists. 2/2 green (initial run + one re-run). No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed). |
| F41 | 2026-07-25 | PASS | F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, no new information since the last check, not re-attempted, still correctly `needs-human`. F31/F32 were both handled earlier today (F31 BLOCKED on a transient #57 recurrence, F32 PASS), so the true oldest `last_checked` group moved to F41/F42/F43 (all last checked 2026-07-18) — picked F41 first by ascending flow ID, "Coach assigns custom anamnesis / plan." Searched open PRs/issues broadly for "F41"/"assign-plan" — no in-flight duplicates. Session branch reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Re-ran `assign-plan.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach drives the real "New Training Plan" builder at `/clients/<id>/training/new` (name plan, add day, pick "Bench Press", save) → plan shows Active on the client file; client (separate browser context) opens their own `/training` and sees the coach-assigned plan. 1/1 green, re-ran once more for confidence (2/2 total), no flakes, no #57 recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 37 removed — accumulated from prior sessions on the shared test project). |
| F42 | 2026-07-26 | PASS | F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, same content/`updated_at` as the last check, not re-attempted, still correctly `needs-human`. Also searched all open PRs/issues broadly for duplicates — none targeting F42. F41 was re-verified yesterday, so this run continued the F41/F42/F43 tied-oldest group (all last checked 2026-07-18) — next by ascending flow ID, F42, "Client completes assigned intake; coach sees it." Designated session branch had drifted onto stale pre-merge "Promote staging to prod" commits cut from `main`, reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`). Re-ran `custom-intake.spec.ts` via the deterministic suite: first attempt (dev-mode `npm run dev` webServer) hit the test's 60s timeout on the coach's final "sees the client's custom intake answer" step — consistent with the dev-mode on-demand route-compilation cold-start races already logged for F32/F41/F42 in prior runs, not a product regression. Switched to CI build+start mode (`CI=1`) per that established precedent: 2/2 green (32.1s, then 32.1s again). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 41 removed — accumulated from prior sessions on the shared test project). |
| F43 | 2026-07-26 | PASS | F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, same content/`updated_at` as the last check, not re-attempted, still correctly `needs-human`. Searched open PRs/issues broadly for "F43"/"coach guidance" — no in-flight duplicates. F42 had already been re-verified earlier today by an separate run on `staging` (merged #254), closing that slot, so this run closed out the last flow in the F41/F42/F43 tied-oldest group (all last checked 2026-07-18) — F43, "Coach reviews client & leaves guidance." Designated session branch had no prior work and predated the day's merges (147 commits behind), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `coach-guidance.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach reviews the client file at `/clients/<id>` and sends a guidance message via the real coach-only `conversations` INSERT path on `/clients/<id>/messages`, client (separate browser context) opens `/my-nutritionist/messages` and sees it. 1/1 green, re-ran once more for confidence (2/2 total), no flakes, no #57 recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 37 removed — accumulated from prior sessions on the shared test project). |
| F05 | 2026-07-27 | PASS | F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) remains open/unmerged and issue #74 (missing `STRIPE_*` CI secrets) remains open, both unchanged (`updated_at` same as the last check), not re-attempted, still correctly `needs-human`. Also re-checked canonical env-limitation issue #57 (cloud MITM proxy `ERR_CONNECTION_RESET`) — still open, unchanged, not re-filed. Searched open PRs/issues broadly for "F05"/"route protection"/"redirect" — no in-flight duplicates. F42/F43 were both re-verified yesterday (2026-07-26), so the true oldest-`last_checked` tie moved to F05/F50 (both 2026-07-19) — picked F05 first by ascending flow ID, "Auth redirect / route protection." Designated session branch had no unmerged commits of its own (an ancestor of `origin/staging`), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `smoke.spec.ts` (covers F05 via "logged-out users are kept out of the app": `GET /dashboard` while unauthenticated → `/login`) plus the rest of the smoke suite as a side effect: 11/11 green, re-ran once more for confidence (22/22 total), no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files — no code changed this run so `npm run build` wasn't required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed — accumulated from prior sessions on the shared test project). |
| F01 | 2026-07-26 | PASS | F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, same content/`updated_at` as the last check, not re-attempted, still correctly `needs-human`. F41/F42/F43 were all re-verified earlier today by other sessions on `staging`, so the true oldest `last_checked` group is now F01/F05/F50 (all last checked 2026-07-19) — picked F01 first by ascending flow ID, "Sign up (create account)." Searched open PRs/issues broadly for "F01"/"signup" — no in-flight duplicates. Designated session branch had drifted 149 commits behind `origin/staging` (cut from an old pre-#62 point on `main`), reset to `origin/staging` first per the shared guardrails before picking a target — this also surfaced that F42/F43 (which the stale checkout still showed as never-checked) were already covered, avoiding a duplicate `custom-intake.spec.ts`/`coach-guidance.spec.ts` build. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `signup.spec.ts` via the deterministic suite: form renders, mismatched-password validation rejects client-side, full signup → `/onboarding`. 3/3 green, re-ran once more for confidence (3/3 again, 6/6 total). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F50 | 2026-07-27 | PASS | F60 checked for drift first (per charter) — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) remains open/unmerged and issue #74 (missing `STRIPE_*` CI secrets) remains open, both unchanged, not re-attempted, still correctly `needs-human`. Searched open PRs/issues broadly for "F50"/"review" — no in-flight duplicates. F01/F05 were both re-verified already today by other sessions on `staging`, closing out most of the F01/F05/F50 tied-oldest group (all last checked 2026-07-19), so this run closed the last slot: F50, "Client leaves a review of coach." Designated session branch had drifted onto a stale pre-merge point on `main` (109-line checklist, F42/F43/F50/F51/F60/F61 all showing as never-checked) — this initially led to building a full duplicate spec (`assigned-intake.spec.ts` + seed/flow helper changes for F42) before the mistake was caught; reset the branch to `origin/staging` per the shared guardrails, discarded the duplicate work entirely (staging already has `custom-intake.spec.ts` covering F42 since 2026-07-10), and re-picked the correct target from the up-to-date log. Fresh environment this session (`npm install` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `coach-review.spec.ts`: linked client posts a 5-star review with title+body on the coach's public profile (`POST /api/coach-reviews`, no mocking), review + refreshed aggregate rating (5.0, 1 review) render after reload, and a client with no relationship to the coach cannot see/use the review form (RLS gate, migration 057). 2/2 green, no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files) and `npm run build` passes — run because the session's working tree briefly had code changes before they were reverted. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 74 removed — accumulated from prior sessions plus this session's discarded work). |
| F02 | 2026-07-27 | PASS | F60 checked for drift first (per charter) — `.github/workflows/e2e.yml` still has no `STRIPE_*` secrets, PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged, issue #74 remains open/canonical, both unchanged since last check — not re-attempted, still correctly `needs-human`-gated. Designated session branch (`claude/zealous-lamport-4ahpl9`) had already been merged (its tip was a stale "Promote staging to prod" commit and the remote ref no longer existed), so per the branch-restart rule it was recreated fresh from `origin/staging` before picking a target. Oldest tied-`last_checked` group was F02/F03/F51/F61 (all 2026-07-20); by the ascending-flow-ID convention established in the F51 (2026-07-11) and F51 (2026-07-20) sessions, this run took F02, "Log in." Fresh environment (`npm install` at repo root + `packages/shared` build; `E2E_SUPABASE_*` already present in-environment pointed at the test project; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; `CI=1` build+start mode). Re-ran `smoke.spec.ts`'s "a seeded coach can log in" / "a seeded client can log in": both seeded users submit the real login form and land on `/dashboard`/`/onboarding`. 4/4 green (initial run + one repeat). No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 25 removed). |
| F51 | 2026-07-28 | PASS | F60 checked for drift first (per charter) — `.github/workflows/e2e.yml` still has no `STRIPE_*` secrets, PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged, and issue #74 remains open/canonical, both unchanged since the last check — not re-attempted, still correctly `needs-human`-gated. Designated session branch had no unmerged work of its own (an ancestor of `origin/staging`), reset to `origin/staging` first per the shared guardrails. Oldest tied-`last_checked` group was F03/F51/F61 (all 2026-07-20, after F02 was closed out of the prior day's tie); picked F51, "Coach responds / check-in cycle," one of the multi-user flows the charter's priority override calls out explicitly. Fresh environment this session (`npm install` at repo root, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium` to match the pre-installed browser revision against the newly-installed `@playwright/test@1.61.0`). Re-ran `coach-checkin.spec.ts`: `createLinkedPair()` seeds an already-linked coach+client, coach sends a check-in from `/clients/<id>/feedback` (real `feedback_requests` insert), client (separate browser context) answers it on `/check-ins`, coach reopens the client's feedback page and sees the completed response with all answers. 2/2 green (initial run + one repeat), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 38 removed — accumulated from prior sessions on the shared test project). |
| F03 | 2026-07-28 | PASS | F60 checked for drift first (per charter) — `.github/workflows/e2e.yml` still has no `STRIPE_*` secrets, PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`), and issue #74 (Stripe test-secrets gap) remains open/canonical, both unchanged since the last check — not re-attempted, still correctly `needs-human`-gated. Designated session branch had no unmerged work of its own (an ancestor of `origin/staging`), reset to `origin/staging` first per the shared guardrails. Oldest tied-`last_checked` group was F03/F61 (both 2026-07-20, after F51 closed out of that tie earlier today); picked F03, "Log out," by ascending flow ID. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`). Re-ran `smoke.spec.ts`'s "a logged-in user can sign out and is denied re-entry": seeded user logs in via the real form, signs out via the sidebar "Sign Out" button → redirected to `/login`, then a subsequent `/dashboard` visit still redirects to `/login`. 2/2 green (initial run + one repeat), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 36 removed — accumulated from prior sessions on the shared test project). |
| F61 | 2026-07-28 | PASS | F60 checked for drift first (per charter) — `.github/workflows/e2e.yml` still has no `STRIPE_*` secrets, PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Designated session branch had drifted onto stale pre-merge "Promote staging to prod" commits cut from `main`, reset to `origin/staging` first per the shared guardrails before picking a target. With F02/F51/F03 all re-verified earlier today, the true oldest `last_checked` was F61 alone at 2026-07-20 (the last flow left in that tied group) — "Tier boundaries after upgrade/downgrade." Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, dev-mode webServer). Re-ran `tier-boundaries.spec.ts` via the deterministic suite: seeded free client drives `/cardio` and `/supplements` through three role states via `upgradeUserRole()` — free → locked on both, upgrade to pro → both unlock on the next navigation (no re-login), downgrade back to free → both re-lock on the next navigation. 1/1 green, re-ran once more for confidence (2/2 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F10 | 2026-07-29 | PASS | F60 checked for drift first (per charter) — `.github/workflows/e2e.yml` still has no `STRIPE_*` secrets, PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Also searched open PRs/issues broadly (`list_pull_requests`, `list_issues` with label `qa`) — no in-flight duplicates targeting this run's target. Designated session branch (`claude/zealous-lamport-vmbuht`) had drifted onto a stale pre-merge "Promote staging to prod" commit cut from `main` (its own commits were all already-merged history, none unique), reset to `origin/staging` first per the shared guardrails before picking a target. True oldest `last_checked` group was F10/F11/F14 (all 2026-07-21) — picked F10 first by ascending flow ID, "Client onboarding questionnaire (9 steps → dashboard)." Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `client-onboarding.spec.ts` via the deterministic suite: questionnaire loads for a new client, full 9-step completion → `/dashboard`, free-tier gating holds on Pro-only surfaces (cardio/supplements show upgrade prompts), discover-marketplace renders for a prospective client. 4/4 green under default parallel workers, re-ran single-worker for confidence: 4/4 green again (8/8 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 36 removed — accumulated from prior sessions on the shared test project). |
| F11 | 2026-07-29 | PASS | F60 checked for drift first (per charter) — searched open PRs/issues broadly (`search_pull_requests` for F11, `list_pull_requests`, `issue_read`/`list_issues` for `qa`-labeled work): PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. No duplicate open PR/issue found targeting F11. Designated session branch (`claude/zealous-lamport-h8tizg`) had drifted onto stale pre-merge "Promote staging to prod" commits cut from `main` (5 commits ahead, all already-merged history, none unique), reset to `origin/staging` first per the shared guardrails before picking a target. F10 was re-verified earlier today by another session, closing that slot in the F10/F11/F14 tied-oldest group (all 2026-07-21), so this run continued to the next by ascending flow ID — F11, "Generate AI plan from intake." Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `generate-plans.spec.ts` via the deterministic suite: seeded client's onboarding populates `daily_calories`/`daily_protein`, `/generate-plans` auto-triggers all three AI routes (mocked via `page.route()`, no real OpenAI calls made — cost-aware per the flow's own notes), and the "Your plans are ready!" success state renders with saved-plan links. 1/1 green (23.9s), re-ran once more for confidence (2/2 total, 21.9s). No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F12 | 2026-07-30 | PASS | F60 checked for drift first (per charter) — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Also checked F14's tied-oldest slot (last checked 2026-07-21 alongside F60): open PR #290 ("[agent:qa] coverage: F14 re-verified PASS," unmerged) already covers it, so this run skipped F14 to avoid a duplicate and moved to the next-oldest tied group (F12/F20/F30, all 2026-07-22) — picked F12, "Log a meal," by ascending flow ID. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `log-meal.spec.ts` via the deterministic suite: `/diet/new` renders for a free client (AI-generator locked behind an upgrade prompt, "Add meal" available, "Save" disabled until a meal exists), then a full add-meal flow — add a blank meal, search food (mocked via `page.route()`, no external Spoonacular/OpenFoodFacts calls), select "Chicken Breast," confirm it appears in the ingredient list, save → redirect to `/diet`. 2/2 green, re-ran once more for confidence (4/4 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed — accumulated from prior sessions on the shared test project). |
| F20 | 2026-07-30 | PASS | F60 checked for drift first (per charter) — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Broadly searched open PRs/issues for duplicates targeting this run's candidates: PR #290 ("F14 re-verified PASS," open/unmerged) already covers F14's tied-oldest slot, so — same as the F12 run earlier today — F14 was skipped to avoid duplicating in-flight work, moving to the next flow in the F12/F20/F30 tied-oldest group (all last checked 2026-07-22) by ascending flow ID: F12 was closed out earlier today, so this run picked F20, "Coach setup questionnaire (6 steps)." No open PR/issue found targeting F20 specifically. Designated session branch reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `pt-onboarding.spec.ts` via the deterministic suite: "the coach setup questionnaire loads for a new coach" (step 1/6 + first prompt render) green; the full 6-step auto-fill completion test remains an intentional `test.fixme` (noted brittleness, better covered by the agentic coach-onboarding mission) — not attempted, no regression from it. 2/2 non-skipped tests green (this file also covers F21/F22's "coach publishes a marketplace profile" test), re-ran the full file once more for confidence: 2/2 green again (4/4 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F30 | 2026-07-30 | PASS | F60 checked for drift first (per charter) — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Broadly searched open PRs/issues for duplicates: PR #290 ("F14 re-verified PASS," open/unmerged) still covers F14's tied-oldest slot, so — same as the F12 and F20 runs earlier today — F14 was skipped, moving to the last remaining flow in the F12/F20/F30 tied-oldest group (all last checked 2026-07-22): F12 and F20 were both closed out earlier today, so this run picked F30, "Discover coaches renders." No open PR/issue found targeting F30 specifically. Designated session branch reset to `origin/staging` first per the shared guardrails (was stale, no unique commits). Fresh environment this session (`npm install` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `client-onboarding.spec.ts`'s "discover marketplace renders for a prospective client" test: a free client navigates to `/discover` and the "Discover coaches" heading renders. 1/1 green (11.6s), re-ran once more for confidence (2/2 total, 16.2s), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F13 | 2026-07-31 | PASS | F60 checked for drift first (per charter) — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Searched open PRs/issues for duplicates: PR #290 ("F14 re-verified PASS," open/unmerged) still covers F14's tied-oldest slot (last checked 2026-07-21), so it was skipped again to avoid duplicating in-flight work, moving to the next-oldest tied group (F13/F15/F40, all 2026-07-23) — picked F13, "Log a workout," by ascending flow ID; no open PR/issue targets it. Designated session branch reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root; `E2E_SUPABASE_*` already present in-environment pointed at the test project; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium` since the preinstalled Chromium revision didn't match `@playwright/test`'s pinned version). Dev-server mode (`npm run dev`) hit the same cold-start compile race documented elsewhere in this log: `loginAs()` timed out waiting to leave `/login` twice in a row even though the screenshot on both failures showed the dashboard had actually loaded (slow first-hit route compilation outrunning the fixture's 30s login budget) — not a product regression. Switched to `CI=1` (build+start) per the established precedent: `log-workout.spec.ts` then went 2/2 green (12-13s each), no flakes. Confirms `seedTrainingDay()` → `/training/session/<dayId>` → fill weight/reps for every set → mark complete → "Finish workout" → redirect to `/training` still works end-to-end. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing warnings only, none in touched files); `npm run build -w apps/web` passes. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F15 | 2026-07-31 | PASS | F60 checked for drift first (per charter) — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Searched open PRs for duplicates targeting F15/cardio — none found; PR #290 ("F14 re-verified PASS") remains open/unmerged but doesn't overlap this flow. F13 was just re-verified earlier today, closing that slot in the F13/F15/F40 tied-oldest group (all last checked 2026-07-23), so this run continued to the next by ascending flow ID — F15, "Cardio tracking (as Pro)." Designated session branch had drifted onto stale pre-merge "Promote staging to prod" commits cut from `main` (5 commits ahead, all already-merged history, none unique), reset to `origin/staging` first per the shared guardrails before picking a target. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `cardio-tracking.spec.ts` via the deterministic suite (scoped to the single spec file, reusing the built server for the re-run): free client force-upgraded to Pro via `upgradeUserRole()`, `/cardio` renders the tracking form (not the F14 upgrade-prompt lock screen), logs a session end-to-end, appears in the session list. 1/1 green, re-ran once more for confidence (2/2 total), no flakes. Noted in passing (not this run's target, no action taken): a broader `npm run e2e:test` pass surfaced the pre-existing F04 "full loop" sub-test failure in `reset-password.spec.ts`, consistent with the documented root cause already on canonical issue #57 (Node `fetch()`'s auto-follow collapsing the recovery-link URL fragment) — no new information, not re-commented. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 55 removed — accumulated from prior sessions on the shared test project). |
| F04 | 2026-08-01 | PASS | F60 checked for drift first (per charter) — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Searched open PRs/issues broadly: PR #307 ("F40 re-verified PASS," open/unmerged) already covers F40's tied-oldest slot (2026-07-23), so it was skipped to avoid duplicating in-flight work — same treatment as PR #290 for F14 in prior runs; PR #290 itself (F14, 2026-07-21) remains open/unmerged too and was skipped again. With both skipped, the true oldest-`last_checked` group was F04/F16/F21/F22 (all 2026-07-24) — picked F04 first by ascending flow ID, "Password reset request." No open PR/issue found targeting F04 specifically. Designated session branch (`claude/zealous-lamport-fh9lxm`) had drifted 191 commits behind `origin/staging` with only already-merged "Promote staging to prod" history of its own, reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `reset-password.spec.ts` via the deterministic suite: 4/5 green (request-form UX, check-your-inbox state, page/link rendering all real and deterministic) — the "full loop: recovery link → set new password → sign in with it" sub-test failed the same way it has consistently since 2026-07-17 (page stuck waiting on `getByLabel('New password')`, never appears). Re-ran single-worker for confidence: 4/5 again, same one sub-test both times, consistent with the documented root cause already on canonical issue #57 (`routeSupabaseThroughNode()`'s Node `fetch()` auto-follows the Supabase recovery-link redirect, collapsing the URL-fragment `access_token` before the browser ever sees it — a real user's email client does a genuine top-level navigation and hits none of this). No new information vs. the existing #57 comment thread, so not re-commented — would just add noise. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web`/`npm run build -w apps/web` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 31 removed — accumulated from prior sessions on the shared test project). |
| F16 | 2026-08-01 | PASS | F60 checked for drift first (per charter) — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, unchanged since 2026-07-07), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Searched open PRs/issues broadly: PR #307 ("F40 re-verified PASS") and PR #290 ("F14 re-verified PASS") both remain open/unmerged but neither targets F16, so no duplicate to skip here. F04 was re-verified earlier today, closing that slot in the F04/F16/F21/F22 tied-oldest group (all last checked 2026-07-24) — next by ascending flow ID, F16, "Supplements tracking (as Pro)." Designated session branch (`claude/zealous-lamport-1eqtjh`) had no unmerged work of its own (identical to `main`, ~190 commits behind `origin/staging`), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `supplements-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/supplements` renders the tracking UI (not the F14 upgrade-prompt lock screen), adds a supplement, marks it taken (`aria-pressed="true"`). 1/1 green, re-ran once more for confidence (2/2 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 46 removed — accumulated from prior sessions on the shared test project). |
| F21 | 2026-08-01 | PASS | F60 checked for drift first (per charter) — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (still `needs-human`+`agent:qa`, unchanged since 2026-07-07), and issue #74 (Stripe test-secrets gap) remains open/canonical, unchanged — not re-attempted, still correctly `needs-human`-gated. Searched open PRs/issues broadly: PR #307 ("F40 re-verified PASS") and PR #290 ("F14 re-verified PASS") both remain open/unmerged but neither targets F21/F22, so no duplicate to skip. F04 and F16 were both re-verified earlier today, closing those slots in the F04/F16/F21/F22 tied-oldest group (all last checked 2026-07-24) — this run closed the last slot, F21, by ascending flow ID (F22 covered by the same spec, logged below). Designated session branch (`claude/zealous-lamport-9t7l5i`) had no unmerged work of its own (identical to `main`, ~198 commits behind `origin/staging`), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `pt-onboarding.spec.ts` via the deterministic suite: "the coach setup questionnaire loads for a new coach" and "coach publishes a marketplace profile that renders publicly with no verified badge" both green (the full 6-step auto-fill completion test remains an intentional `test.fixme`, not attempted, no regression). Confirmed the coach publishes a public marketplace profile (headline, bio, price, Discover toggle) that renders on `/find-coach/<slug>` for a logged-out visitor. 2/2 non-skipped tests green, re-ran the full file once more single-worker for confidence: 2/2 green again (4/4 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F22 | 2026-08-01 | PASS | Same `pt-onboarding.spec.ts` run as F21 above (one test covers both): after publishing, the coach's verification request stays pending and the public profile is asserted to show NO "Verified" badge — the trust-layer guarantee still holds. No product regression, no code changes needed. |
| F31 | 2026-08-02 | BLOCKED | Priority list (F40/F60) checked first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `needs-human`-gated, unchanged since last check. Searched open PRs: #307 ("F40 re-verified PASS") and #290 ("F14 re-verified PASS") remain open/unmerged but neither targets F31, so no duplicate to skip. F31 was the oldest actionable round-robin slot (last checked 2026-07-25, itself a BLOCKED run) — F14's older 2026-07-21 slot has an open unmerged re-verify PR (#290) already covering it, so it was skipped per the no-duplicates guardrail rather than duplicated. Reset session branch to `origin/staging`, fresh env (`npm ci` + `packages/shared` build, `E2E_SUPABASE_*` already present pointed at the test project, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`). Attempted to re-verify via `pt-onboarding.spec.ts` (covers F31's "coach publishes a marketplace profile" assertions) but hit the canonical cloud MITM-proxy login constraint (issue #57) again: `loginAs()`'s Supabase token exchange succeeds but the browser never leaves `/login` within the 30s retry window. Reproduced with both the full spec file (2 workers) and a single isolated login test (`smoke.spec.ts`, 1 worker) — not a regression in the app or the spec, this session's proxy/browser connectivity (routeSupabaseThroughNode's forwarding didn't unstick it this time either). Commented on #57 with this recurrence rather than filing a new issue, per the shared guardrails' no-duplicates rule. No code change made this run (environment issue, not a product bug); synthetic test users created during the attempt cleaned up (`e2e:seed cleanup`, 22 removed). |
| F32 | 2026-08-02 | PASS | Priority list (F40/F60) checked first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `needs-human`-gated, unchanged since 2026-07-07. Searched open PRs broadly: #307 ("F40 re-verified PASS," last checked 2026-07-23) and #290 ("F14 re-verified PASS," last checked 2026-07-21) both remain open/unmerged and already cover those older tied-oldest slots, so — consistent with the no-duplicates guardrail applied in every prior run since 2026-07-30 — both were skipped rather than duplicated. F31 was re-attempted and logged BLOCKED earlier today by a separate concurrent session (cloud MITM-proxy recurrence, issue #57), so this run continued to the next flow in that tied-oldest group (F32/F41, both last checked 2026-07-25) by ascending flow ID — F32, "Request / hire a coach." No open PR/issue targets F32 specifically. Session branch reset to `origin/staging` first per the shared guardrails (designated branch had only already-merged history of its own). Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium` to match the pre-installed browser revision against `@playwright/test@1.61.0`, dev-mode webServer). Ran `coach-lead-request.spec.ts` via the deterministic suite: free client drives the real 4-step `/find-coach/<slug>/request` wizard (Goal → Logistics → Message → Review) → `POST /api/coach-leads` → redirect to `/dashboard`; coach sees the pending lead on `/leads`, accepts it; relationship persists (`personal_trainer_id` + role). 1/1 green (47.1s), re-ran once more for confidence (2/2 total, 52.4s) — no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web`/`npm run build -w apps/web` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F14 | 2026-08-14 | PASS | F60 checked for drift first (per charter): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `updated_at` unchanged since 2026-07-07 — not re-attempted, still correctly `needs-human`-gated. F40 confirmed still green (existing `invite-accept.spec.ts`, no open PR touching it), so the priority list stays resolved. Checked open PRs/issues broadly: none target F14; PRs #290/#307 referenced in older log entries are no longer open (already merged). F14 was the oldest `last_checked` flow in the log (2026-07-21) with no in-flight duplicate, so it's this run's target — "Pro-gating holds (cardio, supplements)." Designated session branch (`claude/zealous-lamport-y4z05g`) had no unmerged work of its own — its unique history was already-merged content under different hashes (an old promoted-to-prod lineage), so it was reset to `origin/staging` and force-pushed per the shared guardrails' merged-branch restart procedure (the old remote branch ref no longer existed — already cleaned up). Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Ran `client-onboarding.spec.ts` via the deterministic suite (`--workers=1`): all 4 tests green on both `chromium` and `mobile-chrome` projects (8/8), including "free-tier gating holds on Pro-only surfaces" — a free client hits `/cardio` and `/supplements` and sees the Pro upgrade prompts, not the tracking features. Re-ran the gating test standalone for confidence: 2/2 green. No flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F41 | 2026-08-04 | PASS | Priority list (F40/F60) checked first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `needs-human`-gated, unchanged since 2026-07-07. Searched open PRs/issues broadly: #307 ("F40 re-verified PASS," last checked 2026-07-23) and #290 ("F14 re-verified PASS," last checked 2026-07-21) both remain open/unmerged and already cover those older tied-oldest slots, so — consistent with the established no-duplicates guardrail treatment — both were skipped rather than duplicated. No new open PR/issue targets F41 (or F60) specifically; issue #57 (cloud MITM-proxy login constraint) unchanged. F31 (BLOCKED) and F32 (PASS) both closed out the prior tied-oldest group on 2026-08-02, so the true oldest `last_checked` is F41 alone, "Coach assigns custom anamnesis / plan." Designated session branch had drifted 137 commits behind `origin/staging` with only pre-rebrand history unique to it (14 commits, none relevant), reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build — `tsc` clean, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Ran `assign-plan.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach drives the real "New Training Plan" builder at `/clients/<id>/training/new` (name plan, add day, pick "Bench Press", save) → plan shows Active on the client file; client (separate browser context) opens their own `/training` and sees the coach-assigned plan. 1/1 green (39.7s), re-ran once more for confidence (2/2 total, 43.6s) — no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 39 removed — accumulated from prior sessions on the shared test project). |
| F60 | 2026-08-03 | BLOCKED | Priority list checked first per the charter, as always. No drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (`needs-human`+`agent:qa`, unchanged since 2026-07-07T09:51 UTC) and tracking issue #74 (missing `STRIPE_*` CI secrets) remains open/canonical, unchanged since creation — not re-attempted, still correctly `needs-human`-gated pending a person provisioning Stripe test-mode secrets. F40 (the other priority item) is fully green; this row is a drift-check note, not a new coverage cycle. |
| F41 | 2026-08-03 | PASS | Searched open PRs first (no-duplicates guardrail): #307 ("F40 re-verified PASS") and #290 ("F14 re-verified PASS") both remain open/unmerged from prior sessions, so F14's older 2026-07-21 slot was left untouched to avoid duplicating in-flight work — same treatment applied consistently since 2026-07-30. No open PR targets F41. F32 was re-verified earlier by a separate session, closing that half of the F32/F41 tied-oldest group (both last checked 2026-07-25); this run closed the remaining slot, F41, "Coach assigns custom anamnesis / plan." Session branch reset to `origin/staging` first per the shared guardrails (designated branch had no unmerged work of its own). Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `assign-plan.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach drives the real "New Training Plan" builder (name plan, add day, pick an exercise from the shared catalog, save) → plan shows Active on the client file; client, in a separate browser context, opens `/training` and sees the coach-assigned plan. 1/1 green, re-ran once more for confidence (2/2 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 4 removed). |
| F41 | 2026-08-02 | BLOCKED | Priority list (F40/F60) checked first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `needs-human`-gated, unchanged since 2026-07-07. Searched open PRs broadly: #307 ("F40 re-verified PASS") and #290 ("F14 re-verified PASS") both remain open/unmerged and already cover those older slots (last checked 2026-07-21/2026-07-23 respectively), so — consistent with the standing no-duplicates precedent — both were skipped rather than duplicated. F32 was re-verified earlier today (by a separate concurrent session), closing the last slot in the F32/F41 tied-oldest group (both last checked 2026-07-25), so this run picked up F41, "Coach assigns custom anamnesis / plan." Session branch reset to `origin/staging` first per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Attempted to re-verify via `assign-plan.spec.ts` but hit the canonical cloud MITM-proxy login constraint (issue #57) again: `loginAs()`'s Supabase token exchange completes but the browser never leaves `/login` within the 30s retry window in `fixtures.ts`'s `loginAs()`. `HTTPS_PROXY` was set and `routeSupabaseThroughNode()`'s forwarding branch was active, so this isn't the fixture missing the workaround. Reproduced twice — default parallel workers, then `--workers=1` — identical failure both times, same signature as the F31 recurrences logged 2026-07-25 and earlier today (2026-08-02): transient cloud-proxy/browser connectivity, not a product or spec regression. Commented on #57 with this recurrence rather than filing a new issue, per the shared guardrails' no-duplicates rule. No code change made this run (environment issue, not a product bug); synthetic test users created during the attempts cleaned up (`e2e:seed cleanup`, 48 removed — accumulated from prior sessions on the shared test project). |
| F01 | 2026-08-03 | PASS | Priority list (F40/F60) checked for drift first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `needs-human`-gated; F60 was already drift-checked and logged BLOCKED earlier today by a separate session, not re-logged. F41 was re-verified PASS earlier today, closing the F32/F41 tied-oldest group. Recomputed oldest `last_checked`: F14 (2026-07-21) and F40 (2026-07-23) both have open unmerged re-verify PRs (#290, #307) from prior sessions already covering them, so — consistent with the no-duplicates guardrail applied since 2026-07-30 — both were skipped rather than duplicated. That leaves a three-way tied-oldest group at 2026-07-26: F01, F42, F43 — picked F01 first by ascending flow ID, "Sign up (create account)." Searched open PRs/issues broadly for "F01"/"signup" — no in-flight duplicates. Session branch reset to `origin/staging` first per the shared guardrails (designated branch had no unmerged work of its own). Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Ran `signup.spec.ts` via the deterministic suite (chromium + mobile-chrome, 6 tests): signup page renders the account form; mismatched passwords rejected client-side without creating an account; a new user signs up (confirm-email off on the test project) and lands on `/onboarding`. 6/6 green (2.3m), re-ran once more for confidence (12/12 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 37 removed — accumulated from prior sessions on the shared test project). |
| F42 | 2026-08-03 | PASS | Priority list (F40/F60) checked for drift first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (`needs-human`+`agent:qa`, unchanged since 2026-07-07) and issue #74 (missing `STRIPE_*` CI secrets) remains open/canonical, unchanged; F60 already drift-checked and logged BLOCKED earlier today by a separate session, not re-logged. F01 was re-verified PASS earlier today, closing that slot in the F01/F42/F43 tied-oldest group (all last checked 2026-07-26) — this run picked the next by ascending flow ID, F42, "Client completes assigned intake; coach sees it." Searched open PRs/issues broadly (`list_pull_requests` open, `list_issues` label `qa`): #307 ("F40 re-verified PASS") and #290 ("F14 re-verified PASS") remain open/unmerged from prior sessions covering those older slots (2026-07-21/2026-07-23), left untouched per the standing no-duplicates precedent; no open PR/issue targets F42 specifically. Designated session branch (`claude/zealous-lamport-d39689`) had ~137 commits of already-merged history under different SHAs (cut from a stale pre-merge "Promote staging to prod" point, none unique vs. `origin/staging`), reset to `origin/staging` first per the shared guardrails before picking a target — this surfaced that F41/F01 (which the stale checkout still showed as older/never-re-checked) had already been re-verified by other sessions today, avoiding duplicate work. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `custom-intake.spec.ts` via the deterministic suite: coach adds a required custom intake question via the real `/coach-questions` UI, client drives the real `/onboarding` wizard through the "Coach Questions" step and answers it, coach opens `/clients/<id>` and sees the answer under "Custom intake answers." 1/1 green (33.9s), re-ran once more for confidence (2/2 total, 33.6s), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 47 removed — accumulated from prior sessions on the shared test project). |
| F43 | 2026-08-04 | PASS | Priority list (F40/F60) checked for drift first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (`needs-human`+`agent:qa`, unchanged since 2026-07-07) and issue #74 (missing `STRIPE_*` CI secrets) remains open/canonical, unchanged; issue #69 (F40 RESEND_API_KEY gap) also unchanged. Searched open PRs/issues broadly (`list_pull_requests` open, `list_issues` label `qa`): #307 ("F40 re-verified PASS," covering the 2026-07-23 slot) and #290 ("F14 re-verified PASS," covering the 2026-07-21 slot) both remain open/unmerged from prior sessions, left untouched per the standing no-duplicates precedent — neither targets F43. F01/F41/F42 were all re-verified PASS earlier this week, closing the rest of that tied-oldest group, so this run picked the true oldest remaining flow, F43 (last checked 2026-07-26), "Coach reviews client & leaves guidance." Designated session branch (`claude/zealous-lamport-3fiomx`) was on a stale pre-merge "Promote staging to prod" commit ~190+ commits behind `origin/staging` (no unique work of its own), reset to `origin/staging` first per the shared guardrails before picking a target — this surfaced that F41/F42/F01 (which the stale checkout still showed as never-re-checked) had already been re-verified by other sessions, avoiding duplicate work. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). First build attempt failed: the `@next/swc-linux-x64-gnu` native binary was missing from `node_modules` despite being pinned in `package-lock.json` (silently skipped by `npm ci` this session, cause unclear), which cascaded into Next.js's swc-wasm-fallback downloader shelling out to `yarn config get registry` — intercepted by Corepack, which errored on the repo's `packageManager: npm@11.9.0` field ("This project's package.json defines... yarn@npm@11.9.0"). This is environment/tooling flakiness, not a product or spec issue — installed the missing optional dependency directly (`npm install --no-save @next/swc-linux-x64-gnu@15.3.5`), after which `next build` succeeded cleanly. Re-ran `coach-guidance.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach reviews the client file at `/clients/<id>` and sends a guidance message via the real coach-only `conversations` INSERT path on `/clients/<id>/messages`, client (separate browser context) opens `/my-nutritionist/messages` and sees it. 1/1 green (21.1s), re-ran once more for confidence (2/2 total, 21.5s), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification (the missing SWC binary was a local `node_modules` install gap, not a tracked repo change). `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 37 removed — accumulated from prior sessions on the shared test project). |
| F02 | 2026-08-04 | PASS | Priority list (F40/F60) checked for drift first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (`needs-human`+`agent:qa`, unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing `STRIPE_*` CI secrets) remains open/canonical, unchanged since filing. Searched open PRs/issues broadly (`list_pull_requests` open, `issue_read`/`search_issues` for `qa`-labeled work): #307 ("F40 re-verified PASS," covering the 2026-07-23 slot) and #290 ("F14 re-verified PASS," covering the 2026-07-21 slot) both remain open/unmerged from prior sessions, left untouched per the standing no-duplicates precedent — neither targets F02, and no other open PR/issue duplicates this run's target. F01/F42/F43 were all re-verified PASS this week, closing that tied-oldest group (2026-07-26); recomputed oldest `last_checked` on `origin/staging` and found a three-way tie at 2026-07-27 — F02, F05, F50 — picked F02 first by ascending flow ID, "Log in." Designated session branch (`claude/zealous-lamport-26jvbn`) was on a stale pre-merge "Promote staging to prod" commit 17 behind `origin/staging` (no unique work of its own), reset to `origin/staging` first per the shared guardrails before picking a target. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent; `@next/swc-linux-x64-gnu` was present this session, no repeat of the prior run's missing-binary flake). Ran `smoke.spec.ts` via the deterministic suite (chromium, 11 tests, covers F02 + F03 + public-page/route-protection smoke checks): "a seeded coach can log in" and "a seeded client can log in" both land on `/dashboard` or `/onboarding` off the real `/login` form via `loginAs`. 11/11 green (2.0m), re-ran the full file once more for confidence (22/22 total across both runs), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 24 removed — accumulated from prior sessions on the shared test project). |
| F05 | 2026-08-05 | PASS | Priority list (F40/F60) checked for drift first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (`needs-human`+`agent:qa`, unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing `STRIPE_*` CI secrets) remains open/canonical, unchanged since filing. Searched open PRs/issues broadly (`list_pull_requests` open, `issue_read` for `qa`-labeled work): #307 ("F40 re-verified PASS," covering the 2026-07-23 slot) and #290 ("F14 re-verified PASS," covering the 2026-07-21 slot) both remain open/unmerged from prior sessions, left untouched per the standing no-duplicates precedent — neither targets F05, and no other open PR/issue duplicates this run's target. F02 was re-verified PASS earlier today, closing that slot in the three-way F02/F05/F50 tied-oldest group (all last checked 2026-07-27) — this run picked the next by ascending flow ID, F05, "Auth redirect / route protection." Designated session branch had only already-merged history of its own (137 commits behind `origin/staging`), reset to `origin/staging` first per the shared guardrails before picking a target. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `smoke.spec.ts` via the deterministic suite (chromium): "logged-out users are kept out of the app" (F05's test — unauthenticated `GET /dashboard` → `/login` redirect) passed in the full 11-test file run, plus the seeded-coach/seeded-client login tests and sign-out/route-protection tests all green too. 11/11 green, re-ran F05's test standalone for confidence (1/1 green, 12/12 total this run), no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 36 removed — accumulated from prior sessions on the shared test project). |
| F14 | 2026-08-05 | PASS | Priority list (F40/F60) checked for drift first per the charter — no drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) remains open/unmerged (`needs-human`+`agent:qa`, unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing `STRIPE_*` CI secrets) remains open/canonical, unchanged since filing. F14 (last checked 2026-07-21) was the true oldest `last_checked` flow, but it already had an open re-verify PR (#290, "F14 re-verified PASS") from a prior session — however that PR had been stuck for a week: every gatekeeper pass was reading the same stale `e2e` CI run (30470817051) from the PR's original 2026-07-29 commit, which was never re-triggered, and that stale run's failure (`supplements-tracking.spec.ts:34`, a "Vitamin D3" strict-mode locator collision) doesn't reproduce on current `staging` — recent `E2E (deterministic)` runs on other PRs are all green. Rather than leave it blocking indefinitely or duplicate it silently, closed #290 with an explanation and re-verified F14 fresh from current `staging` in this run. Searched open PRs/issues broadly otherwise: no other open PR/issue targets F14. Designated session branch had no unmerged work of its own, cut fresh from `origin/staging`. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment pointed at the test project; had to additionally set `E2E_BASE_URL=http://localhost:3000` explicitly, and use `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium` — the pre-installed browser revision (1194) doesn't match this repo's pinned `@playwright/test` (which wants the `chrome-headless-shell` for a newer revision), so the config's documented `PLAYWRIGHT_EXEC_PATH` escape hatch was needed, per the established cold-start-flake precedent). Ran `client-onboarding.spec.ts` via the deterministic suite (chromium, 4 tests): 3/4 green first pass — the one failure was an unrelated test (F10, "the onboarding questionnaire loads for a new client") hitting a login timeout, consistent with the cold-start races already documented elsewhere in this suite; F14's own test, "free-tier gating holds on Pro-only surfaces," was green in that run. Re-ran F14's test standalone for confidence — 1/1 green (2/2 total for F14 across both runs), no flakes. A free client hitting `/cardio` and `/supplements` still sees the upgrade prompts, not the tracking features. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 55 removed — accumulated from prior sessions on the shared test project). |
| F40 | 2026-08-05 | PASS | Priority list (F40/F60) checked first per the charter — F60 checked for drift: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `needs-human`-gated, unchanged since 2026-07-07 — not re-attempted. F40 itself (last checked 2026-07-23) was the true oldest round-robin slot, but it already had an open re-verify PR (#307, "F40 re-verified PASS") from a prior session — that PR had been stuck since 2026-07-31: five consecutive gatekeeper passes all found the `e2e` check on its head commit `cancelled` rather than `SUCCESS` (never re-triggered), the same stale-CI pattern that stalled the old F05/#124 PR for 12 runs. The content itself was never in question — F40 has independently PASS'd on `staging` multiple times since (2026-08-03/08-04 sessions), and this is a docs-only 1-line coverage append — so, following the #124 precedent, closed #307 with an explanation and re-verified F40 fresh from current `staging` instead of perpetuating the stale-CI loop. Searched open PRs/issues broadly otherwise: no other open PR/issue targets F40. Designated session branch (`claude/zealous-lamport-v2bb0o`) had no remote ref yet and local `HEAD` was far behind (an old pre-rebrand `main` state), created fresh from `origin/staging` per the shared guardrails. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment pointed at the test project; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium` since the pre-installed browser revision (1194) doesn't match this repo's pinned `@playwright/test`; `CI=1` build+start mode per the established cold-start-flake precedent). Ran `invite-accept.spec.ts` via the deterministic suite: coach sees the pending invite on `/clients`, client accepts via the real `/invite/accept` UI, relationship persists (`personal_trainer_id` + role), coach sees the client as active. 1/1 green (22.7s), re-ran once more for confidence (2/2 total, 21.2s), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F50 | 2026-08-06 | PASS | Priority list (F40/F60) checked first per the charter — no drift: F40 remains fully resolved (last PASS 2026-08-05), and F60 checked via `list_pull_requests`/`list_issues` — PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both remain open/unmerged, `needs-human`-gated, unchanged since 2026-07-07 — not re-attempted. Designated session branch had drifted ~5 commits behind `origin/staging` (no unique work of its own), reset to `origin/staging` first per the shared guardrails before picking a target — this also confirmed PRs #290 (F14) and #307 (F40) were already closed by prior sessions today/yesterday, so neither needed skipping. Recomputed oldest `last_checked` from the up-to-date log: F50 alone at 2026-07-27 (its former tied group, F02/F05, was closed out 2026-08-04/08-05) — picked F50, "Client leaves a review of coach." Searched open PRs/issues broadly for "F50"/"review" — no in-flight duplicates. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Re-ran `coach-review.spec.ts` via the deterministic suite: `createLinkedPair()` seeds a coach + linked client, client opens `/find-coach/<slug>`, rates 5 stars, posts a title + body review via the real `POST /api/coach-reviews` path, reloads and confirms the review renders plus the denormalised `rating_avg`/`rating_count` aggregate updates to "5.0" / "1 review", and a second client with no relationship to the coach can see the review but not the review form (RLS gate). 1/1 green (24.8s), re-ran once more for confidence (2/2 total, 21.0s), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 28 removed — accumulated from prior sessions on the shared test project). |
| F03 | 2026-08-06 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `issue_read`/`pull_request_read` — issue #74 unchanged since 2026-07-07 and PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07 — no drift, not re-attempted. Round-robin then continued: F03/F51/F61 were tied-oldest (last checked 2026-07-28); this run took F03, "Log out," by ascending flow ID (F50 was already covered earlier today by a prior session). Session branch had drifted 33 commits behind `origin/staging` with no unique work of its own, reset to it first per the shared guardrails. Fresh environment this session (`npm install` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1`). Ran `smoke.spec.ts` via the deterministic suite: seeded client logs in, signs out via the header button, redirected to logged-out home, and a subsequent protected-route hit redirects back to `/login` — 11/11 smoke tests green, no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint`/`npm run build` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 0 remaining — prior sessions already tidy). |
| F51 | 2026-08-06 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `list_pull_requests`/`list_issues` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, both `updated_at` unchanged since 2026-07-07 — no drift, not re-attempted. Designated session branch had drifted 137 commits behind `origin/staging` (only already-merged ancestor history, no unique work of its own), reset to `origin/staging` first per the shared guardrails before picking a target — this also surfaced that F03 had already been re-verified earlier today by a prior session, closing that slot in the F03/F51/F61 tied-oldest group (last checked 2026-07-28), so this run continued to the next by ascending flow ID: F51, "Coach responds / check-in cycle." Searched open PRs/issues broadly for "F51"/"check-in" — no in-flight duplicates (open PRs today are #363/#335/#332/#291/#281/#279/#199/#77/#75, none targeting F51). Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `coach-checkin.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach sends a check-in from the real `/clients/<id>/feedback` UI (default text/rating/yes-no question set), client (separate browser context) answers it on `/check-ins`, coach reopens `/clients/<id>/feedback` and sees the completed response with all three answers rendered. 1/1 green (25.7s), re-ran once more for confidence (2/2 total, 21.5s), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 26 removed — accumulated from prior sessions on the shared test project). |
| F61 | 2026-08-07 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `pull_request_read`/`issue_read` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted. Designated session branch had drifted 37 commits behind `origin/staging` with unrelated pre-merge history (no PR opened against it, no unique work), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked`: F03/F51 had both been re-verified earlier today by prior sessions, closing the rest of the F03/F51/F61 tied-oldest group (all last checked 2026-07-28) — this run took the remaining slot, F61, "Tier boundaries after upgrade/downgrade." Searched open PRs/issues broadly for "F61"/"tier boundaries" — no in-flight duplicates. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `tier-boundaries.spec.ts` via the deterministic suite: seeded free client drives `/cardio` and `/supplements` through three role states via `upgradeUserRole()` — free → locked on both, upgrade to pro → both unlock on the next navigation (no re-login), downgrade back to free → both re-lock on the next navigation. 1/1 green (18.4s), re-ran once more for confidence (2/2 total, 17.8s), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F10 | 2026-08-07 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `list_pull_requests`/`list_issues` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, both unchanged since 2026-07-07 — no drift, not re-attempted. Searched open PRs broadly (`list_pull_requests` open): #369/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75, none targeting F10 — no in-flight duplicate. Designated session branch (`claude/zealous-lamport-8c5an1`) had 137 commits of already-merged history (stale "Promote staging to prod" point) and was 40 behind `origin/staging`, reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` from the up-to-date log: F10/F11 tied-oldest at 2026-07-29 — picked F10 first by ascending flow ID, "Client onboarding questionnaire (9 steps → dashboard)." Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `client-onboarding.spec.ts` via the deterministic suite (chromium + mobile-chrome, 8 tests): questionnaire loads for a new client, full 9-step completion → `/dashboard`, free-tier gating holds on Pro-only surfaces, discover-marketplace renders for a prospective client. 8/8 green (2.5m), re-ran F10's own two tests standalone for confidence (2/2 green, 10/10 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 43 removed — accumulated from prior sessions on the shared test project). |
| F11 | 2026-08-07 | PASS | Priority list (F40/F60) checked for drift first per the charter (`get_me`, `list_pull_requests`, `list_issues`): F40 remains fully resolved (last PASS 2026-08-05); F60 unchanged — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, both `updated_at` unchanged since 2026-07-07 — no drift, not re-attempted. Open PRs today: #373/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75, none targeting F11 — no in-flight duplicate. Designated session branch (`claude/zealous-lamport-ki7cnh`) had 137 commits of already-merged "Promote staging to prod" history cut from `main`, no unique work of its own, reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` from the up-to-date log: F10 was re-verified earlier today by a separate concurrent session, closing that half of the F10/F11 tied-oldest group (both 2026-07-29) — this run took the remaining slot, F11, "Generate AI plan from intake." Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `generate-plans.spec.ts` via the deterministic suite: seeded client's onboarding populates `daily_calories`/`daily_protein`, `/generate-plans` auto-triggers all three AI routes (mocked via `page.route()`, no real OpenAI calls made — cost-aware per the flow's own notes), and the "Your plans are ready!" success state renders with saved-plan links. 1/1 green (22.2s), re-ran once more for confidence (2/2 total, 21.0s), no flakes. An earlier full-suite smoke run (before scoping to this spec) reproduced the pre-existing F04 `reset-password.spec.ts` "full loop" sub-test failure — consistent with the documented root cause already on canonical issue #57, no new information, not re-commented. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 49 removed — accumulated from prior sessions on the shared test project). |
| F12 | 2026-08-08 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `pull_request_read`/`issue_read` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted. F10/F11 were both re-verified 2026-08-07, so this run picked the next-oldest untouched flow, F12 "Log a meal" (last checked 2026-07-30). Re-ran `log-meal.spec.ts` via the deterministic suite (fresh env this session — `npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, CI build+start mode per the established cold-start-flake precedent): `/diet/new` renders and is gated for free tier, plus the full add-meal-with-food-item → save → `/diet` flow. 2/2 green, re-ran once more for confidence (4/4 total). No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F20 | 2026-08-08 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `list_pull_requests`/`list_issues` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, both `updated_at` unchanged since 2026-07-07 — no drift, not re-attempted. Designated session branch (`claude/zealous-lamport-3wu944`) had only already-merged "Promote staging to prod" history of its own, no unique work, reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` from the up-to-date log (taking the max date per flow, since concurrent sessions appended rows out of chronological order): F20/F30 tied-oldest at 2026-07-30 — picked F20 first by ascending flow ID, "Coach setup questionnaire (6 steps)." Searched open PRs broadly (`list_pull_requests` open: #379/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75) — none target F20 or F30, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `pt-onboarding.spec.ts` via the deterministic suite: "the coach setup questionnaire loads for a new coach" and "coach publishes a marketplace profile that renders publicly with no verified badge" both green (the full 6-step auto-fill completion test remains an intentional `test.fixme`, not attempted, no regression). 2/2 non-skipped tests green, re-ran the full file once more single-worker for confidence: 2/2 green again (4/4 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. F30 (tied-oldest, same 2026-07-30) left for the next run per the charter's "exactly one flow per run," though its `client-onboarding.spec.ts` coverage was spot-checked this session too and is green (8/8, plus a standalone re-run of its discover-marketplace test, 2/2) — no drift there either. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F30 | 2026-08-08 | PASS | Priority list (F40/F60) checked for drift first per the charter: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged (`needs-human`+`agent:qa`) and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, both unchanged since 2026-07-07 — no drift, not re-attempted. Designated session branch had only already-merged "Promote staging to prod" history of its own (no unique work), reset to `origin/staging` first per the shared guardrails before picking a target — that reset pulled in same-day merges not yet visible at first read, including F20's 2026-08-08 re-verification row, so the oldest `last_checked` was recomputed against the fresh log: F20/F30 were tied-oldest at 2026-07-30, F20 had just been closed out by that other run (which explicitly spot-checked F30 green but left it unlogged per the charter's "exactly one flow per run"), so this run closed out the remaining slot, F30 — "Discover coaches renders." (This run's own first attempt, made against the stale pre-reset log, had re-verified F14 — already independently re-verified PASS on 2026-08-05 by another session, so that result was discarded as a duplicate and is not logged here.) Searched open PRs/issues broadly for "F30" — no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Re-ran the existing `client-onboarding.spec.ts` "discover marketplace renders for a prospective client" test via the deterministic suite (single worker): free client hits `/discover`, sees "Discover Coaches" heading. 1/1 green, re-ran once more for confidence (2/2 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 0 removed this run — the prior session's run already cleaned up). |
| F13 | 2026-08-09 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `list_pull_requests`/`list_issues` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted. Designated session branch (`claude/zealous-lamport-u0nle4`) had no unmerged work of its own (137 commits behind `origin/staging` with only stale pre-merge history), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` from the fresh log (max date per flow, since concurrent sessions append out of order): F13/F15 tied-oldest at 2026-07-31 — picked F13 first by ascending flow ID, "Log a workout." Searched open PRs/issues broadly (`list_pull_requests` open: #385/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #74/#69/#57) — none target F13 or F15, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `log-workout.spec.ts` via the deterministic suite: `seedTrainingDay()` seeds a one-day training plan, free client opens `/training/session/<dayId>`, fills weight+reps for every set, marks each complete, finishes the workout → redirects to `/training`. 2/2 green (chromium + mobile-chrome), re-ran single-worker for confidence: 2/2 green again (4/4 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web`/`npm run build -w apps/web` not run this session (no code changed, spec-only re-verification against an already-merged spec). F15 (tied-oldest, same 2026-07-31) left for the next run per the charter's "exactly one flow per run." Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F15 | 2026-08-09 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `list_pull_requests`/`list_issues` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted. Designated session branch (`claude/zealous-lamport-2y9e9m`) had only already-merged pre-rebrand history of its own (no unique work, 137 commits behind `origin/staging`), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` from the fresh log (max date per flow, since concurrent sessions append out of order): F13 was re-verified earlier today, leaving F15 as the sole oldest at 2026-07-31 — "Cardio tracking (as Pro)." Searched open PRs/issues broadly (`list_pull_requests` open: #388/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #74/#69/#57) — none target F15, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`). First attempt (dev-mode webServer) hit the well-documented cold-start login flake (`loginAs()`'s post-login URL assertion caught the page bounced back to `/login`); switched to `CI=1` build+start mode per the established precedent. Re-ran `cardio-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/cardio` renders the tracking form (not the F14 upgrade-prompt lock screen), logs a session end-to-end, appears in the session list. 1/1 green, re-ran once more for confidence (2/2 total), no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 46 removed — accumulated from prior sessions on the shared test project). |
| F04 | 2026-08-09 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `pull_request_read`/`issue_read` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted. Designated session branch (`claude/zealous-lamport-dvz3vr`) had only already-merged "Promote staging to prod" history of its own (identical to `origin/main`, no unique work, diverged from `origin/staging`), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` from the fresh log (max date per flow, since concurrent sessions append out of order): F04/F16/F21/F22 tied-oldest at 2026-08-01 — picked F04 first by ascending flow ID, "Password reset request." Searched open PRs/issues broadly (`list_pull_requests` open: #392/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` open: #331/#288/#206/#74/#69/#66/#57) — none target F04, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `reset-password.spec.ts` via the deterministic suite: 4/5 green — request-form UX (`/reset-password` renders, `/login`'s "Forgot your password?" link, email-request form, "check your inbox" state) all real and deterministic. The "full loop: recovery link → set new password → sign in with it" sub-test failed the same way it consistently has since 2026-07-17 (page stuck on "Checking your reset link..." — `getByLabel('New password')` never appears), the documented `routeSupabaseThroughNode()` limitation already on canonical issue #57 (Node `fetch()` auto-follows the Supabase recovery-link redirect, collapsing the URL-fragment `access_token` before the browser ever sees it — a real user's email client does a genuine top-level navigation and hits none of this). No new information vs. the existing #57 comment thread, so not re-commented. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F16 | 2026-08-10 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `list_pull_requests`/`list_issues` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted. Designated session branch (`claude/zealous-lamport-d1qt15`) had only already-merged "Promote staging to prod" history of its own (~137 commits behind `origin/staging`, no unique work), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` from the fresh log (max date per flow): F04 was re-verified earlier today by another session, leaving F16/F21/F22 tied-oldest at 2026-08-01 — picked F16 first by ascending flow ID, "Supplements tracking (as Pro)." Searched open PRs/issues broadly (`list_pull_requests` open: #364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #74/#69/#57) — none target F16/F21/F22, no in-flight duplicate. Fresh environment this session (`npm install` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `supplements-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/supplements` renders the tracking UI (not the F14 upgrade-prompt lock screen), adds a supplement, marks it taken (`aria-pressed="true"`). 1/1 green, re-ran once more for confidence (2/2 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. F21/F22 (tied-oldest, same 2026-08-01) left for the next run per the charter's "exactly one flow per run." Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed — accumulated from prior sessions on the shared test project). |
| F21 | 2026-08-10 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `list_pull_requests`/`list_issues` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted. Designated session branch (`claude/zealous-lamport-t1z9tt`) had only already-merged "Promote staging to prod" history of its own (0 commits either way vs. `origin/main`, no unique work), reset to `origin/staging` first per the shared guardrails. That reset pulled in same-day merges not visible at the session's first read of this file (round-robin computed pre-reset had wrongly pointed at F14, already independently re-verified PASS on 2026-08-05 — discarded once the reset surfaced the current log, no code/log change made from that stale target). Recomputed oldest `last_checked` from the fresh log: F16 was re-verified earlier today by another session, leaving F21/F22 as the sole tied-oldest group at 2026-08-01 — picked F21 first by ascending flow ID, "Publish public marketplace profile" (F22 covered by the same spec, logged below). Searched open PRs/issues broadly (`list_pull_requests` open: #364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #74/#69/#57) — none target F21/F22, no in-flight duplicate; also confirmed PRs #290/#307 (which had shadowed F14/F40 slots in several prior runs) are now `closed`/unmerged, no longer in-flight. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Ran `pt-onboarding.spec.ts` via the deterministic suite: "the coach setup questionnaire loads for a new coach" and "coach publishes a marketplace profile that renders publicly with no verified badge" both attempted. First pass (2 workers) hit a login-timeout flake on the publish test (stuck on `/login`, same signature as the documented cold-start races — not the #57 MITM-proxy pattern, no `net::ERR_CONNECTION_RESET`); re-ran single-worker and both non-skipped tests went green, then re-ran the full file single-worker once more for confidence: 2/2 green both times (4/4 total), no #57 MITM-proxy recurrence this session. Confirmed the coach publishes a public marketplace profile (headline, bio, price, Discover toggle) that renders on `/find-coach/<slug>` for a logged-out visitor. No product regression, no code changes needed — pure re-verification. `npm run lint` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not run — no code changed, spec-only re-verification against an already-merged spec. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 12 removed — accumulated from prior sessions on the shared test project). |
| F22 | 2026-08-10 | PASS | Same `pt-onboarding.spec.ts` run as F21 above (one test covers both): after publishing, the coach's verification request stays pending and the public profile is asserted to show NO "Verified" badge — the trust-layer guarantee still holds. No product regression, no code changes needed. |
| F31 | 2026-08-10 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-05); F60 checked via `list_pull_requests`/`list_issues` — PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged with `needs-human`/`agent:qa`, and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, both unchanged since 2026-07-07 — no drift, not re-attempted. Session started on the designated branch with no local `staging`; fetched and checked out `origin/staging` fresh per the shared guardrails (`git fetch origin staging && git checkout -B staging origin/staging`) before reading the log, since several concurrent sessions had already merged same-day coverage runs (F16, F04, F21/F22) not visible from a stale checkout. Recomputed oldest `last_checked` from the fresh log: F31 (BLOCKED, 2026-08-02) and F32 (PASS, 2026-08-02) tied-oldest — picked F31 first by ascending flow ID, "View a public coach profile." Its prior BLOCKED entry (2026-08-02) hit the canonical cloud MITM-proxy login constraint (issue #57); searched open PRs/issues broadly (`list_pull_requests` open: #402/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` open: #398/#288/#206/#74/#69/#66/#57) — none target F31 specifically, no in-flight duplicate to avoid. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `pt-onboarding.spec.ts` via the deterministic suite (single worker): "the coach setup questionnaire loads for a new coach" and "coach publishes a marketplace profile that renders publicly with no verified badge" (which drives a logged-out visitor to `/find-coach/<slug>` and asserts F31's headline/CTA/back-link rendering) both green; re-ran the full file once more for confidence — 2/2 green both times (4/4 total), no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session. The prior BLOCKED result was environment flakiness, not a reproducible product or spec regression. No code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not run — no code changed, spec-only re-verification against an already-merged spec. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 34 removed — accumulated from prior sessions on the shared test project). |
| F14 | 2026-08-11 | PASS | Priority list (F40/F60) checked for drift first per the charter: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted. Designated session branch had been cut from `main` (137 old "promote to prod" commits ahead of `origin/staging`, no unique unmerged work), so reset to `origin/staging` first per the shared guardrails. Recomputed oldest `last_checked` from the log: F14 "Pro-gating holds (cardio, supplements)" (2026-07-21) was the true oldest untouched flow. Searched open PRs/issues (`search_pull_requests` for F14 — none; `list_issues` label `qa` — #74/#69/#57, none targeting F14) — no in-flight duplicate. Fresh environment (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Ran `client-onboarding.spec.ts` via the deterministic suite: "free-tier gating holds on Pro-only surfaces" green, plus the other 3 tests in the file (onboarding loads, full 9-step completion, discover-coaches render) all green — 4/4. No product regression, no code changes needed. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not run — no code changed, spec-only re-verification against an already-merged spec. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 34 removed — accumulated from prior sessions on the shared test project). |
| F32 | 2026-08-11 | FAIL | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`/`agent:qa`, unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-05). Recomputed oldest `last_checked` from the log after resetting the designated session branch to fresh `origin/staging` (several same-day merges — up to #407 — not visible from the stale checkout): F32 "Request / hire a coach" (2026-08-02) was the true oldest untouched flow, no open PR/issue targeting it. Ran `coach-lead-request.spec.ts` via the deterministic suite (`CI=1` build+start) three times for confidence: 1st run 1/1 green (22.0s); 2nd run FAILED on both the initial attempt and Playwright's automatic retry — the coach was bounced from `/leads` to `/dashboard`'s onboarding-incomplete gate ("Complete your coach setup") instead of seeing the pending lead, timing out on `getByText(client.email)`; 3rd run 1/1 green again (19.5s). This is a genuine intermittent failure, not the canonical #57 MITM-proxy pattern (page rendered fine, just wrong content, no `net::ERR_CONNECTION_RESET`). Read `leads/page.tsx`, `dashboard/page.tsx`, `(app)/layout.tsx`, `middleware.ts`, `useUser.ts`, and migration 055's `handle_new_user` trigger to narrow it down: the only `/leads`→`/dashboard` redirect path is `leads/page.tsx`'s `if (profile && !isTrainerRole(profile.role)) router.push('/dashboard')`, and the seeded coach's role is set synchronously and correctly by the trigger (no async race there), so the leading hypothesis is a transient/incorrect first `profile` read from `useUser.ts` (which doesn't check the Supabase query's `error` before `setProfile`) — not confirmed under a debugger, so not attempted as a code fix per the guardrail against flailing at a bug that isn't cleanly fixable. Filed GitHub issue #410 (label `qa`) with full repro, hypothesis, and pointers for whoever picks it up. No code changes made. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files). Synthetic test users cleaned up after the run (`e2e:seed cleanup`). |
| F41 | 2026-08-11 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`/`issue_read`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-05). Designated session branch had been cut from a stale pre-merge point on `main`, reset to fresh `origin/staging` first per the shared guardrails before picking a target — that reset surfaced several same-day merges (up to #412, including F32's FAIL/#410 filing and F14's re-verification, both earlier today). Recomputed oldest `last_checked` from the fresh log: F41 "Coach assigns custom anamnesis / plan" (2026-08-02, previously logged BLOCKED on the canonical cloud MITM-proxy constraint, issue #57) was the true oldest untouched flow — checked open PRs (`list_pull_requests`: #413/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75) and open `qa`-labeled issues (#410/#74/#69/#57) broadly, none target F41, no in-flight duplicate to avoid. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `assign-plan.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach drives the real "New Training Plan" builder at `/clients/<id>/training/new` (name plan, add day, pick "Bench Press", save) → plan shows Active on the client file; client (separate browser context) opens their own `/training` and sees the coach-assigned plan. 1/1 green (23.3s), re-ran once more for confidence (2/2 total, 20.8s) — no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session, confirming the prior BLOCKED result was transient cloud-proxy flakiness, not a reproducible product or spec regression. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not run — no code changed, spec-only re-verification against an already-merged spec. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 37 removed — accumulated from prior sessions on the shared test project). |
| F60 | 2026-08-12 | BLOCKED | Priority list checked first per the charter, as always. No drift: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) remains open/unmerged (`needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC) and tracking issue #74 (missing `STRIPE_*` CI secrets) remains open/canonical, `updated_at` unchanged since creation (2026-07-07T08:33 UTC) — not re-attempted, still correctly `needs-human`-gated pending a person provisioning Stripe test-mode secrets. F40 remains fully resolved (last PASS 2026-08-05). This row is a drift-check note, not a new coverage cycle. |
| F01 | 2026-08-12 | PASS | Priority list (F40/F60) checked for drift first per the charter (see row above) — no change, not re-attempted. Designated session branch (`claude/zealous-lamport-bpylmw`) had diverged from `origin/staging` (137 commits of stale "Promote staging to prod" history of its own, 85 commits behind, no unique unmerged work), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` from the fresh log (max date per flow): F01 and F42 were tied-oldest at 2026-08-03 — picked F01 first by ascending flow ID, "Sign up (create account)." Searched open PRs broadly (`list_pull_requests` open: #364/#363/#335/#332/#291/#281/#279/#199/#77/#75) — none target F01, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `signup.spec.ts` via the deterministic suite (chromium + mobile-chrome, 6 tests): signup page renders the account form; mismatched passwords rejected client-side without creating an account; a new user signs up and reaches `/onboarding`. 6/6 green (2.0m), re-ran once more for confidence (12/12 total, 1.3m), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F42 | 2026-08-12 | PASS | Priority list (F40/F60) checked for drift first per the charter: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged (`needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F60 already had a drift-check row logged earlier today (see row above), not re-logged; F40 remains fully resolved (last PASS 2026-08-05). Designated session branch (`claude/zealous-lamport-btigmh`) had 137 commits of stale already-merged "Promote staging to prod" history of its own with no unique unmerged work (its tip predated `origin/staging` by ~85 commits and wasn't an ancestor of it, consistent with squash-merge history rewrites noted in prior runs), reset to `origin/staging` first per the shared guardrails before picking a target (old remote ref had already been deleted post-merge, so the reset was pushed as a plain new-branch push, no force needed). Recomputed oldest `last_checked` from the fresh log (max date per flow): F01 was re-verified earlier today by another session, closing that slot, leaving F42 "Client completes assigned intake; coach sees it" as the sole true-oldest flow (2026-08-03). Searched open PRs/issues broadly (`list_pull_requests` state=open, `list_issues` label `qa`): #419/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75 open, plus issues #410 (F32, unrelated)/#74/#69/#57 — none target F42, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `custom-intake.spec.ts` via the deterministic suite: coach adds a required custom intake question via the real `/coach-questions` UI, client drives the real `/onboarding` wizard through the "Coach Questions" step and answers it, coach opens `/clients/<id>` and sees the answer under "Custom intake answers." 1/1 green (30.2s), re-ran once more for confidence (2/2 total, 30.0s) — no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 48 removed — accumulated from prior sessions on the shared test project). |
| F02 | 2026-08-12 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) still open/unmerged (`needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing STRIPE_* CI secrets) still open/canonical, unchanged since filing -- no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-05); F60's drift-check row was already logged earlier today (see row above), not re-logged. Designated session branch (`claude/zealous-lamport-m1icds`) had no remote ref yet, cut fresh from `origin/staging` per the shared guardrails. Recomputed oldest `last_checked` per flow across the full coverage log (max date per flow_id, since concurrent sessions append out of order): F01/F42/F60 all closed out to 2026-08-12 earlier today, leaving F02 "Log in" (2026-08-04) as the sole true-oldest flow. Searched open PRs/issues broadly (`list_pull_requests` state=open: #422/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75) -- none target F02, no in-flight duplicate to avoid. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `smoke.spec.ts` via the deterministic suite (chromium, 11 tests, covers F02 + F03 + public-page/route-protection smoke checks): "a seeded coach can log in" and "a seeded client can log in" both land on `/dashboard` or `/onboarding` off the real `/login` form via `loginAs`. 11/11 green (1.9m), re-ran F02's two login tests standalone for confidence (2/2 green, 13/13 total this run), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed -- pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 34 removed -- accumulated from prior sessions on the shared test project). |
| F43 | 2026-08-13 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) still open/unmerged (`needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing STRIPE_* CI secrets) still open/canonical, unchanged since filing -- no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-05). Designated session branch had drifted onto a stale pre-merge "Promote staging to prod" point on `main` (no unmerged work of its own), reset to `origin/staging` first per the shared guardrails before picking a target -- the stale checkout's copy of this file was 34 rows behind and would have pointed at an already-re-verified flow. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id, since concurrent sessions append out of order): F43 "Coach reviews client & leaves guidance" (2026-08-04) was the true oldest untouched flow. Also noted in passing (not this run's target, no action taken): open issue #410 (F32, "coach intermittently bounced from /leads to /dashboard onboarding-incomplete gate," filed 2026-08-11) remains open/correctly filed with repro + hypothesis, nothing new to add. Searched open PRs/issues broadly (`list_pull_requests` state=open: #425/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) -- none target F43, no in-flight duplicate to avoid. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `coach-guidance.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach reviews the client file at `/clients/<id>` and sends a guidance message via the real coach-only `conversations` INSERT path on `/clients/<id>/messages`, client (separate browser context) opens `/my-nutritionist/messages` and sees it. 1/1 green (19.7s); re-ran once more for confidence -- hit the well-documented cold-start login-timeout flake in `loginAs()` (`fixtures.ts:60`) on the first attempt, distinct from the #57 MITM-proxy `net::ERR_CONNECTION_RESET` signature (page rendered, just slow to redirect); re-ran a third time clean, 1/1 green (17.4s). 2/3 fully clean runs, 1 documented environmental flake, no product regression, no code changes needed -- pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 30 removed -- accumulated from prior sessions on the shared test project). |
| F40 | 2026-08-13 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) still open/unmerged (`needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing STRIPE_* CI secrets) still open/canonical, unchanged since filing -- no drift, not re-attempted. Designated session branch (`claude/zealous-lamport-trcgkm`) had no PR of its own and had diverged from `origin/staging` (tip was an old "Promote staging to prod" point, none of its history unique/unmerged), reset to `origin/staging` first per the shared guardrails before picking a target -- the stale checkout's copy of this file was ~130 rows behind. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F40 "Coach invites client / client accepts" (2026-08-05) was the true oldest untouched flow, older than the F03/F50/F51 tied group (2026-08-06) -- consistent with the F41 (row 210) precedent of F40 continuing to be pooled in the base round-robin once resolved, not excluded from it. Searched open PRs/issues broadly (`list_pull_requests` state=open: #432/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) -- none target F40, no in-flight duplicate to avoid; issue #69 (RESEND_API_KEY gap underlying F40's invite-creation mocking) remains open/canonical, not re-filed. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `invite-accept.spec.ts` via the deterministic suite: coach sees the pending invite on `/clients`, client accepts via the real `/invite/accept` UI, relationship persists (`personal_trainer_id` + role), coach sees the client as active. 1/1 green (19.1s), re-ran once more for confidence (2/2 total, 17.2s), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed -- pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed -- accumulated from prior sessions on the shared test project). |
| F05 | 2026-08-13 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) still open/unmerged (`needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing STRIPE_* CI secrets) still open/canonical, unchanged since filing -- no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-05); F60 already got a fresh drift-check row yesterday (2026-08-12), not re-logged. Designated session branch (`claude/zealous-lamport-5mu1go`) had zero diff from `origin/main` (identical tip, no unmerged work of its own, 96 commits behind `origin/staging`), reset to `origin/staging` first per the shared guardrails before picking a target -- the stale `main`-pinned copy of this file was ~85 rows behind and would have pointed at an already-re-verified flow (F14, last true-checked 2026-07-21 on that stale copy). Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id, since concurrent sessions append out of order): F05 "Auth redirect / route protection" (2026-08-05) was the true oldest untouched flow. Searched open PRs/issues broadly (`list_pull_requests` state=open: #429/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) -- none target F05, no in-flight duplicate to avoid. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `smoke.spec.ts`'s "logged-out users are kept out of the app" test: unauthenticated visitor hitting `/dashboard` is redirected by middleware to `/login`. 1/1 green (1.5m incl. cold build), re-ran standalone for confidence (2/2 total, 1.0m warm) -- no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed -- pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`). |
| F03 | 2026-08-14 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) still open/unmerged (`needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing STRIPE_* CI secrets) still open/canonical, unchanged since filing -- no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-k8cv4a`) had a stale unmerged "Promote staging to prod" tip (no unique history of its own), reset to `origin/staging` first per the shared guardrails before picking a target -- the stale checkout's copy of this file was ~35 rows behind. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): a three-way tie at 2026-08-06 (F03/F50/F51) was the true oldest untouched group -- picked F03, "Log out," by ascending flow ID. Searched open PRs/issues broadly (`list_pull_requests` state=open: #428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) -- none target F03, no in-flight duplicate to avoid; issue #410 (F32 intermittent bounce, filed 2026-08-11) remains open with no PR yet, consistent with the guardrail against flailing at a non-trivial bug, no action taken. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `smoke.spec.ts`'s "a logged-in user can sign out and is denied re-entry" test: seeded user logs in via the real form, signs out via the sidebar "Sign Out" button → redirected to `/login`, then a subsequent `/dashboard` visit still redirects to `/login`. 2/2 green (desktop + mobile-chrome projects), re-ran once more for confidence (4/4 total) -- no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed -- pure re-verification. No code changed, so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed -- accumulated from prior sessions on the shared test project). |
| F50 | 2026-08-14 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`issue_read`: PR #75 (billing-upgrade.spec.ts, checkout-initiation only, mocked) still open/unmerged (`needs-human`/`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing STRIPE_* CI secrets) still open/canonical, unchanged since filing -- no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-mft0sf`) had only already-merged "Promote staging to prod" history of its own, reset to `origin/staging` first per the shared guardrails before picking a target -- the stale checkout's copy of this file predated F14/F41/F60/F42/F43/F02/F05/F40/F03 entries logged since. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): with F03 re-verified earlier today by a concurrent session, F50 and F51 were left as the true oldest tied group (2026-08-06) -- picked F50, "Client leaves a review of coach," first by ascending flow ID. Searched open PRs broadly (`list_pull_requests` state=open: #442/#438/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75) -- none target F50, no in-flight duplicate to avoid. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `coach-review.spec.ts` via the deterministic suite: `createLinkedPair()` seeds a coach + linked client, client opens `/find-coach/<slug>`, rates 5 stars, posts a title + body review via the real `POST /api/coach-reviews` path, reloads and confirms the review renders plus the denormalised `rating_avg`/`rating_count` aggregate updates to "5.0" / "1 review". 1/1 green (20.0s), re-ran once more for confidence (2/2 total, 19.9s) -- no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed -- pure re-verification. No code changed, so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 54 removed -- accumulated from prior sessions on the shared test project). |
| F51 | 2026-08-14 | PASS | Same session as F50 above (already reset to `origin/staging`, priority list and duplicate-PR checks already done this run, no drift). F50 was just re-verified moments earlier, so this run closed out the other half of the tied-oldest group (2026-08-06) -- F51, "Coach responds / check-in cycle." Ran `coach-checkin.spec.ts` via the deterministic suite: `createLinkedPair()` seeds a linked coach+client, coach sends a check-in from `/clients/<id>/feedback` (default text/rating/yes-no question set), client (separate browser context) answers it on `/check-ins`, coach reopens `/clients/<id>/feedback` and sees the completed response with all three answers rendered. 1/1 green (18.2s), re-ran once more for confidence (2/2 total, 17.5s) -- no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed -- pure re-verification. No code changed, so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`). |
| F61 | 2026-08-15 | PASS | Priority list (F40/F60) checked for drift first per the charter: F40 remains fully resolved (last PASS 2026-08-13); F60 checked via `pull_request_read`(#75)/`issue_read`(#74) -- PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged -- no drift, not re-attempted. Searched open PRs/issues broadly (`list_pull_requests`, `list_issues` label `qa`): no new PR/issue targets F61 or F60; issue #410 (F32 intermittent onboarding-gate bounce) and #57 (cloud MITM-proxy login constraint) both unchanged, neither is this run's target. Designated session branch had drifted to `origin/main` (stale, 111 commits behind `origin/staging`) -- reset to fresh `origin/staging` first per the shared guardrails, which also surfaced same-day merges (up to #444) not visible from the stale checkout. Recomputed oldest `last_checked` from the up-to-date log: F10/F11/F61 were tied at 2026-08-07 by date; commit timestamps showed F61 (00:30 UTC) was checked before F10 (08:29 UTC) and F11 (16:32 UTC) that day, so F61, "Tier boundaries after upgrade/downgrade," was the true oldest untouched flow. Fresh environment this run (`npm ci` at repo root + `packages/shared` build -- `tsc` clean, `E2E_SUPABASE_*` already present in-environment pointed at the test project, confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Ran `tier-boundaries.spec.ts` via the deterministic suite: seeded free client logs in, confirmed `/cardio` and `/supplements` both show the Pro upgrade-lock UI; role force-flipped to `pro` via `upgradeUserRole()` (same technique F15/F16 use, no Stripe needed); on the next navigation to each route (same session, no re-login) both gates unlock live -- `useUser()`'s per-mount profile refetch is confirmed to re-evaluate the tier gate on role change, covering the upgrade direction of F61's live re-evaluation claim. 1/1 green (42.2s), re-ran once more for confidence (2/2 total, 51.4s) -- no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed -- pure re-verification. No code changed, so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed). |
| F10 | 2026-08-15 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): both unchanged since 2026-07-07 (`updated_at` identical), still correctly `needs-human`-gated, not re-attempted. Designated session branch (`claude/zealous-lamport-b0w3bx`) had drifted onto stale pre-merge "Promote staging to prod" history cut from `main` (137 commits ahead with no unique work, 114 behind `origin/staging`), reset to `origin/staging` first per the shared guardrails before picking a target. Checked open PRs broadly (`list_pull_requests`) — none target F10/F11; also noted issue #410 (F32 intermittent FAIL, filed 2026-08-11) remains open/canonical with no new information, not re-commented. True oldest `last_checked` group was F10/F11 (both 2026-08-07) — picked F10 first by ascending flow ID, "Client onboarding questionnaire (9 steps → dashboard)." Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Re-ran `client-onboarding.spec.ts` via the deterministic suite: questionnaire loads for a new client, full 9-step completion → `/dashboard`, free-tier gating holds on Pro-only surfaces (cardio/supplements show upgrade prompts), discover-marketplace renders for a prospective client. 8/8 green (chromium + mobile-chrome projects), re-ran once more for confidence (16/16 total), no flakes, no #57 MITM recurrence this session. No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F11 | 2026-08-15 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-86sy0l`) had drifted 137 commits behind `origin/staging` with only already-merged "Promote staging to prod" history of its own, reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F11 "Generate AI plan from intake" (2026-08-07) was the true oldest untouched flow — F10 was re-verified earlier today by a concurrent session, closing the other half of that tied-oldest pair. Searched open PRs/issues broadly (`list_pull_requests` state=open: #438/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F11, no in-flight duplicate to avoid; issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `generate-plans.spec.ts` via the deterministic suite: seeded client's onboarding populates `daily_calories`/`daily_protein`, `/generate-plans` auto-triggers all three AI routes (mocked via `page.route()`, no real OpenAI calls made — cost-aware per the flow's own notes), and the "Your plans are ready!" success state renders with saved-plan links. 1/1 green (19.7s), re-ran once more for confidence (2/2 total, 20.1s) — no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (pre-existing `<img>` warnings only, none in touched files); no code changed so `npm run build` not required. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F12 | 2026-08-16 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-b1iao4`) had only already-merged "Promote staging to prod" history of its own (no unique work), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F12/F20/F30 tied-oldest at 2026-08-08 — picked F12, "Log a meal," first by ascending flow ID. Checked open PRs/issues broadly (`list_pull_requests` state=open: #455/#438/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F12; issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `log-meal.spec.ts` via the deterministic suite: `/diet/new` renders and is gated for free tier, plus the full add-meal-with-food-item (mocked food search) → save → `/diet` flow. 4/4 green (chromium + mobile-chrome), re-ran once more for confidence (8/8 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F20 | 2026-08-16 | PASS | Priority list (F40/F60) checked for drift first per the charter: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51, and issue #74 (missing `STRIPE_*` CI secrets) also unchanged -- no new information, not re-attempted; F40 remains fully resolved. Session branch had been cut from an old `main`-lineage promotion commit instead of `staging` (137 stray commits, never pushed) -- reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow across the fresh log: F12/F20/F30 were tied-oldest at 2026-08-08; F12 was already re-verified earlier today by a separate session (#456), so this run continued the same tied group -- F20, "Coach setup questionnaire (6 steps)," next by ascending flow ID (F30 remains the one still-untouched member of that group). Checked open PRs (`list_pull_requests` state=open) for an existing F20 fix/spec in flight -- none found, no duplicate to avoid. Fresh environment this run (`npm install` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project -- confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Ran `pt-onboarding.spec.ts` via the deterministic suite: the coach setup questionnaire renders step 1/6 for a new coach, and (same spec file) a coach publishes a marketplace profile that renders publicly with verification left pending and no "Verified" badge -- 2/3 green; the third test is a pre-existing `test.fixme` for the brittle full-onboarding auto-fill (unchanged from prior runs, not a new gap). No product regression, no code changes needed -- pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. |
| F30 | 2026-08-16 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Session branch had drifted 137 commits behind `origin/staging` with only already-merged "Promote staging to prod" history of its own, reset to `origin/staging` first per the shared guardrails before picking a target — this also surfaced same-day merges (up to #461) not visible from the stale checkout, including F12/F20 both already re-verified PASS earlier today. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F12/F20/F30 were tied-oldest at 2026-08-08, and with F12/F20 both closed out earlier today, F30 — "Discover coaches renders" — was the true oldest untouched flow. Checked open PRs/issues broadly (`list_pull_requests` state=open: #461/#438/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F30, no in-flight duplicate to avoid; issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `client-onboarding.spec.ts` via the deterministic suite (this one file also covers F10/F14): full 4-test file green including "discover marketplace renders for a prospective client" — a free client navigates to `/discover` and sees the "Discover Coaches" heading. 4/4 green (chromium + mobile-chrome projects), re-ran the discover-marketplace test alone once more for confidence (2/2 more, 6/6 total for that test), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); no code changed so `npm run build` not required. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F04 | 2026-08-17 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`issue_read`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch had only already-merged "Promote staging to prod" history of its own (no unique unmerged work, tip predated `origin/staging` by ~137 commits), reset to `origin/staging` first per the shared guardrails before picking a target — the stale checkout's copy of this file was ~145 rows behind and would have pointed at an already-re-verified flow. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F04/F13/F15 tied-oldest at 2026-08-09 — picked F04 first by ascending flow ID, "Password reset request." Searched open PRs/issues broadly (`list_pull_requests` state=open: #463/#438/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F04, no in-flight duplicate to avoid; issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information, not re-commented; issue #57 (cloud MITM-proxy login constraint) unchanged since 2026-08-02. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `reset-password.spec.ts` via the deterministic suite (chromium + mobile-chrome): 8/10 green — request-form UX (`/reset-password` renders, `/login`'s "Forgot your password?" link, email-request form, "check your inbox" state) all real and deterministic on both projects. The "full loop: recovery link → set new password → sign in with it" sub-test failed on both projects the same way it consistently has since 2026-07-17 (page stuck on "Checking your reset link..." — `getByLabel('New password')` never appears), the documented `routeSupabaseThroughNode()` limitation already on canonical issue #57 (Node `fetch()` auto-follows the Supabase recovery-link redirect, collapsing the URL-fragment `access_token` before the browser ever sees it — a real user's email client does a genuine top-level navigation and hits none of this). No new information vs. the existing #57 comment thread, so not re-commented. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F13 | 2026-08-17 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch had only already-merged "Promote staging to prod" history of its own (no unique unmerged work, tip predated `origin/staging` by 137 commits), reset to `origin/staging` first per the shared guardrails before picking a target. F04 was re-verified earlier today by another session, closing that slot in the F04/F13/F15 tied-oldest group (all last checked 2026-08-09) — next by ascending flow ID, F13, "Log a workout." Searched open PRs/issues broadly (`list_pull_requests` state=open: #468/#466/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F13, no in-flight duplicate to avoid; issue #410 (F32 intermittent onboarding-gate bounce) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `log-workout.spec.ts` via the deterministic suite: `seedTrainingDay()` seeds a one-day training plan, free client opens `/training/session/<dayId>`, fills weight+reps for every set, marks each complete, finishes the workout → redirects to `/training` (`workout_logs` row inserted). 2/2 green (chromium + mobile-chrome), re-ran once more for confidence (4/4 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F15 | 2026-08-17 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`issue_read`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, `updated_at` unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch had only already-merged "Promote staging to prod" history of its own (no unique unmerged work), reset to `origin/staging` first per the shared guardrails — the stale checkout's copy of this file was ~150 rows behind and would have pointed at already-re-verified flows. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F04 and F13 were both re-verified earlier today, closing out the rest of that tied-oldest group (F04/F13/F15, all last checked 2026-08-09) — F15, "Cardio tracking (as Pro)," was the true oldest untouched flow. Searched open PRs/issues broadly (`list_pull_requests` state=open: #465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F15, no in-flight duplicate to avoid; issue #410 (F32 intermittent onboarding-gate bounce) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `cardio-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/cardio` renders the tracking form (not the F14 upgrade-prompt lock screen), logs a cardio session end-to-end, appears in the session list. 1/1 green, re-ran once more for confidence (2/2 total), no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F21 | 2026-08-18 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): both unchanged since 2026-07-07 (`updated_at` identical), still correctly `needs-human`-gated, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-tlorln`) was 139 commits behind `origin/staging` with no unique unmerged work of its own, reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F16/F21/F22/F31 were tied-oldest at 2026-08-10. Checked open PRs broadly (`list_pull_requests` state=open: #477/#476/#475/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75) — PR #475 ("F16 re-verified PASS") is a same-day, in-flight, unmerged coverage PR from a concurrent session already covering F16, so it was left untouched to avoid duplicating that work; none of the open PRs target F21/F22/F31. `list_issues` label `qa`: #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) and #57 (cloud MITM-proxy login constraint) both unchanged, neither is this run's target. With F16 already claimed, F21 — "Publish public marketplace profile" — was next by ascending flow ID. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Ran `pt-onboarding.spec.ts` via the deterministic suite: a coach publishes a marketplace profile that renders publicly at `/find-coach/<slug>` with all profile fields visible. 2/3 green (the coach-setup-questionnaire-loads test also passed; the third is the pre-existing `test.fixme` for the brittle full-onboarding auto-fill, unchanged from prior runs, not a new gap). No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); no code changed so `npm run build` not required. |
| F22 | 2026-08-18 | PASS | Same `pt-onboarding.spec.ts` run as F21 above (one test covers both): after publishing, the coach's verification request stays pending and the public profile is asserted to show NO "Verified" badge — the trust-layer guarantee (no badge while only pending) still holds. Priority list (F40/F60) and duplicate-PR checks already done this run (see F21 row) — no drift, no in-flight duplicate for F22 either. No product regression, no code changes needed — pure re-verification. No additional lint/build run needed (same session as F21, already green). |
| F31 | 2026-08-18 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-l6qhzb`) had been cut from `main` instead of `staging` (137 stray "Promote staging to prod" commits, never pushed), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F16/F21/F22/F31 were tied-oldest at 2026-08-10; F16 has an in-flight open PR (#475, "F16 re-verified PASS") and F21/F22 were already closed out earlier today by another session (see rows above), leaving F31 — "View a public coach profile" — as the sole untouched flow in that group. Searched open PRs/issues broadly (`list_pull_requests` state=open: #476/#475/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F31 specifically, no in-flight duplicate to avoid; issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `pt-onboarding.spec.ts` via the deterministic suite (this file's "coach publishes a marketplace profile…" test drives a logged-out visitor to `/find-coach/<slug>` and asserts F31's headline text, "Request coaching →" CTA, and "← Back to discover" link all render): 2/3 non-skipped tests green (the third is the pre-existing `test.fixme` for the brittle full-onboarding auto-fill, unchanged from prior runs, not a new gap). Re-ran single-worker for confidence: 2/2 green again (4/4 total across both runs), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 28 removed — accumulated from prior sessions on the shared test project). |
| F32 | 2026-08-19 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`pull_request_read`(#75): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-b9wbfg`) had only already-merged "Promote staging to prod" history of its own (145 commits behind `origin/staging`, no unique unmerged work), reset to `origin/staging` first per the shared guardrails before picking a target — the stale checkout's copy of this file was ~150 rows behind. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F16 (2026-08-10) was oldest but has an in-flight open PR (#475, "F16 re-verified PASS") from a same-day concurrent session, so — per the standing no-duplicates precedent — it was left untouched; next was the F32/F41 tied-oldest group (2026-08-11), F32 picked by ascending flow ID, "Request / hire a coach." F32's prior row (2026-08-11) logged FAIL with issue **#410** filed (intermittent bounce from `/leads` to `/dashboard`'s onboarding-incomplete gate, hypothesized `useUser()` profile-fetch race, root cause unconfirmed) — checked it for drift first: still open, no new information, no in-flight PR referencing it, so this run re-attempted F32 per its own repro steps rather than skipping. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Ran `coach-lead-request.spec.ts` via the deterministic suite **3 times in a row** (fresh build+start each time, matching issue #410's own repro recipe, which reported ~1-in-3 failures on this pattern): free client drives the real 4-step `/find-coach/<slug>/request` wizard (Goal → Logistics → Message → Review) → `POST /api/coach-leads` → redirect to `/dashboard`; coach sees the pending lead on `/leads`, accepts it; relationship persists. 3/3 green (19.0s, 19.1s, 18.0s) — the intermittent onboarding-gate bounce did not reproduce this session. This does not close #410 (a 3-run sample can't rule out a ~1-in-3 intermittent race, and the issue's own hypothesis — `useUser()`'s profile fetch not checking `error` before `setProfile` — remains unconfirmed/uninstrumented), so left it open with no new comment (no new information to add). No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 42 removed — accumulated from prior sessions on the shared test project). |
| F21 | 2026-08-26 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, open)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, body unchanged — no drift, not re-attempted, still correctly `needs-human`-gated; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch (`claude/zealous-lamport-qh5t12`) had 137 commits of already-merged "Promote staging to prod" history of its own (no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged, 1-line coverage-only diff) — left untouched again per the standing no-duplicates precedent; F60 (2026-08-12) is the priority-override drift-check-only flow, already confirmed unchanged above, not a fresh coverage cycle; F12 (2026-08-16) has an in-flight open PR (#533, "F12 re-verified PASS," unmerged) — skipped for the same reason. That left the F21/F22/F31 tied-oldest group (2026-08-18) as the true oldest actionable slot — picked F21 first by ascending flow ID, "Publish public marketplace profile." Confirmed via `list_pull_requests`(base=staging, open, 20 open PRs reviewed) and `search_pull_requests`(F21/F22/F31 in:title) that no in-flight PR targets any of these three. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `pt-onboarding.spec.ts` via the deterministic suite: "coach publishes a marketplace profile that renders publicly with no verified badge" — coach completes setup, publishes via the real `/pt-settings` marketplace toggle, profile renders publicly at `/find-coach/<slug>` for a logged-out visitor with headline, "Request coaching →" CTA, and "← Back to discover" link all present (the full 6-step auto-fill completion test remains an intentional `test.fixme`, unchanged from prior runs, not a new gap). 2/2 non-skipped tests green under default parallel workers, re-ran single-worker for confidence: 2/2 green again (4/4 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 45 removed — accumulated from prior sessions on the shared test project). |
| F22 | 2026-08-26 | PASS | Same `pt-onboarding.spec.ts` run as F21 above (one test covers both): after publishing, the coach's verification request stays pending and the public profile is asserted to show NO "Verified" badge — the trust-layer guarantee still holds. Priority list (F40/F60) and duplicate-PR checks already done this run (see F21 row) — no drift, no in-flight duplicate for F22 either. No product regression, no code changes needed — pure re-verification. No additional lint/build run needed (same session as F21, already green). |
| F14 | 2026-08-19 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch had no unmerged work of its own (predated `origin/staging` significantly), reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id): F16 (2026-08-10) remains oldest but still has the same in-flight open PR (#475, "F16 re-verified PASS," updated 2026-08-18) from a concurrent session, so — per the standing no-duplicates precedent — left untouched again; next was the F14/F41 tied-oldest group (2026-08-11) — picked F14 first by ascending flow ID, "Pro-gating holds (cardio, supplements)." Searched open PRs/issues broadly (`list_pull_requests` state=open: #487/#486/#476/#475/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F14, no in-flight duplicate to avoid; issue #410 (F32 intermittent onboarding-gate bounce) remains open with no new information (F32 itself just re-verified 3/3 green above, did not reproduce), not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `client-onboarding.spec.ts` via the deterministic suite (this file also covers F10/F30): full 4-test file green, including "free-tier gating holds on Pro-only surfaces" — a free client hits `/cardio` and `/supplements`, sees the upgrade-prompt lock screens, not the tracking features. 4/4 green, re-ran the gating test alone once more for confidence (1/1 green, 2/2 total for F14), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F01 | 2026-08-20 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-90sjiy`) had no remote ref on `origin` and local `HEAD` predated `origin/staging`, reset fresh from `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id, since concurrent sessions append out of order): F16 (2026-08-10) remains the true oldest but still has the same in-flight open PR (#475, "F16 re-verified PASS," last updated 2026-08-18) from a concurrent session, so — per the standing no-duplicates precedent — left untouched again; next-oldest, F41 (2026-08-11), also now has an in-flight open PR (#491, "F41 re-verified PASS," updated 2026-08-19), so it was skipped too. That left a four-way tie at 2026-08-12 — F01/F02/F42/F60 — with no open PR targeting any of them (checked `list_pull_requests` state=open: #492/#491/#490/#487/#476/#475/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75); picked F01 first by ascending flow ID, "Sign up (create account)." Also confirmed issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information (F32 itself re-verified 3/3 green on 2026-08-19) and issue #57 (cloud MITM-proxy login constraint) unchanged — neither is this run's target. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `signup.spec.ts` via the deterministic suite (chromium + mobile-chrome, 6 tests): signup page renders the account form; mismatched passwords rejected client-side without creating an account; a new user signs up and reaches `/onboarding`. 6/6 green (2.5m), re-ran once more for confidence (12/12 total, 1.7m), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 14 removed — accumulated from prior sessions on the shared test project). Note for the human operator: the local coverage-log copy this session started from was stale by ~18 days (last visible row: `F32 | 2026-08-02 | PASS`), but `origin/staging`'s actual log shows the rotation ran continuously and heavily throughout that window (55 more rows since then, up to `F14 | 2026-08-19 | PASS` — the day before this run), including multiple sessions per day on several days — no sign the rotation stopped; it was a stale-checkout artifact, not a real gap. |
| F16 | 2026-08-20 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): both unchanged since 2026-07-07, still correctly `needs-human`-gated, not re-attempted. Working branch had been cut from a stale pre-promotion commit (not `origin/staging`), so reset to `origin/staging` first per the shared guardrails. F16 (`supplements-tracking.spec.ts`) was the true oldest `last_checked` (2026-08-10), so this run re-verified it via the deterministic suite: 3/4 local runs green (dev-server cold-start login flake self-resolved on retry, same class of flake previously documented on this suite — not an app regression); the passing runs show the Pro client's Supplements page, add-supplement flow, and dashboard render correctly. |
| F42 | 2026-08-20 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, mocked checkout-initiation only) still open/unmerged (`needs-human`/`agent:qa`, unchanged since 2026-07-07T09:51 UTC) and issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since 2026-07-07 — no drift, not re-attempted; F40 issue #69 also unchanged; F40 remains resolved (last PASS 2026-08-13). Designated session branch reset to fresh `origin/staging` first per the shared guardrails. Recomputed oldest `last_checked` from the fresh log (max date per flow): F41 (2026-08-11) was the true oldest but already has an in-flight `[agent:qa]` coverage PR (#491, "F41 re-verified PASS", 2026-08-19) — skipped per the no-duplicates guardrail rather than duplicated. Next-oldest actionable group at 2026-08-12: F02 (auth login, prone to the canonical cloud MITM-proxy login constraint #57), F42, and F60 (BLOCKED/`needs-human`, not actionable) — picked F42 "Client completes assigned intake; coach sees it," which has a clean deterministic spec that authenticates via seeded storage state (avoids #57). Searched open PRs (`base=staging`: #496/#491/#490/#487/#476/#475/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75) and open `qa` issues (#410/#74/#69/#57) — none target F42, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `custom-intake.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach adds a required custom intake question via the real `/coach-questions` UI, client (separate browser context) drives the real `/onboarding` wizard through the "Coach Questions" step and answers it, coach opens `/clients/<id>` and sees the response under "Custom intake answers." 1/1 green (34.0s), no flakes, no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. |
| F02 | 2026-08-21 | PASS | Priority list (F40/F60) checked for drift first per the charter: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-16hbzu`) had only already-merged "Promote staging to prod" history of its own (`f71cce5`, confirmed an ancestor of `origin/main`, no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F41 (2026-08-11) remains the true oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," updated 2026-08-19) — left untouched per the no-duplicates guardrail. Next was a four-way tie at 2026-08-12 (F01/F02/F42/F60); F01 and F42 were both re-verified yesterday (2026-08-20, see rows above) and F60 is the non-actionable priority-override flow (checked for drift only), so this run picked the sole remaining untouched flow in that group, F02, "Log in." Also found and closed open PR #475 ("F16 re-verified PASS," last updated 2026-08-18) as superseded: F16 was independently re-verified and merged on 2026-08-20 (see row above), making #475's pending coverage-log line stale — commented with the superseding row and closed it, per the no-duplicates guardrail. Searched remaining open PRs/issues broadly (`list_pull_requests` base=staging state=open: #496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F02 specifically; issue #57 (cloud MITM-proxy login constraint) is F02's own known-risk, watched for but not hit this run. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production, no `.env.local` present at all, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `smoke.spec.ts` via the deterministic suite (chromium + mobile-chrome): "a seeded coach can log in" and "a seeded client can log in" both green via the real login form against the test Supabase project, plus the full 22-test file (public pages, login form fields, route protection, sign-out). 22/22 green, re-ran the two login tests standalone for confidence: 4/4 green again (26/26 total this session), no #57 MITM-proxy recurrence. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no warnings/errors in touched scope); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed — accumulated from prior sessions on the shared test project). |
| F05 | 2026-08-21 | PASS | Priority list (F40/F60) checked for drift first per the charter (`list_pull_requests`/`list_issues`): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-4q0thq`) had only already-merged "Promote staging to prod" history of its own (`f71cce5`, an ancestor of `origin/main`, no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the true oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS") from a prior session — checked it via `pull_request_read`: still open/unmerged, its `e2e` check `cancelled` (a shared CI infra issue — `playwright install --with-deps` eating the 20-minute job budget before any test runs, per the gatekeeper's own comment on that PR and #475/#476), not a stale/superseded case like the old #124/#290/#307 precedent, so left untouched rather than duplicated or closed. Next-oldest tied group at 2026-08-13: F05/F40/F43 — F40 is the priority-list flow (already confirmed resolved above) and F43 was more recently touched in spirit, so by the standing ascending-flow-ID tie-break this run picked F05, "Auth redirect / route protection." Also found PR #475 ("F16 re-verified PASS") still open despite a prior session's comment claiming it had been closed as superseded (the close call evidently didn't take) — re-verified F16's content is already merged and logged PASS on 2026-08-20 (see row above), so closed #475 now via `update_pull_request` (comment already present from the prior session, no need to duplicate it). Searched open PRs/issues broadly (`list_pull_requests` base=staging state=open: #504/#503/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F05, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `smoke.spec.ts` via the deterministic suite (chromium): F05's own test, "logged-out users are kept out of the app" (unauthenticated `GET /dashboard` → middleware redirect to `/login`), green in the full 11-test file run; re-ran it standalone for confidence (1/1 green, 2/2 total for F05), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F43 | 2026-08-21 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-ikszm5`) had only already-merged "Promote staging to prod" history of its own (`f71cce5`, an ancestor of `origin/main`, no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the true oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS") — checked it via `pull_request_read`: still open/unmerged, `e2e` check still `cancelled` on the same shared CI-infra signature (browser-install eating the job's time budget) noted on #475/#476, unchanged since 2026-08-19 — left untouched again rather than duplicated. Next-oldest tied group at 2026-08-13 was F05/F40/F43; F05 was re-verified earlier today (see row above) and F40 is the priority-list flow (already confirmed resolved), so this run closed out the remaining slot, F43, "Coach reviews client & leaves guidance." Searched open PRs/issues broadly (`list_pull_requests` base=staging state=open: #500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F43, no in-flight duplicate; issue #410 (F32 intermittent onboarding-gate bounce) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `coach-guidance.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach reviews the client file at `/clients/<id>` and sends a guidance message via the real coach-only `conversations` INSERT path on `/clients/<id>/messages`, client (separate browser context) opens `/my-nutritionist/messages` and sees it. 1/1 green (19.2s), re-ran once more for confidence (2/2 total, 16.7s) — no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 38 removed — accumulated from prior sessions on the shared test project). |
| F03 | 2026-08-22 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch had only already-merged "Promote staging to prod" history of its own (167 commits behind `origin/staging`, no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target — the stale checkout's copy of this file was ~160 rows behind. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the true oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," updated 2026-08-19, unmerged) — left untouched again rather than duplicated. Next-oldest tied group at 2026-08-14 was F03/F50/F51 — picked F03 first by ascending flow ID, "Log out." Searched open PRs/issues broadly (`list_pull_requests` base=staging state=open: #510/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F03/F50/F51, no in-flight duplicate; issue #410 (F32 intermittent onboarding-gate bounce) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `smoke.spec.ts` via the deterministic suite: "a logged-in user can sign out and is denied re-entry" — seeded user logs in via the real form, signs out via the sidebar "Sign Out" button → redirected to `/login`, then a subsequent `/dashboard` visit still redirects to `/login` (session actually cleared, not just a UI state change). 22/22 green across the full file (chromium + mobile-chrome), re-ran the sign-out test standalone for confidence (1/1 green, 2/2 total for F03), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F50 | 2026-08-22 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch (`claude/zealous-lamport-12dij5`) had only already-merged "Promote staging to prod" history of its own (an ancestor of `origin/main`, no unique unmerged work, well behind `origin/staging`), reset to fresh `origin/staging` first per the shared guardrails. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F41 (2026-08-11) remains the true oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," updated 2026-08-19, unmerged, `e2e` check still `cancelled` on the shared CI-infra timeout signature per the gatekeeper's own comment) — left untouched rather than duplicated. F03 was re-verified earlier today by a separate concurrent session (see row above), closing that slot in the F03/F50/F51 tied-oldest group (all last checked 2026-08-14) — this run picked up F50, "Client leaves a review of coach," next by ascending flow ID. Searched open PRs broadly (`list_pull_requests` state=open): #512/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75 — none target F50/F51, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`). Ran `coach-review.spec.ts` via the deterministic suite: linked client leaves a review for their coach via the real review form, it renders on the coach's public `/find-coach/<slug>` profile. 1/1 green (50.9s), re-ran once more for confidence (2/2 total, 40.4s) — no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 32 removed — accumulated from prior sessions on the shared test project). |
| F51 | 2026-08-22 | PASS | Same session as F50 above (already reset to `origin/staging`, priority-list drift check and duplicate-PR search already done this run — no drift on F40/F60, no in-flight PR targets F51). F50 was just re-verified moments earlier, closing out the other half of the F03/F50/F51 tied-oldest group (2026-08-14) — this run closed F51, "Coach responds / check-in cycle." Ran `coach-checkin.spec.ts` via the deterministic suite (same invocation as F50, one `npm run e2e:test` call covered both spec files): coach sends a check-in to a linked client, client responds via the real check-in form in a separate browser context, coach reviews the submitted answers on `/clients/<id>`. 1/1 green (52.0s), re-ran once more alongside F50 for confidence (2/2 total, 42.2s) — no flakes, no #57 MITM-proxy recurrence. No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up as part of the shared F50/F51 cleanup pass above (32 removed total for this session). |
| F15 | 2026-08-25 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch (`claude/zealous-lamport-zede2l`) had only already-merged "Promote staging to prod" history of its own (`f71cce5`, an ancestor of `origin/main`, ~199 commits behind `origin/staging`, no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target — the stale checkout's copy of this file was ~26 rows behind (last visible row `F32 | 2026-08-02`), a stale-checkout artifact, not a real gap in the rotation (which ran continuously through the interval). Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F41 (2026-08-11) remains the oldest overall but has a same-signature in-flight open PR (#491, "F41 re-verified PASS," updated 2026-08-19, unmerged) — left untouched per the no-duplicates guardrail. Next-oldest, F12 (2026-08-16), also now has an in-flight open PR (#533, "F12 re-verified PASS," updated 2026-08-24) — skipped for the same reason. That left F15, "Cardio tracking (as Pro)" (2026-08-17), as the true oldest actionable flow. Searched open PRs/issues broadly (`list_pull_requests` state=open: #533/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` state=OPEN: #531/#423/#410/#288/#206/#74/#69/#66/#57) — none target F15, no in-flight duplicate; issue #410 (F32 intermittent onboarding-gate bounce) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production via the spec's own prod-guard in `e2e/lib/env.ts`, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `cardio-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/cardio` renders the tracking form (not the F14 upgrade-prompt lock screen), logs a cardio session end-to-end, appears in the session list. 1/1 green (11.1s), re-ran once more for confidence (2/2 total, 7.4s) — no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 15 removed — accumulated from prior sessions on the shared test project). |
| F10 | 2026-08-22 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch had no unmerged commits of its own (an ancestor of `origin/main`), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the true oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," updated 2026-08-19, unmerged) — checked via `pull_request_read`, no new information, left untouched rather than duplicated. Next-oldest tied group at 2026-08-15 was F10/F11/F61 — picked F10 first by ascending flow ID, "Client onboarding questionnaire (9 steps → dashboard)." Searched open PRs/issues broadly (`list_pull_requests` state=open: #515/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F10/F11/F61, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Ran `client-onboarding.spec.ts` via the deterministic suite: questionnaire loads for a new client, full 9-step completion → `/dashboard`, free-tier gating holds on Pro-only surfaces (cardio/supplements show upgrade prompts, not the tracking features), discover-marketplace renders for a prospective client — all four across both the `chromium` and `mobile-chrome` projects. 8/8 green first try, re-ran once more for confidence: 8/8 green again (16/16 total), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F11 | 2026-08-23 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`/`list_issues`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-13). Designated session branch had only already-merged "Promote staging to prod" history of its own (an ancestor of `origin/main`, no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F41 (2026-08-11) remains the true oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," updated 2026-08-19, unmerged) — checked via `pull_request_read`, no new information, left untouched rather than duplicated. F10 was re-verified yesterday, closing that slot in the F10/F11/F61 tied-oldest group (all last checked 2026-08-15), so this run picked the next by ascending flow ID — F11, "Generate AI plan from intake." Searched open PRs/issues broadly (`list_pull_requests` state=open: #518/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F11, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `CI=1` build+start mode). Ran `generate-plans.spec.ts` via the deterministic suite: seeded client's onboarding populates `daily_calories`/`daily_protein`, `/generate-plans` auto-triggers all three AI routes (mocked via `page.route()`, no real OpenAI calls made — cost-aware per the flow's own notes), and the "Your plans are ready!" success state renders with saved-plan links. 1/1 green (21.2s), re-ran once more for confidence (2/2 total, 20.0s), no flakes. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F40 | 2026-08-23 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, open)/`list_issues`(label `qa`, open): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; issue #69 (F40's own RESEND_API_KEY gap underlying the invite-creation mock) also unchanged, not re-filed. Designated session branch (`claude/zealous-lamport-x6grpx`) had 137 commits of already-merged "Promote staging to prod" history of its own (no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," `updated_at` 2026-08-19, unmerged) — checked it via `list_pull_requests`, no new information, left untouched again per the standing no-duplicates precedent. That makes F40 (2026-08-13) the true oldest actionable flow — "Coach invites client / client accepts," consistent with the row-217 precedent that F40 stays pooled in the base round-robin once resolved rather than excluded from it. Searched open PRs/issues broadly (`list_pull_requests` base=staging state=open: #522/#521/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F40 specifically, no in-flight duplicate; issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `invite-accept.spec.ts` via the deterministic suite: coach sees the pending invite on `/clients`, client accepts via the real `/invite/accept` UI, relationship persists (`personal_trainer_id` + role), coach sees the client as active. 1/1 green (18.9s), re-ran once more for confidence (2/2 total, 18.5s) — no flakes, no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F61 | 2026-08-23 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`/`issue_read`: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, body unchanged since filing — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch (`claude/zealous-lamport-sik39b`) held only already-merged "Promote staging to prod" history from 2026-08-02 (no unique unmerged work of its own), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," `updated_at` 2026-08-19, unmerged, 1-line coverage-only diff) — left untouched again per the standing no-duplicates precedent rather than duplicated or closed (unlike the old #124 case, this one hasn't shown any evidence of being stuck in a merge-conflict loop). That makes F61 (2026-08-15) the true oldest actionable flow — "Tier boundaries after upgrade/downgrade." Searched open PRs/issues broadly (`list_pull_requests` base=staging state=open: #525/#521/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F61, no in-flight duplicate. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `tier-boundaries.spec.ts` via the deterministic suite: seeded free client drives `/cardio` and `/supplements` through three role states via `upgradeUserRole()` — free → locked on both, upgrade to pro → both unlock on the next navigation (no re-login), downgrade back to free → both re-lock on the next navigation. 1/1 green (19.8s), re-ran once more for confidence (2/2 total, 18.2s) — no flakes, no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F04 | 2026-08-24 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, body unchanged — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch (`claude/zealous-lamport-2iw03m`) held 137 commits of already-merged "Promote staging to prod" history (no unique unmerged work of its own), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," `updated_at` 2026-08-19, unmerged, 1-line coverage-only diff) — checked via `pull_request_read`, no new information, left untouched again per the standing no-duplicates precedent. That makes the F04/F13/F15 tied group (2026-08-17) the true oldest actionable slot — picked F04 first by ascending flow ID, "Password reset request." Searched open PRs/issues broadly (`list_pull_requests` base=staging state=open: #528/#521/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F04, no in-flight duplicate; issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Re-ran `reset-password.spec.ts` via the deterministic suite: 8/10 green across `chromium` + `mobile-chrome` — `/reset-password` page renders, the "Forgot your password?" link from `/login`, the no-token email-request form, and the check-your-inbox state after submitting all pass on both projects. The "full loop: recovery link → set new password → sign in with it" sub-test failed on both projects the same way it has consistently since 2026-07-17 (page stuck waiting on `getByLabel('New password')`, which never appears) — consistent with the documented root cause already on canonical issue #57 (`routeSupabaseThroughNode()`'s Node `fetch()` auto-follows the Supabase recovery-link redirect, collapsing the URL-fragment `access_token` before the browser ever sees it; a real user's email client does a genuine top-level navigation and hits none of this). No new information vs. the existing #57 comment thread, so not re-commented — would just add noise. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`). |
| F13 | 2026-08-24 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, open)/`list_issues`(label `qa`, open): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch (`claude/zealous-lamport-4hxwgo`) had 137 commits of already-merged "Promote staging to prod" history of its own (no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged, 1-line coverage-only diff) — left untouched again per the standing no-duplicates precedent. F04 (part of the F04/F13/F15 tied group, 2026-08-17) was already re-verified earlier today (merged), so this run continued that group by ascending flow ID — F13, "Log a workout." Searched open PRs/issues broadly (`list_pull_requests` base=staging state=open: #535/#533/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — none target F13, no in-flight duplicate; issue #410 (F32 intermittent onboarding-gate bounce, filed 2026-08-11) remains open with no new information, not re-commented. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Ran `log-workout.spec.ts` via the deterministic suite: seeded client opens a training session, logs weight+reps for every set across both `chromium` and `mobile-chrome` projects, finishes the workout → redirects to `/training`. 2/2 green, re-ran once more for confidence (4/4 total), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F20 | 2026-08-25 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, open)/`pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch (`claude/zealous-lamport-jitrxz`) held 137 commits of already-merged "Promote staging to prod" history of its own (no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F41 (2026-08-11) remains nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged, 1-line coverage-only diff) — left untouched per the standing no-duplicates precedent. F60 (2026-08-12) is the priority-override drift-check-only flow, already confirmed unchanged above — not a fresh coverage cycle. F12 (part of the F12/F20/F30 tied group, 2026-08-16) already has an in-flight open PR (#533, "F12 re-verified PASS," unmerged, updated 2026-08-24) — skipped to avoid duplicating in-flight work, per the same precedent applied to F41. That left F20, "Coach setup questionnaire (6 steps)," as the true oldest actionable, untouched target (F30 also open, ascending-ID tie-break picks F20 first). Confirmed via `list_pull_requests`(base=staging, open) and `list_issues`(label `qa`, open) that no other in-flight PR/issue targets F20 specifically. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `pt-onboarding.spec.ts` via the deterministic suite: "the coach setup questionnaire loads for a new coach" (step 1/6 + first prompt render) and "coach publishes a marketplace profile that renders publicly with no verified badge" (which also covers F21/F22) both green; the full 6-step auto-fill completion test remains an intentional `test.fixme` (documented brittleness, better covered by the agentic coach-onboarding mission) — not attempted, no regression from it. 2/2 non-skipped tests green under default parallel workers, re-ran single-worker for confidence: 2/2 green again (4/4 total), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 36 removed — accumulated from prior sessions on the shared test project). |
| F30 | 2026-08-25 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch had 137 commits of already-merged "Promote staging to prod" history cut from `main` (no unique unmerged work of its own), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged, 1-line coverage-only diff) — left untouched per the standing no-duplicates precedent. F12 (part of the F12/F20/F30 tied group, 2026-08-16) already has an in-flight open PR (#533, "F12 re-verified PASS," unmerged) — also skipped. `search_pull_requests` confirmed no open PR targets F30 specifically, so this run picked F30, "Discover coaches renders," as the true oldest actionable, untouched flow (a separate concurrent session picked F20 from the same tied group earlier today, logged just above). Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL host, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, dev-mode webServer). Ran `client-onboarding.spec.ts`'s "discover marketplace renders for a prospective client" test: a free client navigates to `/discover` and the "Discover Coaches" heading renders. 2/2 green across `chromium` + `mobile-chrome`, re-ran once more for confidence: 2/2 green again (4/4 total), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 46 removed — accumulated from prior sessions on the shared test project). |
| F31 | 2026-08-26 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch (`claude/zealous-lamport-gckmwy`) had 137 already-merged "Promote staging to prod" commits of its own and no unique unmerged work, reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged) — left untouched per the standing no-duplicates precedent; F12 (2026-08-16) likewise still has its in-flight open PR (#533, "F12 re-verified PASS," unmerged, updated 2026-08-24) — also skipped. That left F31, "View a public coach profile" (last checked 2026-08-18), as the true oldest actionable, untouched flow. `list_pull_requests`(base=staging, open) confirmed no other in-flight PR targets F31 specifically. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL host, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Ran `pt-onboarding.spec.ts` via the deterministic suite — this is the spec that also covers F21/F22 and extends into F31's actual assertions: a logged-out visitor hitting `/find-coach/<slug>` sees the coach's headline text, the "Request coaching →" CTA link, and the "← Back to discover" link all render (the intentional `test.fixme` 6-step-completion stub still skipped, no regression from it). 2/2 non-skipped tests green under default parallel workers, re-ran single-worker for confidence: 2/2 green again (4/4 total), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 34 removed — accumulated from prior sessions on the shared test project). |
| F14 | 2026-08-26 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, open)/`list_issues`(label `qa`, open): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted, still correctly `needs-human`-gated; F40 remains fully resolved. Designated session branch (`claude/zealous-lamport-i0txvu`) held 137 commits of already-merged "Promote staging to prod" history of its own (no unique unmerged work), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged since 2026-08-19) — confirmed via `pull_request_read`(#491) it's genuinely stuck on a CI-infra issue unrelated to the diff (gatekeeper comment: the `e2e` job's 20-minute budget gets consumed by setup/browser-install before the 95-test suite can run, same signature as #475/#476/#281 — already flagged by the janitor's 2026-08-24 digest, issue #531, not a new finding), so left untouched per the standing no-duplicates precedent rather than duplicated; F12 (2026-08-16) likewise still has its in-flight open PR (#533, "F12 re-verified PASS," unmerged, last gatekeeper comment 2026-08-26 citing `e2e` check failure on the head commit) — also skipped. That left the F14/F32 tied-oldest group (both last checked 2026-08-19) as the true oldest actionable — picked F14 first by ascending flow ID, "Pro-gating holds (cardio, supplements)." Confirmed via `list_pull_requests`(base=staging, open) and `list_issues`(label `qa`, open) that no other in-flight PR/issue targets F14 specifically. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL host, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `client-onboarding.spec.ts` via the deterministic suite (this file also covers F10/F30): full 4-test file green across both `chromium` and `mobile-chrome` projects (8/8), including "free-tier gating holds on Pro-only surfaces" — a free client hits `/cardio` and `/supplements`, sees the upgrade-prompt lock screens, not the tracking features. Re-ran the gating test alone once more for confidence (2/2 green, 10/10 total this run), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 22 removed — accumulated from prior sessions on the shared test project). |
| F32 | 2026-08-27 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, body unchanged — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch had no unmerged commits of its own (an ancestor of `origin/main`), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged since 2026-08-19, stuck on the CI `e2e`-job time-budget infra issue tracked in #531) — left untouched per the standing no-duplicates precedent; F12 (2026-08-16) likewise still has its in-flight open PR (#533, "F12 re-verified PASS," unmerged) — also skipped. F14 was re-verified earlier today (2026-08-26 row above), closing that half of the F14/F32 tied-oldest group, so this run picked up F32, "Request / hire a coach." `list_pull_requests`(base=staging, open) confirmed no in-flight PR targets F32. Before running, checked `issue_read`(#410) — an open `qa`-labeled bug filed 2026-08-11: `coach-lead-request.spec.ts` intermittently (~1-in-3 in that report) fails on "coach sees the pending lead on /leads," landing on `/dashboard`'s onboarding-incomplete gate instead; root cause is an unconfirmed hypothesis about `useUser()`'s profile fetch transiently resolving a stale/falsy role on first render. No PR addresses it yet, and root-causing it needs instrumentation, not something to flail at within this run — so the plan was to re-run enough to check for the reported flake and reference #410 rather than re-file if it reproduced. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL host, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Ran `coach-lead-request.spec.ts` via the deterministic suite twice in a row (fresh build+start each time, matching #410's repro recipe): free client drives the real 4-step `/find-coach/<slug>/request` wizard → `POST /api/coach-leads` → redirect to `/dashboard`; coach sees the pending lead on `/leads`, accepts it; relationship persists (`personal_trainer_id` + role). 1/1 green both times (2/2 total, 24.5s + 19.4s) — #410's intermittent redirect did not reproduce this run. Not closing #410 off two green runs (its own report notes 2/3 green historically; this doesn't disconfirm the bug, just didn't catch it this time) — left open, no comment added (no new information beyond what's already documented there). No product regression, no code changes needed. `npm run lint -w apps/web` passes (no ESLint warnings or errors); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 45 removed — accumulated from prior sessions on the shared test project). |
| F01 | 2026-08-27 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, open)/`list_issues`(label `qa`, open): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted, still correctly `needs-human`-gated; F40 remains fully resolved (last checked 2026-08-23). Designated session branch was 214 commits behind `origin/staging` with no unique unmerged work of its own, reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F41 (2026-08-11) remains nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged since 2026-08-19) — left untouched per the standing no-duplicates precedent; F12 (2026-08-16) likewise still has its in-flight open PR (#533, "F12 re-verified PASS," unmerged since 2026-08-24) — also skipped. That left the F01/F16/F42 tied-oldest group (all last checked 2026-08-20) as the true oldest actionable — picked F01 first by ascending flow ID, "Sign up (create account)." `list_pull_requests`(base=staging, open) confirmed no in-flight PR targets F01, F16, or F42 specifically. Also noted in passing (not this run's target, no action taken): open issue #410 (F32 intermittent `/leads`→`/dashboard` redirect) remains open with no fix PR — consistent with yesterday's F32 re-verification, which didn't reproduce it either; left alone. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL host (`kxicmpypviufdrppmuyx.supabase.co`), no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `signup.spec.ts` via the deterministic suite: signup page renders the account form, mismatched-password validation rejects client-side without creating an account, full signup → `/onboarding` — all three across both `chromium` and `mobile-chrome` projects. 6/6 green, re-ran once more for confidence: 6/6 green again (12/12 total), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` not run this session (no code changed, spec-only re-verification against an already-merged spec, consistent with prior no-code-change runs); `npm run build` likewise not required. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 57 removed — accumulated from prior sessions on the shared test project). |
| F16 | 2026-08-27 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, open, label `agent:qa`)/`list_issues`(label `qa`, open): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted, still correctly `needs-human`-gated; F40 remains fully resolved (last checked 2026-08-23). Designated session branch was pinned to a stale "Promote staging to prod" commit far behind `origin/staging`, reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F41 (2026-08-11) remains nominal oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS," unmerged since 2026-08-19) — left untouched per the standing no-duplicates precedent; F12 (2026-08-16) likewise still has its in-flight open PR (#533, "F12 re-verified PASS," unmerged since 2026-08-24) — also skipped. F01 was re-verified earlier today, closing one slot in the F01/F16/F42 tied-oldest group (all last checked 2026-08-20), so this run picked the next by ascending flow ID — F16, "Supplements tracking (as Pro)." `list_pull_requests`(base=staging, state=open) and `list_issues`(label `qa`, state=OPEN) confirmed no in-flight PR/issue targets F16 specifically (open agent:qa PRs are only #491/F41, #533/F12, and #75/F60). Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production via the `e2e/lib/env.ts` prod-guard (no `.env.local`/`.env.e2e` present, guard passed), `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `supplements-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/supplements` renders the tracking UI (not the F14 upgrade-prompt lock screen), adds a supplement, marks it taken (`aria-pressed="true"`). 1/1 green, re-ran once more for confidence (2/2 total), no flakes. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web`/`npm run build -w apps/web` not run this session (no code changed, spec-only re-verification against an already-merged spec). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 34 removed — accumulated from prior sessions on the shared test project). |
| F16 | 2026-08-28 | PASS | Priority list (F40/F60) checked for drift: PR #75 (`billing-upgrade.spec.ts`) and issue #74 (Stripe CI secrets gap) both still open/unmerged/canonical, `updated_at` unchanged since 2026-07-07 — not re-attempted, still `needs-human`-gated. `list_pull_requests`(base=staging, open) confirmed the same two in-flight coverage PRs already flagged by prior runs remain unmerged: #491 ("F41 re-verified PASS," nominal oldest at 2026-08-11) and #533 ("F12 re-verified PASS," 2026-08-16) — both left untouched per the standing no-duplicates precedent, so this run followed the same chain those prior sessions established and picked the next oldest actionable flow. Designated branch (`claude/zealous-lamport-fkns2i`) had 137 stale already-merged commits with no unique work, reset to fresh `origin/staging` before picking a target. **Went further on #533 than prior sessions did**: pulled its failing `e2e` job log (run 32706488084) instead of just noting the gatekeeper's "check is failing" comment. Root cause is NOT infra/timeout (unlike #491's separate, already-tracked #531 issue) — it's a real, reproducible bug in `supplements-tracking.spec.ts` itself: `page.getByText('Vitamin D3')` is a strict-mode-ambiguous locator that intermittently matches both the new supplement's list heading AND the `toast.success(\`${form.name} added\`)` success toast (`apps/web/src/app/(app)/supplements/page.tsx:101`) when the toast hasn't dismissed yet — CI's job log showed the exact failure (`strict mode violation: getByText('Vitamin D3') resolved to 2 elements`) on both the initial attempt and its retry. Reproduced against the test Supabase project: passed 1/1 on a single local run but the ambiguity is real and timing-dependent (Playwright's own error suggested the fix: scope to the heading role). Since every future `agent:qa` coverage PR runs the full e2e suite and this makes it fail unpredictably in CI (blocking merges across the whole pipeline, not just #533), and the fix is a trivial, obviously-correct test-only locator change (no product code touched), fixed it directly rather than filing an issue: `apps/web/e2e/specs/supplements-tracking.spec.ts` now asserts `page.getByRole('heading', { name: 'Vitamin D3' })` instead of the ambiguous text match. Verified: 1 + 3 + 4 = 8/8 green across three separate runs (including `--repeat-each=4` under 2 parallel workers to stress the toast-timing race), `CI=1` build+start mode matching the CI job that failed. `npm run lint -w apps/web` passes (no warnings); `npm run build -w apps/web` passes. Left a comment on #533 pointing at this PR as the root-cause fix rather than touching that PR directly (it's another session's open coverage PR). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed). |
| F42 | 2026-08-28 | PASS | Priority list (F40/F60) checked for drift first per the charter: `pull_request_read`(#75) and `issue_read`(#74) both unchanged since 2026-07-07T08:33/09:51 UTC — no drift, not re-attempted, still correctly `needs-human`-gated; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch (`claude/zealous-lamport-0bnjbx`) was pinned to a stale pre-promotion commit unrelated to `origin/staging`, reset to fresh `origin/staging` before picking a target. `list_pull_requests`(base=staging, open) showed the nominal oldest flows already have in-flight coverage PRs from other sessions — #491 ("F41 re-verified PASS," oldest at 2026-08-11, blocked on the tracked CI time-budget infra issue #531) and #533 ("F12 re-verified PASS," 2026-08-16) — both left untouched per the standing no-duplicates precedent rather than duplicated. (Ran `assign-plan.spec.ts`/F41 once before checking for the in-flight PR and confirming it was a duplicate of #491's already-established PASS; discarded, no PR opened for it.) With F41/F12/F16 (re-verified earlier today) all excluded, the next actionable oldest is F42, "Client completes assigned intake; coach sees it" (last checked 2026-08-20) — `search_pull_requests` confirmed no in-flight PR targets F42. Fresh environment this session (`npm install` at repo root; `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL host `kxicmpypviufdrppmuyx.supabase.co`, no `.env.local`/`.env.e2e` present; `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium` since the preinstalled Chromium revision didn't match this checkout's `@playwright/test` pin; `CI=1` build+start mode). Ran `custom-intake.spec.ts` via the deterministic suite: coach adds a custom intake question on `/clients/<id>/settings`, client (separate browser context) answers it as part of the real 9-step onboarding questionnaire, coach sees the client's response on the client-detail page. 2/2 green (30.2s + 29.8s), no flakes. No product regression, no code changes needed — pure re-verification. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 28 removed — accumulated from prior sessions on the shared test project). |
| F14 | 2026-08-28 | PASS | Priority list (F40/F60) checked for drift first per the charter: `pull_request_read`(#75) and `issue_read`(#74) both unchanged since 2026-07-07T08:33/09:51 UTC — no drift, not re-attempted, still correctly `needs-human`-gated; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch had drifted onto an unrelated pre-promotion commit ~137 commits off `origin/staging` with no unique work of its own, reset to fresh `origin/staging` before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F14 (2026-07-21) was the true oldest actionable flow — `list_pull_requests`(base=staging, open) confirmed no in-flight PR targets F14 (open coverage/agent:qa PRs are only #491/F41, #533/F12, and #75/F60, none of which are F14). Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production via the `e2e/lib/env.ts` prod-guard (no `.env.local`/`.env.e2e` present), `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`). Ran `client-onboarding.spec.ts` via the deterministic suite (`--workers=1`): full 4-test file passed across both `chromium` and `mobile-chrome` projects (8/8), including "free-tier gating holds on Pro-only surfaces" — a free client hits `/cardio` and `/supplements` and sees the Pro upgrade prompts, not the tracking features. Re-ran the gating test standalone for confidence (1/1 green, 2/2 total for F14). No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no warnings); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 49 removed — accumulated from prior sessions on the shared test project). |
| F02 | 2026-08-29 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch (`claude/zealous-lamport-jy93ae`) had no unmerged work of its own and its tip predated current `origin/staging` by hundreds of commits, reset to `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow across the fresh log (max date per flow_id), also accounting for in-flight open coverage PRs (which supersede the merged log's date for that flow): F41 (merged log: 2026-08-11) has open PR #491 ("F41 re-verified PASS", updated 2026-08-19, still unmerged) and F12 (merged log: 2026-08-16) has open PR #533 ("F12 re-verified PASS", updated 2026-08-28, still unmerged) — both left untouched per the standing no-duplicates precedent. With those excluded, the oldest-`last_checked` tied group was F02/F05/F43 (2026-08-21); by the ascending-flow-ID convention, this run took F02, "Log in." Searched open PRs/issues broadly (`list_pull_requests` state=open: #575/#569/#533/#500/#496/#491/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75; `list_issues` label `qa`: #410/#74/#69/#57) — issue #57 is F02's own canonical cloud-login-constraint issue (browser→Supabase auth resets under the cloud MITM proxy), already known and not re-filed; no other open item targets F02. Fresh environment this session (`npm install` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode). Ran `smoke.spec.ts`'s "a seeded coach can log in" / "a seeded client can log in" (drives the real `/login` form via `loginAs()`, not a mocked bypass): both seeded users land on `/dashboard`/`/onboarding`. 2/2 green, re-ran once more for confidence (4/4 total), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F05 | 2026-08-29 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch (`claude/zealous-lamport-7h8jnr`) was pinned to a stale pre-promotion commit (`f71cce5`, 137 commits off with no unique work of its own), reset to fresh `origin/staging` before picking a target. Recomputed oldest `last_checked` per flow from the fresh log, accounting for in-flight open coverage PRs superseding the merged log's date: F41 (2026-08-11) still has open PR #491 ("F41 re-verified PASS", unmerged since 2026-08-19) and F12 (2026-08-16) still has open PR #533 ("F12 re-verified PASS", unmerged since 2026-08-28) — both left untouched per the standing no-duplicates precedent (confirmed via `list_pull_requests`, state=open, base=staging: no other in-flight PR targets either flow). With those excluded and F02 taken earlier today, the next oldest actionable flow was F05, "Auth redirect / route protection" (2026-08-21). Fresh environment this session (`npm install` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode). Ran the full `smoke.spec.ts` file via the deterministic suite: "logged-out users are kept out of the app" (hitting `/dashboard` and `/onboarding` while unauthenticated redirects to `/login`) and "a logged-in user can sign out and is denied re-entry" both green, alongside the other 9 smoke tests (11/11 total, single run, no flakes, no #57 MITM-proxy recurrence). No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed — accumulated from prior sessions on the shared test project). |
| F43 | 2026-08-29 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-23). Recomputed the oldest `last_checked` per flow from a fresh `origin/staging` checkout (max date per flow_id, since concurrent sessions append out of order): F41 (2026-08-11) remains the oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS", unmerged since 2026-08-19) and F12 (2026-08-16) still has open PR #533 ("F12 re-verified PASS", unmerged since 2026-08-28) — both left untouched per the standing no-duplicates precedent (confirmed via `list_pull_requests`, state=open, base=staging: neither has a competing PR, and no open `qa`-labeled issue targets either flow). With those excluded, the next oldest was F43, "Coach reviews client & leaves guidance" (2026-08-21) — no open PR/issue targets it. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL and the spec'''s own prod-guard in `e2e/lib/env.ts`; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode per the established cold-start-flake precedent). Ran `coach-guidance.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach reviews the client file at `/clients/<id>` and sends a guidance message via the real coach-only `conversations` INSERT path on `/clients/<id>/messages`, client (separate browser context) opens `/my-nutritionist/messages` and sees it. 1/1 green, re-ran once more for confidence (2/2 total), no flakes. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 37 removed — accumulated from prior sessions on the shared test project). |
| F03 | 2026-08-30 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since filing — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-23). Recomputed the oldest `last_checked` per flow from a fresh `origin/staging` checkout (max date per flow_id): F41 (2026-08-11) remains oldest but still has the same in-flight open PR (#491, "F41 re-verified PASS", unmerged since 2026-08-19) and F12 (2026-08-16) still has open PR #533 ("F12 re-verified PASS", unmerged since 2026-08-29) — both left untouched per the standing no-duplicates precedent (confirmed via `list_pull_requests`, state=open: neither has a competing PR). With those excluded, the oldest-`last_checked` tied group was F03/F10/F50/F51 (all 2026-08-22); by the ascending-flow-ID convention, this run took F03, "Log out." No open PR/issue targets F03 specifically. Designated session branch (`claude/zealous-lamport-uqcw16`) was pinned to a stale pre-promotion commit (`f71cce5`, 237 commits behind with no unique work of its own), reset to fresh `origin/staging` before picking a target. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode per the established cold-start-flake precedent). Ran `smoke.spec.ts`'s "a logged-in user can sign out and is denied re-entry": seeded user logs in via the real form, signs out via the sidebar "Sign Out" button → redirected to `/login`, then a subsequent `/dashboard` visit still redirects to `/login`. First pass was part of a full-suite run (67 passed / 2 skipped, plus the pre-existing F04 "full loop" sub-test failure already tracked on canonical issue #57 — no new information, not re-commented), then re-ran the F03 test alone against the reused server for confidence: 1/1 green again (2/2 total), no flakes, no #57 MITM-proxy recurrence targeting F03 itself. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F41 | 2026-08-30 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, unchanged since 2026-07-07; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch was pinned to a stale pre-promotion commit (~137 commits off `origin/staging`, no unique work of its own), reset to fresh `origin/staging` before picking a target. Recomputed the oldest `last_checked` per flow from the fresh log (max date per flow_id): F41, "Coach assigns custom anamnesis / plan," at 2026-08-11 was the true oldest by a wide margin (13+ days older than the next flow) — confirmed via `list_pull_requests`/`pull_request_read` that the standing block on it, open PR #491 ("F41 re-verified PASS," a 1-line coverage-log-only diff, unmerged since 2026-08-19), is not a content or merge-conflict problem: its sole `e2e` check run shows `conclusion: cancelled` from 2026-08-19T16:48 with no retry since — the gatekeeper's own comment on the PR attributes this to a CI-config issue affecting multiple PRs that week (setup/browser-install eating the 20-minute job budget before the suite can finish), not specific to this diff. At least 4 subsequent QA runs (F02/F05/F43/F03, 2026-08-29 to 2026-08-30) already found and deliberately skipped this same stuck PR under the no-duplicates guardrail; with 11 days and zero forward progress, this matches the precedent set for the old F05/#124 saga (closed 2026-07-19 after 12 stuck runs) — the content was never in question, so closed #491 as superseded by this PR rather than let F41 stay perpetually blocked. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL and the spec's own prod-guard in `e2e/lib/env.ts`; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode per the established cold-start-flake precedent). Ran `assign-plan.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, coach drives the real "New Training Plan" builder at `/clients/<id>/training/new` (name plan, add day, pick "Bench Press" from the shared exercise catalog, save) → plan shows Active on the client file; client (separate browser context) opens their own `/training` and sees the coach-assigned plan. 1/1 green (17.8s), re-ran once more for confidence (2/2 total, 16.7s), no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes (no warnings); `npm run build` not required since no code changed. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 37 removed — accumulated from prior sessions on the shared test project). |
| F10 | 2026-08-30 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, state=open)/`list_issues`(label `qa`, state=OPEN): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch (`claude/zealous-lamport-wjxd3h`) had already been merged and deleted on the remote (its own last promotion), so per the merged-branch restart rule it was recreated fresh from `origin/staging` and pushed. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F12 (2026-08-16) remains nominal oldest but still has its in-flight open coverage PR (#533, "F12 re-verified PASS," unmerged since 2026-08-24/29) — left untouched per the standing no-duplicates precedent, consistent with every prior session since 2026-08-24. With F12 excluded, the oldest-`last_checked` tied group was F10/F50/F51 (all 2026-08-22); by the ascending-flow-ID convention, this run took F10, "Client onboarding questionnaire (9 steps)." `list_pull_requests`(state=open) confirmed no other in-flight PR targets F10 specifically (open PRs: #591/#581/#569/#533/#500/#496/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75); `list_issues`(label `qa`, state=OPEN) showed #410 (F32 intermittent bounce, unrelated to F10), #74/#69/#57 (all previously known/canonical). Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production via the `e2e/lib/env.ts` prod-guard; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode). Ran `client-onboarding.spec.ts` via the deterministic suite: questionnaire loads for a new client, full 9-step completion → `/dashboard`, free-tier gating holds on Pro-only surfaces (F14), discover-marketplace renders for a prospective client (F30) — all four across both the `chromium` and `mobile-chrome` projects. 8/8 green, re-ran the full file once more for confidence: 8/8 green again (16/16 total), no flakes. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F50 | 2026-08-31 | PASS | Priority list (F40/F60) checked for drift first per the charter via `list_pull_requests`(base=staging, state=open)/`list_issues`(label `qa`, state=OPEN): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, `updated_at` unchanged since 2026-07-07T08:33 UTC — no drift, not re-attempted, still correctly `needs-human`-gated; F40 remains fully resolved. Designated branch reset to fresh `origin/staging` first per the shared guardrails (`git fetch && git checkout staging && git pull && git checkout -B claude/zealous-lamport-5yl8nl origin/staging`) before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F12 (2026-08-16, effectively 2026-08-29 with its in-flight open coverage PR #533 "F12 re-verified PASS" still unmerged) was left untouched per the standing no-duplicates precedent — confirmed via `list_pull_requests`(state=open) it's still open, `updated_at` 2026-08-29, no new information. With F12 excluded, the oldest-`last_checked` tied group was F50/F51 (both 2026-08-22, the only slot left in that group after F03/F10 closed the other two on 2026-08-30); by the ascending-flow-ID convention, this run took F50, "Client leaves a review of coach." `list_pull_requests`(base=staging, state=open) confirmed no in-flight PR targets F50 or F51 specifically (open PRs: #594/#581/#569/#533/#500/#496/#490/#487/#476/#465/#428/#364/#363/#335/#332/#291/#281/#279/#199/#77/#75); `list_issues`(label `qa`, state=OPEN) showed only #410/#74/#69/#57, none targeting F50. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL host (`kxicmpypviufdrppmuyx.supabase.co`) and the spec's own prod-guard in `e2e/lib/env.ts`, no `.env.local`/`.env.e2e` file present; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode per the established cold-start-flake precedent). Ran `coach-review.spec.ts` via the deterministic suite: `createLinkedPair()` seeds an already-linked coach+client, the linked client leaves a review for their coach via the real review form, it renders on the coach's public `/find-coach/<slug>` profile. 1/1 green (17.7s), re-ran once more for confidence (2/2 total, 18.2s) — no flakes, no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 39 removed — accumulated from prior sessions on the shared test project). |
| F51 | 2026-08-31 | PASS | Read `qa-watcher.md` + `_shared-guardrails.md` per charter. Priority list (F40/F60) checked for drift first: `list_pull_requests`/`list_issues` show PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) and issue #74 (missing `STRIPE_*` CI secrets) both still open/unmerged, `updated_at` unchanged since 2026-07-07 — no new drift, not re-attempted, still correctly `needs-human`+`agent:qa`. Designated session branch (`claude/zealous-lamport-u71ym8`) had drifted ~137 commits behind `origin/staging` (predating recent merges through PR #594), reset to `origin/staging` first per the shared guardrails — this reset also revealed the coverage log below had moved on substantially since the branch's stale copy (last entry F32/2026-08-02 in the stale copy vs. F50/2026-08-31 on `origin/staging`), so an initial pick of F14 (stale-oldest) was actually already fresh (2026-08-28) on the real log; re-ran `tier-boundaries.spec.ts` for it anyway as harmless extra verification (2/2 green) before re-deriving the true oldest `last_checked` flow from the up-to-date log. That is F51, "Coach responds / check-in cycle" (last checked 2026-08-22, tied-oldest group with F11/F40/F61 at 2026-08-23). Searched open PRs/issues broadly — no in-flight duplicates targeting F51. Fresh environment this session (`npm ci` at repo root + `packages/shared` build, `E2E_SUPABASE_*` already present in the environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `coach-checkin.spec.ts` via the deterministic suite: coach sends a check-in to a linked client, client responds via the real check-in form in a separate browser context, coach reviews the submitted answers on `/clients/<id>`. 2/2 green (17.7s + 18.5s), no flakes, no #57 MITM-proxy recurrence. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after each run (`e2e:seed cleanup`, 44 removed after the F14 extra-verification pass + 4 removed after the F51 run). |
| F12 | 2026-08-31 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing Stripe test-mode CI secrets) unchanged since creation — no drift, not re-attempted, still correctly `needs-human`-gated. F40 remains fully resolved (last PASS 2026-08-23). So this run resumed the base round-robin at the oldest untouched flow, F12 "Log a meal" (last checked 2026-08-16). Session branch was a stale local leftover not based on `staging` (had an old "Promote staging to prod" commit, not an ancestor of `origin/staging`) — reset to `origin/staging` first per the shared guardrails. Re-ran `log-meal.spec.ts` via the deterministic suite (`npm ci` + `packages/shared` build + `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, CI build+start mode): `/diet/new` renders and is gated for free tier, plus the full add-meal-with-food-item → save → `/diet` flow. 2/2 green under default parallel workers, re-ran single-worker for confidence: 2/2 green again (4/4 total). No product regression, no code changes needed — pure re-verification. `npm run lint` passes (no warnings). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed — accumulated from prior sessions on the shared test project). No open `qa`-labeled issues besides the known canonical ones (#57, #69, #74) and #410 (F32 intermittent bounce, pre-existing, unrelated to this run's flow). |
| F11 | 2026-09-01 | PASS | `generate-plans.spec.ts` green (1/1); F40/F60 priority checked, no drift, both still correctly gated (#75/#74 open). |
| F61 | 2026-09-01 | PASS | Priority list (F40/F60) checked for drift first per the charter via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — no drift, not re-attempted; F40 remains fully resolved (last PASS 2026-08-23). Designated session branch (`claude/zealous-lamport-y4cpvt`) was 259 commits behind `origin/staging` with no unique unmerged work of its own, reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F12 (2026-08-16 in the merged log) still has its in-flight open coverage PR (#533, "F12 re-verified PASS," unmerged since 2026-08-24) — left untouched per the standing no-duplicates precedent. With F12 excluded, F40 and F61 were tied-oldest at 2026-08-23 — F11 (also part of that tied group) was already re-verified earlier today by a concurrent session (row above), so this run took the remaining slot, F61, "Tier boundaries after upgrade/downgrade," rather than re-checking F40 an eleventh time with no new signal to gain. `list_pull_requests`(base=staging, state=open) confirmed no in-flight PR targets F61 specifically (open PRs: #609/#608/#581/#569/#533/#500/#496/#490/#487/#476/#465/#428/#363/#335/#332/#291/#281/#279/#199/#77/#75); `list_issues`(label `qa`, state=OPEN) showed only #410/#74/#69/#57, none targeting F61. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL host (`kxicmpypviufdrppmuyx.supabase.co`), no `.env.local`/`.env.e2e` file present; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode). Ran `tier-boundaries.spec.ts` via the deterministic suite: seeded free client drives `/cardio` and `/supplements` through three role states via `upgradeUserRole()` — free → locked on both, upgrade to pro → both unlock on the next navigation (no re-login), downgrade back to free → both re-lock on the next navigation. 1/1 green (18.4s), re-ran once more for confidence (2/2 total, 18.6s) — no flakes, no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 55 removed — accumulated from prior sessions on the shared test project). |
| F40 | 2026-09-01 | PASS | Read `qa-watcher.md` + `_shared-guardrails.md` per charter. Priority list (F40/F60) checked for drift first via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since creation — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch had an old "Promote staging to prod" commit that was not an ancestor of `origin/staging` (and no longer existed on the remote), so it was reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F40 itself was the oldest untouched flow at 2026-08-23, with no other flow tied at that date once F11/F61 (also 2026-08-23) were re-verified earlier today by concurrent sessions (rows above). `list_pull_requests`(base=staging, state=open) confirmed no in-flight PR targets F40 specifically (open PRs: #612/#581/#569/#533/#500/#496/#490/#487/#476/#465/#428/#363/#335/#332/#291/#281/#279/#199/#77/#75); `list_issues`(label `qa`, state=OPEN) showed only #410/#74/#69/#57 — #69 (RESEND_API_KEY missing) is a known pre-existing gap already worked around by `invite-accept.spec.ts` seeding the invite row directly, not a new duplicate to file. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL host (`kxicmpypviufdrppmuyx.supabase.co`), no `.env.local`/`.env.e2e` file present; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode). Ran `invite-accept.spec.ts` via the deterministic suite: coach and client (two independent browser contexts) both log in; a pending invite is seeded directly (bypassing the RESEND_API_KEY-gated email send per #69); coach sees it as "Awaiting client acceptance" on `/clients`; client accepts via the real `/invite/accept` UI and lands on `/onboarding`; the `personal_trainer_id` link persists; coach then sees the client as `active` on `/clients`. 1/1 green (34.0s), re-ran once more for confidence (2/2 total) — no flakes, no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F04 | 2026-09-02 | PASS | Read `qa-watcher.md` + `_shared-guardrails.md` per charter. Priority list (F40/F60) checked for drift first via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since creation — no drift, not re-attempted, still correctly `needs-human`-gated; F40 remains fully resolved (last PASS 2026-09-01). Designated session branch was ~260 commits behind `origin/staging` with no unique unmerged work of its own, reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id, since concurrent sessions append out of order): F04 and F13 were tied-oldest at 2026-08-24 — picked F04, "Password reset request," by ascending flow ID. `list_pull_requests`(state=open) confirmed no in-flight PR targets F04 specifically (open PRs: #615/#581/#569/#533/#500/#496/#490/#487/#476/#465/#428/#363/#335/#332/#291/#281/#279/#199/#77/#75); `issue_read`(#74) already checked above, and no other open `qa`-labeled issue names F04. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL host (`kxicmpypviufdrppmuyx.supabase.co`), no `.env.local`/`.env.e2e` file present so the prod-guard in `e2e/lib/env.ts` is a no-op here; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`; `CI=1` build+start mode). Ran `reset-password.spec.ts` via the deterministic suite: request-form UX, check-your-inbox state, and page/link rendering all real and deterministic — 8/10 green (chromium + mobile-chrome). The "full loop: recovery link → set new password → sign in with it" sub-test failed both times the same way it has since 2026-07-17 (`getByLabel('New password')` never appears — `routeSupabaseThroughNode()`'s Node `fetch()` auto-follows the Supabase recovery-link redirect, collapsing the URL-fragment `access_token` before the browser ever sees it; a real user's email client does a genuine top-level navigation and hits none of this). Re-ran once more for confidence: 8/10 again, same one sub-test both times — no new information vs. the existing canonical issue #57 comment thread, so not re-commented. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes with no warnings (`git status` clean, no code changed, so `npm run build` not required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 44 removed — accumulated from prior sessions on the shared test project). |
| F13 | 2026-09-02 | PASS | Read `qa-watcher.md` + `_shared-guardrails.md` per charter. Priority list (F40/F60) checked for drift first via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since creation — no drift, not re-attempted, still correctly `needs-human`-gated. Designated session branch had ~137 stray already-merged "Promote staging to prod" commits cut from `main` rather than `staging`, reset to fresh `origin/staging` first per the shared guardrails before picking a target. F04 was re-verified earlier today by a concurrent session (row above), closing the F04/F13 tied-oldest group (both last checked 2026-08-24) — this run took the remaining slot, F13, "Log a workout." `list_pull_requests`(state=open) confirmed no in-flight PR targets F13 specifically (open PRs: #618/#581/#569/#533/#500/#496/#490/#487/#476/#465/#428/#363/#335/#332/#291/#281/#279/#199/#77/#75); no open `qa`-labeled issue names F13 either. Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`; `E2E_SUPABASE_*` already present in-environment, pointed at the test project — confirmed non-production by URL host (`kxicmpypviufdrppmuyx.supabase.co`), no `.env.local`/`.env.e2e` file present so the prod-guard in `e2e/lib/env.ts` is a no-op here; `E2E_BASE_URL=http://localhost:3000`; `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; `CI=1` build+start mode). Ran `log-workout.spec.ts` via the deterministic suite: seeded free client logs sets for a workout session via the real `/training` logging UI and finishes → redirects to `/training`. 1/1 green (10.0s), re-ran once more for confidence (2/2 total, 10.4s) — no flakes, no `net::ERR_CONNECTION_RESET`/#57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 33 removed — accumulated from prior sessions on the shared test project). |
| F21 | 2026-09-02 | PASS | Read `qa-watcher.md` + `_shared-guardrails.md` per charter. Priority list (F40/F60) checked for drift first: PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, unchanged since 2026-07-07; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged — confirmed via `.github/workflows/e2e.yml` still has no `STRIPE_*` vars. No drift, not re-attempted (this exact state has been reconfirmed unchanged across many prior runs since 2026-07-07 — re-verifying it again this run would be pure busywork per the guardrails' no-invented-work rule). F40 remains fully resolved (last PASS 2026-09-01). Designated session branch had ~120 commits of stale already-merged history with no unique unmerged work of its own (not an ancestor of current `origin/staging`), reset to fresh `origin/staging` first per the shared guardrails before picking a target. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F60 (2026-08-12) is the priority-override drift-check-only flow, already confirmed unchanged above, not a fresh coverage cycle. That left the F21/F22/F31 tied-oldest group (2026-08-26) as the true oldest actionable slot. `search_pull_requests`(F21 OR F22 OR F31 in:title, is:open) confirmed no in-flight PR targets any of the three — picked F21 first by ascending flow ID, "Publish public marketplace profile." Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project — confirmed non-production by URL host, no `.env.local`/`.env.e2e` present, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode). Ran `pt-onboarding.spec.ts` via the deterministic suite: "coach publishes a marketplace profile that renders publicly with no verified badge" — coach completes setup, publishes via the real `/pt-settings` marketplace toggle, profile renders publicly at `/find-coach/<slug>` for a logged-out visitor with headline, "Request coaching →" CTA, and "← Back to discover" link all present (the full 6-step auto-fill completion test remains an intentional `test.fixme`, unchanged from prior runs, not a new gap). 2/2 non-skipped tests green under default parallel workers (2.7m), re-ran single-worker for confidence: 2/2 green again (2.2m) — 4/4 total, no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `git status` clean (no code changed), so `npm run lint`/`npm run build` not required this session. Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 34 removed — accumulated from prior sessions on the shared test project). |
| F15 | 2026-09-03 | PASS | Read `qa-watcher.md` + `_shared-guardrails.md` per charter. Priority list (F40/F60) checked for drift first via `pull_request_read`(#75)/`issue_read`(#74): PR #75 (`billing-upgrade.spec.ts`, checkout-initiation only, mocked) still open/unmerged, `needs-human`+`agent:qa`, `updated_at` unchanged since 2026-07-07T09:51 UTC; issue #74 (missing `STRIPE_*` CI secrets) still open/canonical, unchanged since 2026-07-07T08:33 UTC — no drift, not re-attempted, still correctly `needs-human`-gated (consistent with every prior check since filing). F40 remains fully resolved (last PASS 2026-09-01). Designated session branch (`claude/zealous-lamport-1rt6wb`) had 0 unique commits vs. `origin/main` (`git merge-base --is-ancestor HEAD origin/main` true) — confirmed it was cut from `main` instead of `staging`, reset to fresh `origin/staging` first per the shared guardrails before picking a target; this also revealed the branch's stale checkout of this file was ~100 rows behind (last visible entry F32/2026-08-02), so the initial F14 pick from that stale copy was discarded once the up-to-date log showed F14 already re-verified 2026-08-28. Recomputed oldest `last_checked` per flow from the fresh log (max date per flow_id): F60 (2026-08-12) is the priority-override drift-check-only flow, already confirmed unchanged above, not a fresh coverage cycle. That left the F15/F20/F30 tied-oldest group (2026-08-25) as the true oldest actionable slot. `search_pull_requests`(F15/F20/F30 in:title,body, is:open) confirmed no in-flight PR targets any of the three — picked F15 first by ascending flow ID, "Cardio tracking (as Pro)." Fresh environment this session (`npm ci` at repo root + `packages/shared` build via `tsc`, `E2E_SUPABASE_*` already present in-environment pointed at the test project, `E2E_BASE_URL=http://localhost:3000`, `PLAYWRIGHT_EXEC_PATH=/opt/pw-browsers/chromium`, `CI=1` build+start mode per the established cold-start-flake precedent). Ran `cardio-tracking.spec.ts` via the deterministic suite: free client force-upgraded to Pro via `upgradeUserRole()`, `/cardio` renders the tracking form (not the F14 upgrade-prompt lock screen), logs a cardio session end-to-end, appears in the session list. 1/1 green (8.4s), re-ran once more for confidence (2/2 total, 6.8s) — no flakes, no #57 MITM-proxy recurrence this session. No product regression, no code changes needed — pure re-verification. `npm run lint -w apps/web` passes with no warnings (`git status` clean, no code changed, so `npm run build` not required). Synthetic test users cleaned up after the run (`e2e:seed cleanup`, 35 removed via the F14 discarded attempt + 0 removed after the F15 run — already clean). |
